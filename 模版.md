# 数值表
## n规模算法对应表  |
> 评测机每秒上限 $10^8$ 次
| 数据规模 n | 可过复杂度 | 典型算法 |
|:--|:--|:--|
| ≤ 10–12 | O(n!) | 全排列暴力 |
| ≤ 20–25 | O(2ⁿ) 或 O(2ⁿ·n) | 子集枚举、状压 |
| ≤ 500 | O(n³) | Floyd、区间 DP |
| ≤ 5000 | O(n²) | 朴素 DP、双重循环 |
| ≤ 1e5 | O(n log n) 或 O(n√n) | 排序、堆 Dijkstra、线段树 |
| ≤ 1e6 | O(n) 或 O(n log n)（常数小） | BFS、前缀和、筛 |
| ≥ 1e8/1e9 | O(log n)、O(√n)、O(1) | 二分、数学、快速幂 
## $2^i$ 对应表
| i | 2ⁱ | 10^j接近值 | 用途记法 |
|:-:|:--|:--|:--|
| 10 | 1024 | ≈10^3 | |
| 16 | 65536 | ≈6.5\times 10^4 | |
| 17 | 131072 | ≈1.3\times 10^5 | |
| 18 | 262144 | ≈2.6\times 10^5 | |
| 19 | 524288 | ≈5.2\times 10^5 | |
| 20 | 1048576 | ≈10^6 | |
| 24 | 16777216 | ≈1.68\times 10^7 | |
| 30 | 1073741824 | ≈10^9 | |
| 31 | 2147483648 | ≈2.15\times 10^9 | *int*溢出线 |
| 32 | 4294967296 | ≈4.3\times 10^9 | unsigned 极限 |
| 40 | ≈1.1e12 | ≈1.1\times 10^12 | |
| 60 | ≈1.15e18 | ≈1.15\times 10^{18} | $10^{18}$基准线 |
| 63 | ≈9.2e18 | ≈9.22\times 10^18 | *long long*溢出线 |

# 代码模版
## 线段树(懒标记完整版)  
> 建树 $O(n)$ ，单次操作 $O(logn)$
```cpp
vector<ll> num, tree, lazy; // tree和lazy要开4倍

// 线段树本体
void push_up(int p) { tree[p] = tree[p << 1] + tree[p << 1 | 1]; }

// 懒标记处理
void apply(int p, int l, int r, ll k) {
    tree[p] += (r - l + 1) * k;
    lazy[p] += k;
}
// 乘法懒标记需要初始化为 1
// 加乘都有时，需要按 (a+b)c=ac+bc 先处理乘法懒标记再处理加分懒标记  
// 处理乘法懒标记时需要同时处理加分懒标记(给加分懒标记乘上对应值)  
// 含有覆盖操作时，需优先处理覆盖懒标记，覆盖标记需要使用额外数组，且需要同时清空其他运算懒标记

// 懒标记下传
void push_down(int p, int l, int r) {
    if (l == r || !lazy[p]) return;
    int mid = (l + r) >> 1;
    apply(p << 1, l, mid, lazy[p]);
    apply(p << 1 | 1, mid + 1, r, lazy[p]);
    lazy[p] = 0;
}

// 初始化
void build(int p, int l, int r) {
    if (l == r) {
        tree[p] = num[l];
        return;
    }
    int mid = (l + r) >> 1;
    build(p << 1, l, mid);
    build(p << 1 | 1, mid + 1, r);
    push_up(p);
}

// 区间更新
void update(int p, int l, int r, int ql, int qr, ll k) {
    if (ql <= l && r <= qr) {
        apply(p, l, r, k);
        return;
    }
    push_down(p, l, r);
    int mid = (l + r) >> 1;
    if (ql <= mid) update(p << 1, l, mid, ql, qr, k);
    if (mid < qr) update(p << 1 | 1, mid + 1, r, ql, qr, k);
    push_up(p);
}

// 区间查询
ll query(int p, int l, int r, int ql, int qr) {
    if (ql <= l && r <= qr) return tree[p];
    push_down(p, l, r);
    int mid = (l + r) >> 1;
    ll ans = 0;
    if (ql <= mid) ans += query(p << 1, l, mid, ql, qr);
    if (mid < qr) ans += query(p << 1 | 1, mid + 1, r, ql, qr);
    return ans; 
}
```

## 树状数组 O(logn)
```cpp
struct bit {
    int n;
    vector<ll> tree;
    bit(int n1): n(n1), tree(n + 2, 0) {}
    void add(int i, ll delta) {
        if (i <= 0) return;
        for (; i <= n; i += i & -i)
            tree[i] += delta;
    }
    ll query(int i) {
        ll sum = 0;
        for (; i > 0; i -= i & -i)
            sum += tree[i];
        return sum;
    }
};
```

## 最大公因数 GCD
```cpp
int gcd(int a, int b) {
    return b == 0 ? a : gcd(b, a % b);
}
```

## 最小公倍数 LCM
```cpp
int lcm(int a, int b) {
    return a / gcd(a, b) * b;
}
```

## 快速幂 O(log b)
```cpp
ll fast_pow(ll a, ll b) {
    ll res = 1;
    while (b > 0) {
        if (b & 1) res = (res * a) % MOD;
        a = (a * a) % MOD;
        b >>= 1;
    }
    return res;
}
```

## 并查集
```cpp
class UnionFold {
    private:
        /* 连通分量[不同集合]个数 */
        int count;			    //连通分量个数
        /* 每个节点的上一节点[祖先] */
        vector<int> parent;	//祖先
        /* 以每个节点为根节点的树大小 */
        vector<int> size; 		//大小 
    public:
        UnionFold(int n) : count(n), parent(n + 1), size(n + 1, 1) {
            for (int i = 0; i <= n; i++) {
                parent[i] = i;
            }
        }
        void unionSets(int p, int q){
            int rootP = find(p);int rootQ = find(q);
            if (rootP == rootQ) return;
            if (size[rootP] > size[rootQ]) {
                parent[rootQ] = rootP; size[rootP] += size[rootQ];
            } else {
                parent[rootP] = rootQ; size[rootQ] += size[rootP];
            }
            count--;
        }
        bool connected(int p, int q) { return find(q) == find(p); }
        int find(int x) {
            if (x == parent[x]) return x;
            return parent[x] = find(parent[x]);
        }
        int Count() { return count; }
};
```

## 最短路 Dijkstra
> 仅非负权可用，标准 $O((n+m) logn)$ ，分层图 $O(k(n+m)\cdot log(kn))$  
> 扩展: 0-1BFS，双端队列，仅边权为0和1使用，权0入队头，权1入队尾
```cpp
const ll INF = 4e18;
struct state {
    int node;   // 当前节点
    ll dis;     // 前往节点的距离[权值]
    state(int n, ll d): node(n), dis(d) {}
    // 小顶堆[确保升序第一个最小]
    bool operator<(const state& other) const {
        return dis > other.dis;
    }
};
vector<ll> Dijkstra(int start) {
    vector<ll> dis(n + 1, INF);
    vector<int> parent(n + 1, -1);
    priority_queue<state> q;
    q.push(state(start, 0LL));
    dis[start] = 0;
    while (!q.empty()) {
        auto [u, w] = q.top(); q.pop();
        if (dis[u] != w) continue;
        for (auto& [v, w2]: mp[u]) {
            if (w + w2 < dis[v]) {
                dis[v] = w + w2;
                parent[v] = u;
                q.push({v, w + w2});
            }
        }
    }
    return dis;
}
```

## 最近公共祖先 LCA  
> 预处理O(nlogn)，单查O(logn)
```cpp
vector<vector<int>> mp, f;  // 图和起始数组
vector<int> dep;            // 点深度
const int LOGN = 20;        // 根据点最大值设置

// 基本函数
int getLCA(int u, int v) {
    if (dep[u] < dep[v]) swap(u, v);
    // 先递归两个节点到同一高度
    for (int i = LOGN - 1; i >= 0; i--)
        if (dep[f[u][i]] >= dep[v]) u = f[u][i];
    if (u == v) return u;
    // 然后齐头并进
    for (int i = LOGN - 1; i >= 0; i--)
        if (f[u][i] != f[v][i])
            u = f[u][i], v = f[v][i];
    return f[u][0];
}

// DFS初始化
void dfs(int u, int fa, int d) {
    dep[u] = d;
    f[u][0] = fa;
    for (int i = 1; i < LOGN; i++)
        f[u][i] = f[f[u][i - 1]][i - 1];
    for (int& v: mp[u]) {
        if (v != fa) dfs(v, u, d + 1);
    }
}

// BFS初始化，防止树链过长导致爆栈
dep[s] = 1;
f[s][0] = s;
queue<int> q;
q.push(s);
while (!q.empty()) {
    int u = q.front(); q.pop();
    for (int i = 1; i < LOGN; i++)
        f[u][i] = f[f[u][i - 1]][i - 1];
    for (int& v: mp[u]) {
        if (v != f[u][0]) {
            dep[v] = dep[u] + 1;
            f[v][0] = u;
            q.push(v);
        }
    }
}
```

## 树上差分 O(n+m)
```cpp
// 需要在LCA初始化中保存访问树节点的序列，只需保证父节点比子节点先访问即可  
vector<int> diff(n + 1);
while (k--) {
    int u, v; cin >> u >> v;
    diff[u]++;  // 两节点本身加
    diff[v]++;
    int l = getLCA(u, v);
    diff[l]--;  // 两节点共同父节点和该父节点的父节点减
    if (f[l][0] != 0) diff[f[l][0]]--;
}
int ans = 0;
for (auto it = path.rbegin(); it != path.rend(); it++) {
    int& u = *it;
    ans = max(ans, diff[u]);
    // 子节点加回父节点，前缀和还原差分数组
    if (f[u][0] != 0) diff[f[u][0]] += diff[u];
}
```

## 最小生成树 MST
### Prim O((n+m) logn)
```cpp
const ll INF = 1LL << 60;
struct state {
    int u; ll w;
    bool operator<(const state& a) const {
        return w > a.w;
    }
};
vector<vector<int>> tree;       // 最终的MST
vector<int> parent(n + 1, 0);   // 每个节点的父节点

ll prim(vector<vector<pair<int, int>>>& mp, int start, int n) {
    ll sum = 0, cnt = 0;
    priority_queue<state> pq;
    vector<bool> vis(n + 1, 0);
    vector<ll> dis(n + 1, INF);
    pq.push({start, 0});
    dis[start] = 0;
    while (!pq.empty()) {
        auto [u, w] = pq.top(); pq.pop();
        if (vis[u]) continue;
        if (dis[u] != w) continue;
        vis[u] = 1;
        sum += w;
        cnt++;
        if (parent[u] != 0) {   // 保存MST
            tree[u].push_back(parent[u]);
            tree[parent[u]].push_back(u);
        }
        for (auto& [v, w1]: mp[u]) {
            if (!vis[v] && w1 < dis[v]) {
                dis[v] = w1;
                parent[v] = u;
                pq.push({v, w1});
            }
        }
    }
    return cnt == n ? sum : -1;
}
```
### Kruskal O(m logm)
```cpp
// 并查集模版

// 结构体
struct state {
    int u, v; ll w;
    bool operator<(const state& other) const {
        return w < other.w;
    }
};

// Kruskal 方法
vector<state> edge;         // 题目给与的图
vector<vector<int>> tree;   // 最终的MST
// [步骤]保存边
sort(range(edge));
ll sum = 0, cnt = 0;
UnionFold b(n);
for (auto& [u, v, w]: edge) {
    if (b.merge(u, v)) {
        sum += w;
        cnt++;
        tree[u].push_back(v);
        tree[v].push_back(u);
        if (cnt == n - 1) break;
    }
}
```

## 拓扑+DP O(n+m)
```cpp
vector<vector<int>> mp(n + 1);
vector<int> in(n + 1, 0);   // 入度计算
for (int i = 0; i < m; i++) {
    int u, v; cin >> u >> v;
    mp[u].push_back(v);
    in[v]++;
}
const ll MOD = 80112002;
vector<ll> dp(n + 1, 0);
queue<int> q;
for (int i = 1; i <= n; i++)
    if (!in[i]) {   // 根据0入度选择起点
        q.push(i);
        dp[i] = 1;
    }
while (!q.empty()) {
    int u = q.front(); q.pop();
    for (int& v: mp[u]) {
        in[v]--;    // 遇边就减入度
        dp[v] = mod(dp[u] + dp[v], MOD);
        // 入度为0才入队
        if (!in[v]) q.push(v);
    }
}
ll ans = 0;
for (int i = 1; i <= n; i++)
    if (mp[i].empty())
        ans = mod(ans + dp[i], MOD);
cout << ans;
```

## 二分
### 最大化最小值
```cpp
// 目标：找到满足 check(x)为 true 的最大 x
int left = 0, right = MAX_VAL;
while (left < right) {
    int mid = left + ((right - left + 1) >> 1); // 向上取整
    if (check(mid)) { // mid 满足条件，尝试找更大的，[mid, right)
        left = mid;
    } else { // mid 不满足，答案一定在左边，[left, mid-1]
        right = mid - 1;
    }
}
return left; // 或 right
```
### 最小化最大值
```cpp
// 目标：找到满足 check(x)为 true 的最小 x
int left = 0, right = MAX_VAL;
while (left < right) {
    int mid = left + ((right - left) >> 1); // 向下取整
    if (check(mid)) { // mid 满足条件，尝试找更小的，[left, mid]
        right = mid;
    } else { // mid 不满足，答案一定在右边，[mid+1, right)
        left = mid + 1;
    }
}
return left; // 或 right
```

# C++对拍  
> 需准备好`a.exe`(暴力)、`b.exe`(优化)、`in.txt`(输入)、`out.txt`(输出)
```cpp
mt19937_64 rng(time(0)); 
ll rnd(ll l, ll r) {    // 生成随机数
    return uniform_int_distribution<ll>(l, r)(rng);
}

void gen() {
    ofstream fout("in.txt");
    // ======
    // 根据实际情况输入修改
    int n = rnd(1, 10);
    fout << n << "\n";
    for (int i = 0; i < n; i++) {
        fout << rnd(1, 1e18) << " " << rnd(1, 1e18) << " " << rnd(1, 1e18) << "\n";
    }
    // ======
    fout.close();
}

int main(){
    for(int t = 1; t <= 100000; t++){
        gen(); // 生成数据
        if(system("a.exe < in.txt > a.txt") != 0) { // 跑暴力
            cout << "\na.exe crashed!" << endl;
            break;
        }
        auto start = chrono::high_resolution_clock::now();
        if(system("b.exe < in.txt > b.txt") != 0) { // 跑正解
            cout << "\nb.exe crashed!" << endl;
            break;
        }
        auto end = chrono::high_resolution_clock::now();
        chrono::duration<double> diff = end - start;
        if(diff.count() > 1.0 || system("fc a.txt b.txt > nul") != 0){  // 输出错误(计时超过1秒与答案不对)
            cout << "Wrong Answer!" << endl;
            cout << "Test Case: " << t << endl;
            cout << "--- Input ---" << endl;
            system("type in.txt");
            cout << "\n--- Expected (a.txt) ---" << endl;
            system("type a.txt");
            cout << "\n--- Actual (b.txt) ---" << endl;
            system("type b.txt");
            break;
        }
        cout << "Test " << t << " OK" << endl;
    }
}
```
# C++和py跨语言对拍
> 需存在`a.exe`(由`a.cpp`编译而来，优化或暴力)、`b.py`(优化或暴力)、`out_a.txt`(`a.cpp`输出)、`out_b.txt`(`b.py`输出)、`in.txt`(输入)
```py
import subprocess
import random
import os

# ================= 配置区 =================
# 1. 执行命令 (根据系统修改，Windows 用 "python", Linux/Mac 用 "python3")
CMD_A = ["./a.exe"]            # C++ 程序 (确保已编译)
CMD_B = ["pypy3", "b.py"]     # Python 程序

# 2. 文件名
FILE_IN = "in.txt"
FILE_OUT_A = "out_a.txt"
FILE_OUT_B = "out_b.txt"

# 3. 数据生成函数 (根据题目需求修改)
def generate_data():
    # 示例：随机生成 m 和 n
    q = random.randint(1, 10)
    with open(FILE_IN, "w") as f:
        f.write(f"{q}\n")
        for _ in range(q):
            l = random.randint(1, 2)
            r = random.randint(1, 10)
            f.write(f"{l} {r}\n")
# =========================================

def run_program(cmd, input_file, output_file):
    """
    运行程序，并强制其读写文件。
    如果程序本身不支持文件读写，我们通过 shell 重定向来实现。
    """
    with open(input_file, 'r') as fin, open(output_file, 'w') as fout:
        subprocess.run(cmd, stdin=fin, stdout=fout, stderr=subprocess.DEVNULL)

def main():
    # 预检查：确保 a.exe 存在
    if not os.path.exists("a.exe"):
        print("错误: 找不到 a.exe，请先编译 a.cpp")
        return

    cnt = 0
    while True:
        cnt += 1
        print(f"Testing Case #{cnt}...", end=" ")
        
        # 1. 生成输入文件 in.txt
        generate_data()
        
        # 2. 运行 a.cpp (结果 -> out_a.txt)
        run_program(CMD_A, FILE_IN, FILE_OUT_A)
        
        # 3. 运行 b.py (结果 -> out_b.txt)
        run_program(CMD_B, FILE_IN, FILE_OUT_B)
        
        # 4. 读取两个输出文件进行比对
        with open(FILE_OUT_A, 'r') as f1, open(FILE_OUT_B, 'r') as f2:
            res_a = f1.read().strip()
            res_b = f2.read().strip()
        
        if res_a == res_b:
            print(" AC")
        else:
            print(" WA!")
            print("\n" + "="*30)
            with open(FILE_IN, 'r') as f:
                print(f"Input:\n{f.read()}")
            print(f"Output A (a.cpp): {res_a}")
            print(f"Output B (b.py): {res_b}")
            print("="*30)
            break

if __name__ == "__main__":
    main()
```

# 数学理论
1. **裴蜀定理**:所有形如 $a\times x+b\times y$ 的整数，恰好是 $gcd(x, y)$ 的倍数 
2. **浮点数比较**:
```cpp
const double eps = 1e-9;
abs(a - b) < eps    // a = b
a - b > eps         // a > b
a - b < -eps        // a < b
```
3. `a << n`等价于 $a\cdot 2^n$ ，可用`(a << n) & 1`快速判断 $a$ 的第 $n$ 位是否为1

# 技巧:
1. 转盘时可以固定一个，然后枚举另外两个
2. 对于回文字符串，优先考虑枚举长度  
3. 寻找`k`的倍数构成序列，做`k`余数序列，相同余数但不同的两个位置，必然构成`k`倍数的连续序列  
4. 寻找不重叠的区间，对右端点进行升序排序寻找即可