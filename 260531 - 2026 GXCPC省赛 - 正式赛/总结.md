# 比赛实况：
1. 09:10:02交了一发G题，结果运行超时
    我的想法是对当前数t逐个遍历（用函数转字符串逐个计算加减），将符合的数存入set中
    下一个就看是否在set内，不是就先从上一个再到当前这个进行逐个遍历，判断符合放入set中
    然后再在vector中通过find寻找，输出it-a.begin()
2. 09:15:14交了一发G题，结果运行超时
    纵使vector中有序，但也应该使用lower_bound寻找第一个大于的，而不是直接用find寻找
    同时如果是新增的（当前的比上一个大），就新增完输出set的大小就行了
3. 09:22:39交了一发G题，结果运行超时
    由于样例数量我用的t变量跟每次进来的数字n用了同一个变量名，引发错误，内部n改成了val
4. 09:28:59交了一发D题，结果AC
    直接用map存储每个字符的数量，输出G、X、P、C一半的最小值即可，比去年CCPC高职场的CCPC字符拼接较容易（因为没有重叠）
5. 09:31:32交了一发G题，结果运行超时
    加了一个对于数位含0的判断，因为含0的情况下乘积为0，数位和不可能等于数字本身
6. 09:38:29交了一发A题，结果答案错误
    舍友（初入茅庐，计算机方面不懂，拉来凑人头）给出了绝妙的思路，要求给N个底数分配M个指数
    那直接给最大的底数上指数M就可以了，剩下的全为0，那么就得到公式：mx^m + n - 1
    因为不管再怎么分配，最大的数指数越大，其他的数是追赶不上来的
    快速幂模版+公式即可
7. 09:44:27交了一发A题，结果答案错误
    最终输出公式时忘加了求余，使用上您给我的求余超级公式"% md + md) % md"
8. 09:47:55交了一发G题，结果运行超时
    由于10及其以下的数，都无法满足题目条件，故从10开始枚举，枚举到val，同时对符合条件的数字计数即可
9. 09:52:15交了一发A题，结果AC
    因为我快速幂模版使用的MOD是1e9+7，题目要求的998244353，导致部分结果可能不一致，故修改为题目要求的MOD即可
    同时对最大数为998244353直接输出n-1即可
10. 10:16:38交了一发G题，结果AC
    队友直接发现了数字的规律，对于10及其以下的，是没有符合条件的；
    对于10-100之内，仅尾数为9的数字能符合条件；
    而100及其以上的，无论哪个数，都无法通过数位乘积和数位和得到数字本身
    故直接分类讨论，得到答案即可
11. 11:44:13交了一发J题，结果答案错误
    J题我先找到特例，当a数组中最大值am小于b数组最大值bm，或a数组最小值an大于b数组最小值bn，这题就无解
    因为a数组内不管怎么移动，都无法高于最大值am或低于最小值an
    同时存在总操作步数，即每个数字的代价(|a_i-b_i|)之和
    再对n=1单独判断、n=2下最大最小值相等进行判断，最后输出sum即可
12. 12:28:33交了一发F题，结果运行超时
    先判特例：s和t都有且仅有一个点（对方）
    再逐步往后模拟，先固定s，然后将t移动到下一个点x，当且仅当x同时存在与s和t的边
    执行完然后判断s和t是否变为a和b，然后再固定t执行同样操作
13. 13:19:38交了一发F题，结果答案错误
    在队友思路下，改变为了BFS寻图。从t出发寻找是否有一条路能够到达a，到达就存在a和b
14. 13:50:01交了一发J题，结果答案错误
    由于a数组内再怎么变都无法超过am和an，所以当前先保留特例判断
    然后先行对a数组内非最大最小值的数进行处理，随后再对剩下的最大最小值进行处理
    处理中若发现b[i]超范围，直接退出输出No即可
    但我这里无法照顾到一种情况，即当前数为最小值，加完后变为最大值了，下一个需要改变的最大值反而无法行动了

# 比赛题目赛时分析：
1. D.歪歪做旗帜
    类似于去年高职高专场的CCPC字符拼接，就没有CCPC字符重叠的麻烦而已
2. A.大户爱的幂和
    这题纯靠舍友的贪心思想，如果没有他的贪心思想，这题就没有做出来的可能
3. G.歪歪学数学
    这题如果队友没有发现出这个规律，很有可能到现在我都还是在怀疑我手动模拟是否有错
    当初我就想到这题必定有规律和奇妙解法，绝对不可能会手动模拟的
4. J.山脊封锁
    这题首先特例“am < bm 或 bn < an”是必然要被淘汰的
    一开始我的直接输出总代价过于鲁莽，但我感觉这个总代价也要保留
    后续我只有手动模拟每个数据，但选择不同的数据会有不同的结果，无法通过代码实现去寻找那个准确的首个操作数据
5. F.屏峰
    一开始读题后，我以为是s和t中s能到a的条件，是s、t、a都有一个公共点b，s先到b再到a
    这样就能维持一个不动然后再移动另一个，直到两人同时移动到目标点了
    但我队友反驳我说，题目是不存在环的，我这样操作是必定会存在环的
    他这个移动必定是可以由s出发，寻找是否存在一条终点为b、途径a和t的路径，但最后也错了
    后面我想着使用DP，无论是点对点的一维DP还是坐标(s,t形式)的二维DP，都无法找到转移方程
    然后我也想过，我这样死找必定会遇到死胡同，且我还是不允许回头的BFS寻找，必定会遇到死胡同
    但如果要回头的，我无法终止他，即不知道在何时、何状态进行终止
6. E.八奇炼桃都
    一开始我们三人的主要思路，就是存在多少个不重复形状的梯形
    因为上面两点和下面两点，任意两点最终组成的四条线形成的两个交点连线必然垂直于x轴，那肯定就是梯形
    但队友手解样例仅找出5种情况，无法找到题目的16种情况是如何得到的
7. H.小Z怎么又在打游戏
    这题看着简单，但也不能直接去手动枚举出所有路线，必然是DP
    但我和我队友都认为，这题用DP必然会因为10^8而超时，必然存在一个公式使得时间复杂度大大下降
8. B.大户爱的期望
    这题主要问题在三人都不知道游戏流程和期望是什么
9. K.三角形广场
    这题队友想到可以划分几类出来，即分割成两部分，但具体实现和思路我们均未找到
10. I.雨阶共鸣 和 C.盒先生的星星
    没看，盲猜是难题

# 最终询问：
1. 文件中是本次省赛的题目，你能评价一下这次省赛的题目难度如何吗？我感觉他跟区域赛难度差不多了，纵使我还没写过区域赛的题目。
2. 如上是我的提交情况和题目分析。本次比赛最终结果3题AC，罚时297，排名277。最终3题且罚时小于或等于100就有铜奖，4题就有银奖。这次我没有获得奖项。你觉得我这次表现怎么样？是不是太拉跨，队友给出了所有的思路，但我都没有方法去实现（如F和J），我是不是完全没有天赋，真的不适合走ICPC这条路啊？在还没有完全付出精力的当下，是否在面对困难时，知难而退，不做icpc这条性价比极低的道路呢？
3. 我发现我的思路很闭锁，不像我队友的思路非常发散。对于G题，我能感到有规律但找不到，不想他可以且愿意花时间找到。如果是我，这种题我都会等到比赛结束向您询问再补题。赛后他说对于K题和B题，只要给与时间他绝对能写得出来。同时赛中的很多题目，他都给与了正确的思路方向，可是我常常想要找到他思路的反例，以证明他思路的不完美，从而常常被他说否定他的思路。为什么我的思路会如此闭锁？而且就像我平时不善言语一样，是因为我不想去思考别人的心思。我在社交时不可避免地通过对方的小动作和言语来分析对方的心里状态和想法。玩游戏也是如此。对于博弈的游戏，例如洛克王国宠物争斗、王者荣耀等，因为要学习和分析宠物、英雄的各种技能和影响，不想学习从而不去玩，只愿去玩那种无脑射击、不涉及博弈的游戏，甚至斗地主我都不想碰。如果我要走ICPC这条路的话，是不是我这种闭锁思想必须要改变，必须要增加广度而非一味钻研的深度啊？今天的比赛我能说队友的很多思路我都没有对应工具，您觉得我暑假该怎么做呢？
4. 下午经学校老师找到了桂电王老师，王老师说支持我们参加ICPC网赛，且愿意介绍给ACM基地负责的老师。是不是最终是否能参赛，还得看ACM基地负责的老师，而非王老师一言拍板啊？
5. 对于第三条，我发现我自己还特别悲观和扫兴，且在悲观到一定程度（例如发现题目完全没思路时）会转移注意力，完全没有像别人一样保持注意力集中，而是会不受影响的发散（简称心飘了）。虽然这是与ICPC无关的心理问题，请问您能否教我纠正呢？
6. 这次能麻烦您仔细分析和回答，尽量长和细致，可以吗？