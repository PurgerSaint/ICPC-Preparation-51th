#include<bits/stdc++.h>
#define ios ios::sync_with_stdio(false);cin.tie(0);
using namespace std;
using ll = long long;
int main() {
    ios;
    int a, d; cin >> a >> d;
    cout << (a <= d ? "Yes" : "No");
    return 0;
}