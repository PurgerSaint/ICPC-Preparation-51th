# 比赛情况：
比赛时间: 2026-06-06 20:00:00(UTC+8)
比赛名称: ABC 461
比赛结果: [Rank]5549 [Performance]797 [New Rating]549 [Diff]+35

# 题目详情：
1. A.cpp Armor
    给与攻击值A和防御值D，问能否防御(攻击值小于或等于D)攻击值为A的伤害？
2. B.cpp The Honest Woodcutters
    河里掉了N把斧头(1,2,...,N)，第i名屠夫说第A_i把斧头是他的，但河神知道第i把斧头是第B_i名屠夫的
    问是否所有屠夫都没有说谎？
3. C.cpp Variety
    有N块宝石，每块宝石有颜色C和价值V两个参数。
    问在这N块宝石中怎么拿K块，使得最终总价值最大，要求不少于M种颜色，输出最终最大总价值。
4. D.cpp Count Subgrid Sum = K
    有H*W的矩阵，然后给与H个字符串S，S_i第j个字符代表方格第i行第j列的数字为S_i[j]
    数字仅有0和1，问最终有多少个矩阵使得矩阵内总和为K
5. E.cpp E-liter
    有N*N的矩阵，初始都为白色
    有Q个操作：
        第一种为将第R行全刷为黑色
        第二种为将第C列全刷为白色
    每个操作完输出矩阵内黑色格子的数量

# 提交详情：
- 提交时间:20:01:27 题目:A 状态:AC 详细状态:-
    直接模拟即可
- 提交时间:20:06:29 题目:B 状态:AC 详细状态:-
    直接模拟即可，题目有点绕
- 提交时间:20:26:05 题目:C 状态:WA 详细状态:所有WA
    两个最大优先队列，先把所有值按颜色和价值存入第一个队列中
    然后再在第一个队列中取出M种颜色，其中存在相同颜色的再放入另一个队列中
    接着把第二个队列的内容加回第一个队列，最后在第一个队列继续取完剩下的即可
- 提交时间:20:27:08 题目:C 状态:AC 详细状态:-
    测试输出忘记删除了
- 提交时间:20:45:08 题目:D 状态:WA 详细状态:AC*26 WA*5
    按序保存到矩阵中，然后用二维矩阵和求前缀和，然后四层循环(定一个点，再枚举另一个点)求得符合的矩阵
- 提交时间:20:50:04 题目:D 状态:WA 详细状态:AC*26 WA*5
    加入总和的判断，如果总和未到K直接输出
- 提交时间:21:05:55 题目:E 状态:TLE 详细状态:AC*22 TLE*33
    用`map<int, set<int>> mat`存储对应状态，为被涂黑的行-被涂白的列
    涂黑若存在，就清空对应set，否则加入新的
    涂白就逐个给set加入对应列
    总和每次都逐个运算
- 提交时间:21:09:27 题目:E 状态:WA 详细状态:AC*15 WA*9 TLE*31
    涂黑简便运算，预留前置变量总和`sum`，存在就是总和减去原有列再清空，不存在就直接加n
    涂白就是每个涂黑行加入对应列，然后再减去所有涂黑行的数量`mat.size()`即可

# 补题：
- D.Count Subgrid Sum = K
    对于500*500的矩阵存在高达156亿的子矩阵数量，原有int类计数变量会超出范围
    Gemini分析，换成longlong即可通过
    为什么用int？因为我认为500*500=25W没有超过int，但是我忘了这是子矩阵数量
- E.E-liter
    我的错误：嵌套太大导致时间复杂度大幅增多，且变量容易溢出
    Gemini推荐的做法：时间戳+树状数组
    将每个操作按时间序列化
    对于涂黑一行，问的是在这个操作之前，即上次行操作到当前时间内，有哪些列被涂白了
    对于涂黑一列，问的是在这个操作之前，即上次列操作到当前时间内，有哪些行被涂黑了