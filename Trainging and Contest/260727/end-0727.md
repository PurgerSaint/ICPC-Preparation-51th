1. 补题
# 1112 Div.2
> 比赛名称: 1112 Div.2
> 比赛时间: 2026/07/26 22:35-(+1)00:35
> 比赛结果: [Rank]8822 [Solved]2 [Rating-Change]-5 [New-Rating]945 
# 2250A.Threshold Movement
[Tag]模拟、数学
## 题目详情
There are $n+2$ positions numbered from $0$ to $n+1$. Initially, position $i$ contains an element of weight $w_i$ for every $1\le i\le n$, while positions $0$ and $n+1$ are empty.
You choose an integer $k$. Then every element moves exactly once, simultaneously:
-   If $w_i \lt k$, the element at position $i$ moves to position $i-1$;
-   If $w_i \gt k$, the element at position $i$ moves to position $i+1$;
-   If $w_i=k$, the entire movement process fails immediately.
An integer $k$ is perfect if the movement does not fail and, upon completion, every position from $1$ to $n$ contains exactly one element.
Determine whether a perfect integer $k$ exists.
> $1<=t<=500, 1<=n<=100, 1<=w_{i}<=10^9$
## 提交详情
- 提交时间:(07/26)22:43 状态:WA 详细状态:`Wrong answer on pretest 2`
> 题目有个关键点*每个元素恰好移动一次*，即每个元素都要移动一次  
我第一次想的想法，是构造每个元素的方向，这里我想着存在元素走多步  
然后我再看了题，每个元素都要移动，且仅能移动一次，移动完每个位置必须要有元素  
那么对于第一个元素必须向右移动，第二个元素必须向左移动  
再从样例中看出来，是否可以让奇数位置向右移动，让偶数位置向左移动，恰好两两交换  
那么就找到奇数位置的最小值、偶数位置的最大值，判断这俩是否有差值(两者之间差值是否大于1)  
- 提交时间:(07/26)22:48 状态:WA 详细状态:`Wrong answer on pretest 2`
> 第一次提交WA了，然后我想到，如果两元素差值仅有1，即两元素连续，此时也无法完成移动  
**补题**:这里其实不用加连续的检测的，因为两个连续，最后的差值检测必然无法通过
- 提交时间:(07/26)22:51 状态:AC 详细状态:`-`
> 还有一个麻烦之处我没有发现，对于偶数，上述移动才成立，而对于奇数，最后一个元素必然要移动到 $n+1$ 处，使得题目条件无法成立  
故还需另判断是否为奇数长度  

# 2250B.String Construction
[Tag]构造
## 题目详情
You are given two integers $n$$n$ and $k$$k$.
Construct a binary string$∗$$^{\text{∗}}$ $s$$s$ of length $n$$n$, such that both of the following conditions hold:
-   The absolute difference between the number of characters $0$$\mathtt{0}$ and the number of characters $1$$\mathtt{1}$ in $s$$s$ is at most $1$$1$.
-   There are exactly $k$$k$ pairs of adjacent equal characters in $s$$s$. Formally, there are exactly $k$$k$ indices $i$$i$ ($1≤i≤n−1$$1 \le i \le n-1$) satisfying $si=si+1$$s_i = s_{i + 1}$.
Or determine that no such string exists.
$∗$$^{\text{∗}}$A binary string is a string where each character is either $0$$\mathtt{0}$ or $1$$\mathtt{1}$.
> $1<=t<=1000, 2<=n<=2\times 10^5, 0<=k<=n-1$
## 提交详情
- 提交时间:(07/27)00:18 状态:WA 详细状态:`Wrong answer on pretest 2`
> 设 $a=n/2+(n\bomd 2),b=n/2$ ,`a`和`b`恰好是 $n$ 的各自一半  
要构造为 $k$ 对，那么最少需要 $k+1$ 个字符，最多需要 $2k$ 个字符  
第一次我先想到用`a`或`b`来选 $k+1$ 个字符专门用于构造，剩下字符再相邻拼接  
第二次我再找到特例， $k$ 恰好是 $n/2$ 对，即构造成`001100`这样的  
但是对于这两种，第一次的想法对于剩下的字符来说，有可能无法凑足够差值为1的`0`和`1`字符数  
第二次也是同理，对于偶数无法构成
第三次我就转换了思路，对于长度为 $n$ 的字符串，恰好能拼成 $a-1+b-1$ 即 $n-2$ 对，如果 $k$ 大于这个，就不行    
那么就可以直接将 $k$ 按`a`和`b`一样拆成两份 $x$ 和 $y$，分别用`a`和`b`进行构造  
先插入 $x+1$ 个`0`, 然后在满足 $y>0$ 的情况下再插入 $y+1$ 个`1`  
再根据剩余字符是否为偶数，在前面加入`1`，使得当前字符平衡  
最后根据剩余字符量，以`0`和`1`相接逐步拼接即可(这里也判断前面了是否加入了 $y+1$ 个`1`, 来决定`0`、`1`谁在前谁在后)
- 提交时间:(07/27)00:33 状态:WA 详细状态:`Wrong answer on pretest 2`
> 这里我是从自行找的样例 $n=10,k=1$ 找到的，第一次提交的代码会在前面多加入一个`1`，使得字符串字符数量不平衡  
第一次我是用`r > 0 && !(r % 2)`进行判断，这里`r`代表剩余构造字符  
但这里加`1`的逻辑，跟剩余字符无关，应该是跟是否 $x>y$ 有关，即当前拼接的 $x+1$ 个`0`和 $y+1$ 个`1`是否不平衡有关  
- 提交时间:(07/27)00:40 状态:WA 详细状态:`Wrong answer on pretest 2`
> 这里我还是从自行找的样例发现的 $n=1,k=0$ ，按第二次的代码会输出`-1`  
因为这个例子直接经过`n-2<k`判断直接退出了，如果给这个判断加入`n>2`，后面`x>0`改为`x>1`，这里就会阻碍任何 $k=1$ 的样例  
不如直接加入 $n=1,k=0$ 的特殊样例判断算了，然后其余的保持原样
- 提交时间:(07/27)00:51 状态:AC 详细状态:`-`
> 这里我依旧是从自行找的样例中发现的 $n=5,k=3$ ,这里 $k=3$ 对恰好用完所有字符  
因为我用的是`x>y`判断，他这里没有识别出是否有剩余字符，应该是有剩余字符才能在前面加`1`  
再加一个`r>0`判断即可  

2. 模拟
# (2025-I)G.Sorting
[Source]2025-网赛-第一场
[通过比]2146/3462
[Time]理解+尝试38:55，补题02:23
## 题目详情
You are given $m$ pairs ($a_{i}, b_{i}$), $1≤a_{i}<b_{i}≤n$, we define the following program to sort a permutation:
```cpp
for i = 1 to 1000000000000000000:
    for j = 1 to m:
        if p[a[j]] > p[b[j]]:
            swap(p[a[j]], p[b[j]])
```
Check whether it can sort every permutation $p$ of length $n$.
> $2<=n<=2\times 10^5, 1<=m<=2\times 10^5, 1<=a_{i}<=b_{i}<=n$
## 提交详情
- 提交时间:23:25:08 状态:WA 详细状态:`Test #1:WA`
> 虽然这是去年第一场，我有点预防针（知道这题是冒泡排序）  
然后我还是仔细看了一下，这题就是排序若干次下  
每次排序就是比较两两下标，前面比后面大的就交换  
本质是连通图问题  
我先是考虑并查集，但是有按大小合并，导致最后必然出现一个点大小是 $n$ ，其余所有点都是1  
然后使用单向图+Dijkstar，对于每个点 $i$ 是否能到 $i+1$ 和 $n$ 之内的所有点  
- 提交时间:23:25:08 状态:TLE 详细状态:`Test #3:TLE`
> 第一次提交看错了，是`Yes`不是全大写的`YES`
- 提交时间:23:36:51 状态:AC 详细状态:`-`
> 我看了去年提交的补题代码，直接用`set`保存，然后枚举每个 ${i,i+1}(1<=i<n)$ 是否存在即可  
因为每个点 $i$ 必须能到 $i+1$ 和 $n$ 之内的所有点  
故直接看是否相邻存在即可

# (2025-I)B.Creating Chaos
[Source]2025-网赛-第一场
[Time]理解+尝试11:51
## 题目详情
As a Cappuccino Assassino, you despise order. You want everything in the world to be as chaotic as possible.
In front of you are n cups of cappuccino, arranged in a line and numbered from 1 to n. You wish to drink k of them, so that the remaining cappuccinos are as disordered as possible.
Suppose the positions of the remaining cappuccinos are a1, a2, . . . , an−k. The degree of order is defined
as: $\sum_{n−k}^{i=1} \sum_{n−k}^{j=i+1} gcd(|a_{i} − a_{j}|, n)$ , where gcd represents the greatest common divisor.
You need to minimize this degree of order and output the sequence of cappuccinos you choose to drink.
If there are multiple answers, you can output any one of them.
> $1<=k<=n<=1000$
## 提交详情
- 提交时间:23:53:23 状态:AC 详细状态:`-`
> 这题我没有多少记忆  
要使得 $\sum_{n−k}^{i=1} \sum_{n−k}^{j=i+1} gcd(|a_{i} − a_{j}|, n)$ 最小，必然需使得 $gcd(|a_{i}−a_{j}|, n)$ 最小  
最好的情况是 $|a_{i}−a_{j}|$ 为素数，或其不含 $n$ 的因子  
但这样去枚举因子必定会导致超时，他还是差值，不好直接控制剩下 $n-k$ 个数的差值分布  
那既然这样，我试着丢弃了最大公因数这个限制，直接控制 $|a_{i}−a_{j}|$ 最小  
然后我有一个大胆的想法，直接输出前 $k$ 个数  
因为对比发现，题目给与的例子 $n=12,k=5$ ，其输出的`2 4 7 9 11`和我的极端例子`1 2 3 4 5`所带来的差值完全一样，然后我尝试加大间隔，那么必然使得总差值增大，有可能使得这个最大公因子增大

# (2025-I)A.Who Can Win
[Source]2025-网赛-第一场
[Time]思考40:25
## 题目详情
In ICPC contests, the scoreboard is frozen during the last hour. Submissions made during the freeze are shown with result Unknown.
You are given multiple submission logs from old ICPC contests. However, the results of submissions made after the freeze are hidden. For each contest, determine all teams that could possibly be the champion.
**Rules*
Each submission is represented as a tuple:
(teamname, problemid,time,result)
• teamname: a string of at most 10 characters, consisting of uppercase or lowercase letters and digits. It identifies the team that made the submission.
• problemid: a single uppercase letter (A–Z) that identifies the problem being attempted.
• time: an integer between 0 and 299, inclusive, representing the number of minutes from the start of the contest.
• result: the outcome of the submission:
– Accepted: the submission correctly solves the problem.
– Rejected: the submission does not solve the problem.
– Unknown: the submission result is unknown because it was made after the scoreboard was frozen.
– Note: a submission has result Unknown if and only if its time ≥ 240.
For a contest where all results are known, a team’s score is determined by:
1. Solved problems: the number of problems for which this team has at least one Accepted submission.
2. Penalty time: the total time consumed by all problems that the team solved (0 if none).
• For each solved problem, calculate the time as follows:
(a) Take the submission time of the team’s first Accepted submission for that problem.
(b) Add 20 minutes for each of the team’s submissions to the same problem that occurred earlier in time and had result Rejected.
• A submission is considered earlier than another if its submission time is smaller.
• Note: a team will never make more than one submission in the same minute.
A champion team is one with the highest number of solved problems and, among them, the smallest penalty time. Multiple champion teams may exist if they have the same number of solved problems and the same penalty time.
Please determine all teams that could possibly be champions for any scenario where each Unknown submission is replaced independently by either Accepted or Rejected.
> $1<=T<=10000, 1<=s<=10^5$
## 思考
> 这题是真正羊装狼的模拟题  
处理封榜前的数据就让我陷入了数据结构的深坑之中  
然后对后续的`Unknown`，要么贪心的设置为第一个为通过  
否则就只能做暴力分支，必定TLE  
## 提交详情
- 提交时间:(07/28)01:15:46 状态:AC 详细状态:`-`
> **查看历史AC代码**后才知道，这题的数据结构很多，略复杂，实际过题思路跟我的贪心一样，设置第一个未知为通过

3. 盲训
# (Div.2)2250C.Rank Subsequence
[Tag]暴力、贪心、模拟
[Time]理解+尝试20:12，补题27:21
## 题目详情
You are given $n$ elements arranged in a line, numbered $1, 2, \ldots, n$ from left to right.
You may delete any number of elements (possibly none). The remaining elements form a **subsequence** and keep their relative order. Let the length of this subsequence be $m$. If the element with original index $i$ becomes the $j$\-th element of the subsequence ($1 \le j \le m$), define
-   its left rank as $j$, and
-   its right rank as $m - j + 1$.
For each element, you are given two integer segments $[l_i, r_i]$ and $[u_i, v_i]$. The $i$\-th element is valid in a subsequence of length $m$ if and only if, when it occupies position $j$ in that subsequence, both of the following hold:
-   Its left rank is not in the range $[l_i, r_i]$ (i.e., $j \notin [l_i, r_i]$);
-   Its right rank is not in the range $[u_i, v_i]$ (i.e., $m - j + 1 \notin [u_i, v_i]$).
A subsequence is valid if every element remaining in it is valid.
Find the maximum possible length of a valid subsequence. The answer may be $0$.
> $1<=t<=5000, 1<=n<=5000, 1<=i<=n, 1<=l_{i}<=r_{i}<=n$
## 提交详情
- 提交时间:16:27 状态:WA 详细状态:`Wrong answer on test 2`
> 我是逐步模拟拿取的选择，对于当前元素的 $l,r,u,v$ ，设 $len$ 为已选取元素的个数  
如果 $len+1$ 在 $[l,r]$ 之内，就跳过当前元素  
如果当前元素的最坏情况(当前元素为子序列最后一个元素)和最好情况(子序列第 $n-i+1$ 个元素)，即 $1$ 和 $n-i+1$ 都在 $[u,v]$ 之内，也跳过  
然后根据下标来划掉部分子序列长度  
对于后缀排名为 $t$ 的子序列长度来说，设当前元素正排名为 $i$ ，则当前子序列长度为 $i+t-1$  
那么对于当前元素 $i$ 来说，标记所有 $len+j-1$ 满足 $u<=j<=v$ 为不可行  
最后直接返回 $len$ 
- 提交时间:17:03 状态:AC 详细状态:`-`
> 经*GPT*解读作者题解后，我发现作者是枚举 $m$ 并判断，类似二分的做法  
第一次提交中，我的意图为通过当前长度来选择元素，忽视了最终长度对当前元素的影响  
后续我想给第一次提交加入二次筛选，决定是否删除某些元素  
但是删除一个元素，不仅会影响最终长度，还会影响后续元素的左排名  
即这个二次筛选的做法从`当前长度`、`最终长度`扩展到后续`所有元素的存在与否`，涉及的元素更多了  
而按作者的方法，应该直接将我这个选元素的方法，放在判断当前枚举的子序列长度是否可行上，这样就能过题了  

# 2158B.Split
[Rating]1200
[Tag]构造、贪心、数学
[Time]理解+尝试47:07，补题40:58
## 题目详情
You are given a sequence $a$$a$ containing $2n$$2n$ integers. Let $f(b)$$f(b)$ denote the number of distinct elements with an odd number of occurrences in sequence $b$$b$. You need to split the given array into two disjoint subsequences $p$$p$ and $q$$q$, each of size $n$$n$, such that $f(p)+f(q)$$f(p) + f(q)$ is maximized. Output the **maximum** value.
A sequence $a$$a$ is a subsequence of a sequence $b$$b$ if $a$$a$ can be obtained from $b$$b$ by the deletion of several (possibly, zero or all) element from arbitrary positions.
> $1<=t<=10^4, 1<=n<=2\times 10^5, 1<=a_{i}<=2n$
## 提交详情
- 提交时间:18:08 状态:TLE 详细状态:`Time limit exceeded on test 2`
> 我一开始先是按2250C的方法，去枚举 $f(p)+f(q)$ 
然后判断时，即对每个元素出现次数进行判断，奇数次数就`p++`，偶数次数可以分为两个，就`p`和`q`同时加  
这里发现，判断结果都是一样的，故枚举无效  
但最后出来的结果 $f(p)+f(q)$ 对于`2,2,2,2`与样例不同  
然后我用DFS+回溯去找到所有子序列，对每个长度为 $n$ 子序列再去判断 $f(p)+f(q)$  
这里我先对 $a$ 进行优化了，复制了一份到 $b$ 并给 $b$ 排序+去重  
同时另开两个计数用的数组，一个`num_cnt`先行记好 $a$ 中所有数字出现的次数，另一个`cnt`是记录子序列数字出现的次数  
判断时直接遍历 $b$ ,由`num_cnt`减去`cnt`可得到另一个序列出现的次数  
如果`cnt`对某个元素出现次数为奇数，`p++`；如果另一个序列出现的次数为奇数，`q++`  
这样取得最大的 $f(p)+f(q)$ 就行
- 提交时间:22:05 状态:AC 详细状态:`-`
> 经*GPT*解读作者题解后，作者的思路是根据每个元素的出现次数来分配  
如果元素出现次数为奇数，他将不能通过拆分成两个奇数，即无法同时为两个子序列同时做贡献，只能达到贡献为1  
如果元素出现次数为偶数，可以分配为两个偶数，能同时为两个子序列做贡献，达到贡献为2
但对于偶数要额外讨论，一种是普通偶数，自然可以分成两个奇数，对两边子序列同时做贡献  
另一种是为4的倍数，如果均分，自然会分为两个偶数，使得对两边贡献为0，要不然只能分为1和3，产生了2的长度差  
而当奇数出现次数为0、出现次数为4的倍数的元素个数为奇数时，无法通过奇数来弥补这个长度差，于是只能通过普通偶数来弥补  
故最后要特判该情况，给答案再减二