1. 盲训
# 2223A.Zhily and Bracket Swapping
[Rating]1300
[Tag]构造、贪心
[Time]理解+思考41:20，补题16:53，默写03:38
## 题目详情
Deep in the wilderness, Zhily and Jily discovered two sequences composed of brackets. Each sequence carries some logical structure, but neither of them is necessarily a regular bracket sequence on its own. They found that by swapping brackets between the two sequences, they could repair them. They wish to make both sequences regular bracket sequences by swapping brackets between them.
A regular bracket sequence is a sequence consisting of '(' and ')', which can be turned into a valid math expression by inserting 1 and + any number of times into the sequence. For example, the sequence "()(()())" is a regular bracket sequence, while "())((" or "(()" are not regular bracket sequences.
You are given two bracket sequences $a$$a$ and $b$$b$ of **even** length $n$$n$. You can perform the following operation any number of times:
-   Choose a position $i$$i$ ($1≤i≤n$$1 \leq i \leq n$) and swap $ai$$a_i$ and $bi$$b_i$.
Determine whether you can turn both $a$$a$ and $b$$b$ into regular bracket sequences.
**Input**
Each test contains multiple test cases. The first line contains the number of test cases $t$$t$ ($1≤t≤104$$1 \le t \le 10^4$). The description of the test cases follows.
The first line of each test case contains a single integer $n$$n$ ($2≤n≤2⋅105$$2 \leq n \leq 2\cdot 10^5$, $n$$n$ is even) — the length of the strings $a$$a$ and $b$$b$.
The second line of each test case contains a sequence $a$$a$ of length $n$$n$ consisting only of characters '(' and ')'.
The third line of each test case contains a sequence $b$$b$ of length $n$$n$ consisting only of characters '(' and ')'.
It is guaranteed that the sum of $n$$n$ over all test cases does not exceed $$.
**Output**
For each test case, output "YES" if you can turn both $$ and $$ into a regular bracket sequences, "NO" otherwise. You can output "YES" and "NO" in any case (for example, "yES", "yes", and "Yes" will be recognized as a positive response).
## 提交详情
- 提交时间:11:33 状态:WA 详细状态:`Wrong answer on test 2`
> 交换指定位置的字符，使得两者都是有效的括号序列  
先统计两个字符中左括号和右括号数量  
既然要使得两者交换后能匹配，或原本就匹配，必然要左右括号两两对应，即必须数量相等，否则怎么换必然有一个不匹配  
然后还有特例判断，两个字符串的首字符不能是左括号，尾字符不能是右括号，这样换都换不了   
这样再用栈做两个字符串的括号匹配，如果每个字符串匹配完后左右括号仍有剩余，则不行  
不对，应该是可以的，这里关键是两边符号数量要相等，即第一个的左括号数量要等于第二个的右括号数量，这样才能完成交换匹配  
应该先做全匹配，即计数全清零  
然后根据是否能匹配，我在考虑是否要进行模拟详细匹配，即寻找交换的是否足够，以及最重要的位置是否匹配得上  
根据题目所给样例`s`为`(((())`，`t`为`()()))`  
这个样例中，无论是左括号入栈，还是右括号入栈，对于`s`得到都是首两个字符不匹配，对于`t`则是后两个字符不匹配  
然后论交换，仅需交换 $i=2$ 的字符一次即可，而两个不匹配字符与一次交换这两个不等的数学变量，无法直接得出结果  
且也不好直接做出答案，但我有极大直觉觉得这题无需去仔细匹配，仅需判断是否存在交换后成功的可能性即可  
同时，我也想到了，不用去用栈匹配，两个字符串的差值可能就是要匹配的数量，但不好的地方是无法精准找到更换的地方  
更换必要性我觉得还要加一条，两个字符串剩余的字符只能包含一种，如果包含两种，代表无法更换
- 提交时间:11:55 状态:AC 详细状态:`-`
> 经*GPT*指导下，他用了前缀和的算法，因为*合法括号序列的判定本来就等价于前缀和条件*  
将`(`进行前缀和计数，遇到`)`就减去前缀和  
例如对于`(()())`，其前缀和为`1 2 1 2 1 0`  
且最终如果可以成为合法括号序列，最终前缀和必定为0，且前缀和不会为负数(代表右括号过多)  
本题*GPT*也沿用了前缀和算法，设`calA`是`s`字符串的前缀和，`calB`同理  
当两者对应符号都为`(`时，两者前缀和均加一；当两者对应符号都为`)`时，两者前缀和均减一；  
当两者对应符号不同时，则必定有一个要加一，另一个减一  
而这里可以使用题目的交换，即将模拟下字符串字符交换的更优操作，转换为两者前缀和的加减变化  
要使得最终两个字符串均匹配，那么这两个字符串各自对应的前缀和都必须为0  
即要达到这种平衡，必然要使得两者在交换，触发前缀和加减变化时要更趋向两者平衡的方向  
即加一要给少的那个加，减一要给多的那个减  
且这样操作，是将模糊的更优交换操作具象化实现了，通过收益(前缀和平衡)来实现了  
> 花费03:38，代码默写成功  
本题我解不出来的原因，在于我一直执着于更优交换操作的具体如何实现，妄图用字符间对应关系来解决本题  
第一次学到了前缀和对于这种更优交换操作的具体实现

# 2217C.Grid Covering
[Rating]1300
[Tag]中国剩余定理、数学、数论
[Time]理解+尝试01:06:11，补题45:09，代码默写00:32
## 题目详情
Prakul has been working hard setting problems for Codecraft. When he's deep in thought, he likes jumping around his room in weird but specific ways. After some time, he wonders if jumping in this manner can cover all the tiles of his room.
Prakul's room can be considered as a grid $A$ consisting of $n$ rows and $m$ columns. He starts walking from $A_{1,1}$. If he is currently at $A_{i,j}$ he can do either of the following moves:
-   Jump $b$ steps right to $A_{\,i,\;((j+b-1)\bmod m)+1}$, or
-   Jump $a$ steps down to $A_{\,((i+a-1)\bmod n)+1,\;j}$.
A special restriction is that he can start with either move, but **must alternate** between them.
Note that his room is quite special as it wraps around itself. Moving one step right from the $m$\-th column places him in the $1$\-st column. Similarly, moving one step down from the $n$\-th row places him in the $1$\-st row.
Since he still has to work on problemsetting, he needs your help. Determine if he can visit all tiles in $A$ in a finite number of jumps.
**Input**
Each test contains multiple test cases. The first line contains the number of test cases $t$ ($1 \le t \le 10^4$). The description of the test cases follows.
The only line of each test case contains four integers $n, m, a, b$ ($1 \leq n, m, a, b\leq 10^{9}$).
**Output**
For each test case, print "YES" if Prakul can cover all the tiles of his room, and "NO" otherwise.
You can output the answer in any case (upper or lower). For example, the strings "yEs", "yes", "Yes", and "YES" will be recognized as positive responses.
## 提交详情
- 提交时间:13:05 状态:WA 详细状态:`Wrong answer on test 2`
> 交替执行向右走 $b$ 步、向下走 $a$ 步，能否完全遍历完 $n\times m$ 的所有方格  
我想通过奇偶性来判断这题，即 $b$ 步的间隔是否能完全遍历 $m$ 长度的所有格，$a$ 步是否能完全遍历 $n$ 长度的所有格  
论证步长的奇偶是否会导致无法达到某格  
存在例子`n=5,a=4`，从`i=4`出发，下一步是绕一圈到达`i=3`，再下一步就是`i=2`，即步长奇偶不影响是否能遍历完所有格  
再论证步长是对应格长度(例如 $a$ 步长对应 $n$ 长度)的倍数是否无法到达某格  
以同样的例子`n=5,a=5`，从`i=4`出发，下一步是 $i=(4+5-1)\bmod 5+1=4$ ，回到了原点  
如果修改步长为`a=10`，从`i=4`出发，同样会回到原点  
故得证，如果步长是对应长度的倍数，会始终待在某一行或某一列中
还有，如果对应长度是步长的倍数，也不行，因为此时会始终固定在部分行或部分列中行动，无法遍历所有行或列  
特例判断完毕，本题感觉不能通过模拟进行，存在数学规律可以直接解决本题  
但构造样例发现问题  
对于`n=10,a=4`，自`i=0`出发，经`0 4 8 2 6`后回到了原点，此处未遍历奇数行  
对于`n=6,a=4`，自`i=0`出发，经`0 4 2`后回到了原点，此处未遍历奇数行  
对于`n=9,a=5`，自`i=0`出发，经`0 5 1 6 2 7 3 8 4`后回到了原点，此处经历了所有行  
对于`n=9,a=4`，自`i=0`出发，经`0 4 8 3 7 2 6 1 5`后回到了原点，此处经历了所有行  
对于`n=9,a=7`，自`i=0`出发，经`0 7 5 3 1 8 6 4 2`后回到了原点，此处经历了所有行  
对于`n=8,a=6`，自`i=0`出发，经`0 6 4 2`后回到了原点，此处未遍历奇数行  
故得证，步长和对应长度同为偶数，也不得行  
但这里又引出新证明，是否步长和对应长度存在大于1的最小公倍数，也不行呢？  
对于`n=15,a=10`，自`i=0`出发，经`0 10 5`后回到了原点，此处未遍历全部行 
对于`n=9,a=6`，自`i=0`出发，经`0 6 3`后回到了原点，此处未遍历全部行  
对于`n=21,a=14`，自`i=0`出发，经`0 14 7`后回到了原点，此处未遍历奇数行  
前偶数论证可证明存在最大公因数为2也不行  
故得证，步长和对应长度存在大于1的最大公因数，也不行  
同时这个论证可以看出，如果步长和对应长度存在大于1的最大公因数`l`，其最终遍历的所有点间距即为`l`  
之前得证的偶数理论可以归纳到这个理论中，因为偶数理论就是最大公因数为2，倍数理论也是同理  
但目前仅剩样例中`3411 4134 32 23`无法解决  
排除最大公因数影响，根据之前的例子，无法通过奇偶来直接判断，且这两对(3411和32, 4134和23)的最大公因数均为1  
无明显关系，故还差部分结论无法找到，将这个例子进行特判交上去领一发`WA`
- 提交时间: 状态: 详细状态:``
> 经*GPT*指导下，我遗漏了对周期方向的枚举  
假设第一步向右，自 $(0,0)$ 出发，第一步向右为 $(0,b)$ ，第二步向下为 $(a,b)$  
接着第三步就是 $(a,2b)$ ，第四步就是 $(2a,2b)$  
则这样第 $k$ 个偶数步就是 $O_{k}=(ka\bmod n, kb\bmod m)$ ，第 $k$ 个奇数步就是 $E_{k}=((k-1)a\bmod n, kb\bmod m)$  
那么设行坐标经历`r`次后回到原点，即 $r=n/gcd(n, a)$ ；同理`c`为纵坐标回到原点的次数，即 $c=m/gcd(m, b)$  
存在 $ra\equiv 0(\bmod n), bc\equiv 0(\bmod m)$  
设第 $k$ 偶数步回到原点，必然存在 $ka\equiv 0(\bmod n), kb\equiv 0(\bmod m)$  
由上述可知 $k\equiv 0(\bmod r), k\equiv 0(\bmod r)$  ，即使得`k`为`r`和`c`的最小公倍数，可得最小`k`取值为 $L=lcm(r, c)$  
奇数步同理，若设第 $x$ 个偶数步 $O_{x}=(xa\bmod n, xb\bmod m)$ 与第 $y$ 个奇数步 $E_{y}=((y-1)a\bmod n, yb\bmod m)$ 重合  
存在 $xb\equiv yb(\bmod m), xa\equiv (y-1)a(\bmod n)$ ，等价于 $x\equiv y(\bmod \frac{m}{b})\equiv y(\bmod c)$ ，同理 $x\equiv y-1(\bmod r)$ ，使其有解的条件仅为 $gcd(r, c)=1$  
故若偶数步可能与奇数步重合的情况下，仅可能存在 $L$ 个由奇数步和偶数步构成的所有状态  
那么不重合时(即 $gcd(r, c)>1$)，就是 $2L$ 个状态  
由于题目要遍历所有格子，必须存在 $gcd(n,a)=1,gcd(m,b)=1$ ，此时可得 $r=n,c=m$  
故 $L=lcm(n,m)=\frac{nm}{gcd(n,m)}$ ，设 $g=gcd(n,m)=gcd(r,c)$  
由上述可知，若 $g=1$ ，奇偶状态重合，可遍历格子总数为 $L=nm$  
若 $g=2$ ，奇偶状态不重合，可遍历格子总数为 $2L=2\times \frac{mn}{2}=mn$  
若 $g>2$ ，此时奇偶状态不重合，可遍历格子总数为 $2L=\frac{2mn}{g}$  
由于 $g>2$ ，则 $\frac{2}{g}<1$ ，得 $2L=\frac{2mn}{g}<mn$ ，此时无法完全遍历完所有格子  
故题目还存在 $gcd(n,m)<=2$ 的要求  
> 花费00:32，代码默写成功  
我只推出了 $gcd(n,a)=1,gcd(m,b)=1$ 使得行列不重复  
完全不知道可以通过周期推出可遍历格子总数，一味得以模拟的想法去想着怎样才能遍历完所有格子


2. 题型训练
# P1435 [IOI 2000] 回文字串
[Rating]普及
[Tag]动态规划DP、区间DP
[Time]理解+尝试30:12，补题20:00，默写03:33
## 题目详情
*题目背景*
IOI2000 第一题
*题目描述*
回文词是一种对称的字符串。任意给定一个字符串，通过插入若干字符，都可以变成回文词。此题的任务是，求出将给定字符串变成回文词所需要插入的最少字符数。
比如 $\verb!Ab3bd!$ 插入 $2$ 个字符后可以变成回文词 $\verb!dAb3bAd!$ 或 $\verb!Adb3bdA!$，但是插入少于 $2$ 个的字符无法变成回文词。
**注意**：此问题区分大小写。
*输入格式*
输入共一行，一个字符串。
*输出格式*
有且只有一个整数，即最少插入字符数。
*说明/提示*
*数据范围及约定*
记字符串长度为 $l$。
对于全部数据，$0<l\le 1000$。
## 提交详情
- 提交时间:14:55:12 状态:WA 详细状态:`WA*9 AC*1`
> 插入最少的字符，使得给与字符串为回文字符串  
第一眼想到一个暴力方法，即枚举回文字符串的中心或中心对  
对于奇数字符串，可以枚举某个字符作为回文字符串中心，但奇数字符串可能存在通过插入奇数个字符成为偶数长度的回文字符串  
偶数长度也同理，故这里因更优操作的不确定(选择枚举中心或中心对)，放弃枚举回文字符串的中心  
如果枚举中心对，则要求给与字符串中必须存在两个或以上的相同字符，否则完全无法枚举  
初始想法，插入的字符数就是左右两边不匹配的字符数  
可是要去实际枚举左右两侧，如`DBAb3bdD`，也存在不确定操作(我是以左侧字符固定，然后寻找右侧字符串中是否存在)，无法通过模拟查找  
且这里会造成枚举单个中心时，额外多O(n*n)的复杂度  
如果单轮左右两侧出现的字符及其次数来看，可以一试  
我直接枚举固定的中心点，取每次枚举的最小值
- 提交时间:15:03:49 状态:WA 详细状态:`WA*9 AC*1`
> 在上个提交的基础上加入对中心区间的枚举
- 提交时间: 状态: 详细状态:``
> 经*GPT*指导，他用的是最长公共子序列的方法，即对于当前中心，寻找左右两侧的最长公共子序列  
组成整个序列的最长回文子序列，设这个子序列长度为`len`，整个字符串长度为`n`，故需要补充字符`n-len`  
然后他用了`dp[i][j]`代表第 $i$ 个字符和第 $j$ 个字符组成的回文字符串下，需要添加的最少字符  
如果当前两个字符相等($s_i=s_j$)，就取上一区间的答案，即`dp[i][j]=dp[i+1][j-1]`  
如果当前两个字符不相等，就看是将 $s_j$ 加入到左侧需要的字符少，还是将 $s_i$ 加入到右侧需要的字符少  
故此时状态转移为`dp[i][j]=min(dp[i+1][j], dp[i][j-1])+1`  
> 耗时03:33，默写失败，主要是我忘记怎么取最大值，根据此处定义最终答案应为`dp[0][n-1]`(从0到 $n-1$ 组成回文串所需要的最少字符)

# P1775 石子合并（弱化版）
[Rating]普及
[Tag]动态规划DP、区间DP
[Time]理解+思考24:10，补题15:05，默写07:41
## 题目详情
设有 $N(N \le 300)$ 堆石子排成一排，其编号为 $1,2,3,\cdots,N$。每堆石子有一定的质量 $m_i\ (m_i \le 1000)$。现在要将这 $N$ 堆石子合并成为一堆。每次只能合并相邻的两堆，合并的代价为这两堆石子的质量之和，合并后与这两堆石子相邻的石子将和新堆相邻。合并时由于选择的顺序不同，合并的总代价也不相同。试找出一种合理的方法，使总的代价最小，并输出最小代价。
*输入格式*
第一行，一个整数 $N$。
第二行，$N$ 个整数 $m_i$。
*输出格式*
输出文件仅一个整数，也就是最小代价。
## 提交详情
- 提交时间:16:18:41 状态:AC 详细状态:`-`
> **我的思考**  
选择任意两堆石子进行合并，合并的代价为两堆石子重量之和，合并后的重量为两堆石子之和，求最少总代价  
对于示例`2 5 3 1`  
如果先合并`2 5`，此时重量为 $8$ ，存在代价 $8$，石子变为`8 3 1`  
再合并最后两个`3 1`，此时重量为 $8$ ，代价变为 $12$ ，石子变为`8 12`  
最后再合并，得到最终代价 $12+20=32$ ，远高于题目答案  
如果枚举第一步合并的相邻两个，但剩下的石子存在不确定的更优操作，即可以最优是新堆和相邻旧堆，也可以是旧堆的两个合并再和新堆合并  
要使得总代价最小，必然需要使得每次合并代价都最小，即使得 $w=m_{i}+m_{i+1}$ 最小  
如果按MST的方法，每次都选相邻点和最小的边(相邻的两个)，麻烦的在于数据结构不好处理  
如果按之前的区间DP来写，设`dp[i][j]`为合并区间 $[i,j]$ 需要的最小代价  
这样写我没相同怎么做状态转移，因为这个区间代价的来源我不清楚  
以及我怎么感觉这题有点树的味道  
无论怎么合并，合并成怎样，最终都像是一颗树啊，一颗二叉树  
**GPT补题**  
*GPT*对于`dp[i][j]`的定义同我一样，但是他是通过枚举分界点来解决的  
即合并 $[i,j]$ 堆，最后一步必然为合并以 $k$ 为分界点的两堆 $[i,k]$ 和 $[k+1,j]$  
那么就枚举 $k(i<=k<=j)$ ，使得 $dp[i][k]+dp[k+1][j]$ 最小，最后再加上 $\sum{j}^{l=i} m_{l}$ ，就是合并 $[i,j]$ 的最小代价  
然后*GPT*从长度枚举出发，因为`dp[i][j]`需要依赖更小的区间  
> 耗时07:41，默写失败，失败在于我没懂怎么给DP的二维数组初始化

# P1439 两个排列的最长公共子序列
[Rating]普及+/提高-
[Tag]动态规划DP、二分、树状数组
[Time]理解+思考09:19，补题04:05，默写02:03
## 题目详情
给出 $1,2,\ldots,n$ 的两个排列 $P_1$ 和 $P_2$，求它们的最长公共子序列。
*输入格式*
第一行是一个数 $n$。
接下来两行，每行为 $n$ 个数，为自然数 $1,2,\ldots,n$ 的一个排列。
*输出格式*
一个数，即最长公共子序列的长度。
*说明/提示*
- 对于 $50\%$ 的数据， $n \le 10^3$；
- 对于 $100\%$ 的数据， $n \le 10^5$。
## 提交详情
- 提交时间:16:27:07 状态:MLE 详细状态:`AC*5 MLE*5`
> 按LCS的做法，设`dp[i][j]`为 $P_1$ 前 $i$ 个数、 $P_2$ 前 $j$ 个数中能构成的最长子序列  
如果当前两个数相等，自然继承上一次选择即可，即`dp[i][j]=dp[i-1][j-1]`  
如果当前两个数不等，就看 $P_1$ 前 $i-1$ 个数与 $P_2$ 前 $j$ 个数和 $P_1$ 前 $i$ 个数与 $P_2$ 前 $j-1$ 个数构成的子序列哪个最长了，即`dp[i][j]=min(dp[i-1][j],dp[i][j-1])`
- 提交时间:16:37:37 状态:AC 详细状态:`-`
> 经*GPT*咨询后，我的问题在于二维数组空间太大  
本题可以直接通过将 $P_1$ 中每个元素转化为其在 $P_2$ 的位置  
然后根据位置求LIS，*GPT*使用的是牌堆法(去年暑假学过，LIS专用)  
> 牌堆法:即每次放牌进入，如果当前牌在牌堆中存在跟他一样或比他大的，就进行更换，否则换到牌堆底部  
> 用时02:03，代码默写成功  
> 复盘了一下之前用BIT写LIS的方法，即BIT记录每个位置出现的次数，然后对于每个位置就统计当前出现的总次数，次数最大的就是LIS

# P1725 琪露诺
[Rating]普及+/提高-
[Tag]动态规划 DP、线段树、单调队列、动态规划优化、优先队列 ST 表、STL
[Time]理解+尝试20:00，补题37:26，默写09:49
## 题目详情
在幻想乡，琪露诺是以笨蛋闻名的冰之妖精。
某一天，琪露诺又在玩速冻青蛙，就是用冰把青蛙瞬间冻起来。但是这只青蛙比以往的要聪明许多，在琪露诺来之前就已经跑到了河的对岸。于是琪露诺决定到河岸去追青蛙。
小河可以看作一列格子依次编号为 $0$ 到 $N$，琪露诺只能从编号小的格子移动到编号大的格子。而且琪露诺按照一种特殊的方式进行移动，当她在格子 $i$ 时，她只移动到区间 $[i+L,i+R]$ 中的任意一格。你问为什么她这么移动，这还不简单，因为她是笨蛋啊。
每一个格子都有一个冰冻指数 $A_i$，编号为 $0$ 的格子冰冻指数为 $0$。当琪露诺停留在那一格时就可以得到那一格的冰冻指数 $A_i$。琪露诺希望能够在到达对岸时，获取最大的冰冻指数，这样她才能狠狠地教训那只青蛙。
但是由于她实在是太笨了，所以她决定拜托你帮它决定怎样前进。
开始时，琪露诺在编号 $0$ 的格子上，只要她下一步的位置编号大于 $N$ 就算到达对岸。
*输入格式*
第一行三个正整数 $N, L, R$。
第二行共 $N+1$ 个整数，第 $i$ 个数表示编号为 $i-1$ 的格子的冰冻指数 $A_{i-1}$。
*输出格式*
一个整数，表示最大冰冻指数。
*说明/提示*
对于 $60\%$ 的数据，$N \le 10^4$。
对于 $100\%$ 的数据，$N \le 2\times 10^5$，$-10^3 \le  A_i\le 10^3 $，$1 \le L \le R \le N $。数据保证最终答案不超过 $2^{31}-1$。
## 提交详情
- 提交时间:17:00:30 状态:WA 详细状态:`WA*11 TLE*1 AC*1`
> 自 $i=0$ 开始，每次移动到 $[i+L,i+R]$ 中，问怎样使得得到的总冰冻指数最大  
要使得总和最大，必然可以通过每一步最大  
即我可以从 $i$ 直接跳到 $[i+L,i+R]$ 中最大的那一格  
定义`dp[i]`为到 $i$ 的最大冰冻指数  
那么线性递推，枚举每一个起点，推出每一步跳到范围内的最大值
- 提交时间: 状态: 详细状态:``
> 经*GPT*指导，我发现我有以下错误:
> 1. 数据输入有误，应该是0到N共N+1个人  
> 2. 最终可达的最大值应该是 $max(dp_{n-l},\dots,dp_{n})$  
> 然后我再花了08:34去尝试用线段树查找区间内最大值，但问题在于区间内有些格子是不可达的，不能从这些不可达的格子出发  
且线段树只能返回对应值，而不能返回对应值所在的下标  
> 再经*GPT*提醒下，花费07:56用线段树改写失败，问题在于我忘记加冰冻指数，且忘记取值范围( $max(dp_{n-l},\dots,dp_{n})$ )  
> 默写用时09:49，失败。失败在于我知道线段树是对DP做的，但是我忘了初始化第一个元素(第一个元素要设置为第一个冰冻指数)，着急得把查询逻辑用了更新逻辑(`l==r`)而不是查询逻辑(`ql<=l&&r<=qr`)

3. 复习
# 力扣300.最长递增子序列
所属模型： LeetCode
当前分数： 4
为什么今天复习： 经典LIS问题，是线性DP的核心题型。距上次练习（0722）已23天，远超得分4所需的7天复习周期，需定期复习以保持手感并确保能独立实现。
建议限时： 25分钟
> 耗时01:14，代码复现成功

# P1048 采药
所属模型： 洛谷
当前分数： 2
为什么今天复习： 0/1背包基础题，当前得分仅为2（略懂，关键状态或转移不完整）。作为今天的攻坚题，需通过重做提升掌握度，距上次练习（0724）已21天。
建议限时： 15分钟
> 耗时20:50，记忆化DFS复盘失败，主要在于我忘记边界处理  
即超出边界后返回0即可，不代表当前选择无效

# P1115 最大子段和
所属模型： 洛谷
当前分数： 3
为什么今天复习： 经典线性DP问题（Kadane算法），网赛关联度高。距上次练习（0810）已4天，刚好达到得分3所需的3天复习周期，需强化记忆以实现独立识别。
建议限时： 20分钟
> 耗时01:59，复盘成功

# P4017 最大食物链计数
所属平台： 洛谷
当前分数： 4
为什么今天复习： 拓扑排序+DP的综合题，是网赛中常见的算法组合。距上次练习（0805）已9天，超过得分4所需的7天复习周期，需要巩固以保持熟练度。
建议限时： 30分钟
> 耗时12:31，复盘失败，失败在于我忘了拓扑是根据入度为0决定是否加入队列的，且我忘记这题需要使用DP来计数，遍历其链接边时没有处理入度