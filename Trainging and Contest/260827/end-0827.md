1. 复盘
# P3372【模板】线段树 1
[Rating]普及+/提高−
[Tag]-
[Time]复盘15:35
## 题目详情
*题目描述*
如题，已知一个数列 $\{a_i\}$，你需要进行下面两种操作：
1. 将某区间每一个数加上 $k$。
2. 求出某区间每一个数的和。
*输入格式*
第一行包含两个整数 $n, m$，分别表示该数列数字的个数和操作的总个数。
第二行包含 $n$ 个用空格分隔的整数 $a_i$，其中第 $i$ 个数字表示数列第 $i$ 项的初始值。
接下来 $m$ 行每行包含 $3$ 或 $4$ 个整数，表示一个操作，具体如下：
1. `1 x y k`：将区间 $[x, y]$ 内每个数加上 $k$。
2. `2 x y`：输出区间 $[x, y]$ 内每个数的和。
*输出格式*
输出包含若干行整数，即为所有操作 2 的结果。
*说明/提示*
对于 $15\%$ 的数据：$n \le 8$，$m \le 10$。  
对于 $35\%$ 的数据：$n \le {10}^3$，$m \le {10}^4$。    
对于 $100\%$ 的数据：$1 \le n, m \le {10}^5$，$a_i,k$ 为正数，且任意时刻数列的和不超过 $2\times 10^{18}$。
## 提交详情
- 提交时间:22:56:32 状态:AC 详细状态:`-`
> 复盘失败，我犯了两个小错误:  
> 1. 更新时误将把中点`mid`当目标值`k`传递给子树了  
> 2. 累加时应该把目标范围值加回`tree[p]`，而非直接覆盖

# P3373 【模板】线段树 2
[Rating]普及+/提高−
[Tag]-
[Time]复盘28:27
## 题目详情
如题，已知一个数列 $a$，你需要进行下面三种操作：
- 将某区间每一个数乘上 $x$；
- 将某区间每一个数加上 $x$；
- 求出某区间每一个数的和。
*输入格式*
第一行包含三个整数 $n,q,m$，分别表示该数列数字的个数、操作的总个数和模数。
第二行包含 $n$ 个用空格分隔的整数，其中第 $i$ 个数字表示数列第 $i$ 项的初始值 $a_i$。
接下来 $q$ 行每行包含若干个整数，表示一个操作，具体如下：
操作 $1$： 格式：`1 x y k`  含义：将区间 $[x,y]$ 内每个数乘上 $k$。
操作 $2$： 格式：`2 x y k`  含义：将区间 $[x,y]$ 内每个数加上 $k$。
操作 $3$： 格式：`3 x y`  含义：输出区间 $[x,y]$ 内每个数的和对 $m$ 取模所得的结果。
*输出格式*
输出包含若干行整数，即为所有操作 $3$ 的结果。
*说明/提示*
【数据范围】
对于 $30\%$ 的数据：$n \le 8$，$q \le 10$。  
对于 $70\%$ 的数据：$n \le 10^3 $，$q \le 10^4$。  
对于 $100\%$ 的数据：$1 \le n \le 10^5$，$1 \le q \le 10^5,1\le a_i,k\le 10^4$。
## 提交详情
- 提交时间: 状态: 详细状态:``
> 复盘失败，错误如下:  
> 1. 我一开始想着先加后乘，但是应该是 $(a+b)*c=ac+bc$ ，应该是先乘后加，懒标记无法详细地跟踪先加后乘  
> 2. 查询时忘记右子树查询为`p << 1 | 1`，漏写了`| 1`

# P1253 扶苏的问题
[Rating]普及+/提高−
[Tag]线段树
[Time]理解+尝试+修改42:46
## 题目详情
给定一个长度为 $n$ 的序列 $a$，要求支持如下三个操作：
1. 给定区间 $[l, r]$，将区间内每个数都修改为 $x$。
2. 给定区间 $[l, r]$，将区间内每个数都加上 $x$。
3. 给定区间 $[l, r]$，求区间内的最大值。
*输入格式*
第一行是两个整数，依次表示序列的长度 $n$ 和操作的个数 $q$。  
第二行有 $n$ 个整数，第 $i$ 个整数表示序列中的第 $i$ 个数 $a_i$。  
接下来 $q$ 行，每行表示一个操作。每行首先有一个整数 $op$，表示操作的类型。
- 若 $op = 1$，则接下来有三个整数 $l, r, x$，表示将区间 $[l, r]$ 内的每个数都修改为 $x$。
- 若 $op = 2$，则接下来有三个整数 $l, r, x$，表示将区间 $[l, r]$ 内的每个数都加上 $x$。
- 若 $op = 3$，则接下来有两个整数 $l, r$，表示查询区间 $[l, r]$ 内的最大值。
*输出格式*
对于每个 $op = 3$ 的操作，输出一行一个整数表示答案。
*说明/提示*
*数据规模与约定*
- 对于 $10\%$ 的数据，$n = q = 1$。
- 对于 $40\%$ 的数据，$n, q \leq 10^3$。
- 对于 $50\%$ 的数据，$0 \leq a_i, x \leq 10^4$。
- 对于 $60\%$ 的数据，$op \neq 1$。
- 对于 $90\%$ 的数据，$n, q \leq 10^5$。
- 对于 $100\%$ 的数据，$1 \leq n, q \leq 10^6$，$1 \leq l, r \leq n$，$op \in \{1, 2, 3\}$，$|a_i|, |x| \leq 10^9$。
*提示*
请注意大量数据读入对程序效率造成的影响。
## 提交详情
- 提交时间:00:11:44 状态:RE 详细状态:`-`
> 先加再覆盖，还是先覆盖再加，我有点混乱  
对于操作`加 覆盖 加`，操作1`加`进行正常执行，然后操作2`覆盖`  
如果对应覆盖区间与上一个加的区间有交集，直接消去前一个操作了  
故懒标记执行`apply_fix`里有对加法懒标记清零的操作  
那既然如果一个区间需要进行覆盖，那么其对应加分懒标记的操作也得删除掉  
故下传懒标记时，应该是先覆盖再加，即如果一个区间需要覆盖，其加法懒标记是无效的   
- 提交时间:00:12:05 状态:WA 详细状态:`-`
> 覆盖标记我又忘记开4倍了
- 提交时间:00:15:47 状态:AC 详细状态:`-`
> 经*GPT*盘点，我的错误如下:  
> 1. 题目要求最大值，应该是直接对`tree[p]`加目标值，而非像正常线段树一样加区间和(写太快了)  
> 2. 覆盖懒标记执行时已经清空了当前区间的加法懒标记，而执行覆盖懒标记时才标记覆盖，则后续遇到覆盖标记的区间时，不用额外再清除其加法懒标记，因为加法懒标记是在覆盖操作后加的