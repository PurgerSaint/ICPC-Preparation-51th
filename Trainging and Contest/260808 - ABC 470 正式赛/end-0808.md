0.1 昨晚Div2
> 比赛名称: Edu 193 Div.2
> 比赛时间: 2026/08/07 22:35-00:35
> 比赛结果: [Rank]11764 [Solved]1 [Rating-Change]-50 [New-Rating]895

1. 复盘
# 2252A.Boss Fight
> 用时04:24，代码默写成功  
本题题目关键是技能次数最多的那个技能，如果要触发护盾，必定是这个次数最多的技能触发  
设这个次数最多的技能有`cnt`次，则其他技能有`n-cnt`次  
如果`n-cnt`比`cnt`大，必然不会被触发  
如果`n-cnt`与`cnt`一样，必然可以通过交叉的方式全部执行  
如果`n-cnt`比`cnt`小，此技能最多被执行 $n-cnt+2$ 次，其余全部无效

# 2252B.Always Changing
> 用时06:56，复盘成功  
本题关键是从结果往回推  
设`x`为要删除`1`的数量，`y`为要删除`0`的数量  
设`cnt1`为要保留的`1`的数量，`cnt0`为要保留`0`的数量  
因题目要求(不能连续删)，故保留和要删除中的`1`和`0`数量均不能差超过1  
可以保留和删除各相差1，也可以仅保留或删除相差1，但不能总共相差2及以上  
最后答案就是`x`和`y`小的两倍(删除内部消)，加上两倍`x`和`y`的差值减一(剩下的部分借一点保留的来删)

# P3372 【模板】线段树 1
> 用时11:35，复盘失败  
经*GPT*提醒下，我错在更新左右子树时，用了左右边界(l和r)而非目标中点mid

# P3373 【模板】线段树 2
> 用时12:15，复盘失败  
经*GPT*提醒下，我的错误在于误判懒标记下传条件，即`lazy_add[p]=0`和`lazy_mu[p]=0`是两个同时成立才不下传，而非单一成立就不下传

2. 题型训练
# P1908 逆序对
[Rating]普及
[Tag]线段树 树状数组 递归 离散化 分治 排序 扫描线 字典树 Trie
[Time]理解+尝试20:03，补题02:10
## 题目详情
猫猫 TOM 和小老鼠 JERRY 最近又较量上了，但是毕竟都是成年人，他们已经不喜欢再玩那种你追我赶的游戏，现在他们喜欢玩统计。
最近，TOM 老猫查阅到一个人类称之为“逆序对”的东西，这东西是这样定义的：对于给定的一段正整数序列，逆序对就是序列中 $a_i>a_j$ 且 $i<j$ 的有序对。知道这概念后，他们就比赛谁先算出给定的一段正整数序列中逆序对的数目。注意序列中可能有重复数字。
**Update：数据已加强。**
*输入格式*
第一行，一个数 $n$，表示序列中有 $n$ 个数。
第二行 $n$ 个数，表示给定的序列。序列中每个数字不超过 $10^9$。
*输出格式*
输出序列中逆序对的数目。
*说明/提示*
对于 $25\%$ 的数据，$n \leq 2500$。
对于 $50\%$ 的数据，$n \leq 4 \times 10^4$。
对于所有数据，$1 \leq n \leq 5 \times 10^5$。
应该不会有人 $O(n^2)$ 过 50 万吧 —— 2018.8 chen_zhe。
## 提交详情
- 提交时间:15:34:29 状态:WA 详细状态:`-`
> 由于之前用树状数组做过这题，本质上是保存`a`中每个数在`a`中的排名  
然后实时加排名对应数字个数，计算当前从当前排名往后的总数  
而且这题用树状数组更合适(线段树代码太多了)
- 提交时间:15:39:31 状态:AC 详细状态:`-`
> 经*GPT*指导下，我错在答案类型应该为`long long`而非`int`

# P1816 忠诚
[Rating]普及
[Tag]线段树 倍增 树状数组 ST 表
[Time]用时07:36
## 题目详情
老管家是一个聪明能干的人。他为财主工作了整整 $10$ 年。财主为了让自己账目更加清楚，要求管家每天记  $k$ 次账。由于管家聪明能干，因而管家总是让财主十分满意。
但是由于一些人的挑拨，财主还是对管家产生了怀疑。于是他决定用一种特别的方法来判断管家的忠诚。他把每次的账目按 $1, 2, 3, \ldots$ 编号，然后不定时地问管家这样的问题：在 $a$ 到 $b$ 号账中最少的一笔是多少？
为了让管家没时间作假，他总是一次问多个问题。
*输入格式*
第一行输入两个数 $m, n$，表示有 $m$ 笔账和 $n$ 个问题。  
第二行输入 $m$ 个数，分别表示账目的钱数 $x_i$。  
接下来 $n$ 行分别输入 $n$ 个问题，每行 $2$ 个数字，分别表示开始的账目编号 $a$ 和结束的账目编号 $b$。
*输出格式*
第一行输出每个问题的答案，每个答案中间以一个空格分隔。
*说明/提示*
对于 $100\%$ 的数据，$1 \leq m \leq 10^5$，$1 \leq n \leq 10^5$，$0 \le x_i \le 10^5$。
## 提交详情
- 提交时间:15:57:36 状态:AC 详细状态:`-`
> 明牌区间最小值，直接用线段树，而且不是动态更新修改的线段树

3. 同学长一起的Div.3VP
> 比赛名称: 1103 Div.3
> 比赛时间: 2026/08/08 15:00-17:30
> 比赛结果: [Rank]12571 [Solved]3 [Penalty]177
# 2236A.Games on the Train
[Rating]800
[Tag]贪心、数学
[Time]-
## 题目详情
Dabir, Egor, and Arseniy just got on the train and decided to play a game. Dabir has a backpack with an infinite number of cubes. He built $n$ towers from them, where the $i$\-th tower has height $h_i$ cubes.
Egor and Arseniy must choose an integer $x_i$ for each tower $i$ and increase its height by $x_i$ **exactly once**. For example, if $h$ = \[$1, 3, 2, 2$\], $x$ = \[$3, 2, 2, 8$\], then after increasing $h$ it will become \[$4, 5, 4, 10$\]. Their goal is to make the heights of all towers equal.
To make the game more interesting, Dabir wants to choose an integer $k$ and add a restriction: each $x_i$ must satisfy $1 \le x_i \le k$. Help him find the smallest $k$ for which it is possible to finish the game.
**Input**
The first line contains a single integer $t$ ($1 \le t \le 10^4$) — the number of test cases.
Then $t$ test cases follow.
The first line of each test case contains a single integer $n$ ($1 \le n \le 5$).
The second line contains $n$ integers $h_1, h_2, \dots, h_n$ ($1 \le h_i \le 6$).
**Output**
For each test case, output a single integer — the minimum value of $k$ such that it is possible to make all towers have equal height.
## 提交详情
- 提交时间:(比赛时)00:03:27 状态:AC 详细状态:`-`
> 直接寻找最大值，然后再寻找与最大值差值最大的，最后就是这个差值加一即可  
*按学长的做法*，最大最小值差值加一

# 2236B.Tatar TV Show
[Rating]800
[Tag]贪心、数学、字符串处理
[Time]-
## 题目详情
During the holidays, Egor came to visit his friend Dabir in the city of Kazan. Out of boredom, Dabir and Egor came up with a new business idea: to make their own TV show.
The format of the show is very simple: in each episode they invite a guest and play a game with them on a binary string.
In today's episode, Egor and Dabir invited Arseniy (aka MAKAN) — the main celebrity of Omsk. For the game, they chose a binary string $s$ of length $n$ and an integer $k$.
Arseniy can make an unlimited number of moves. In one move, he can choose an integer $i$ ($1 \le i \le n - k$) and invert the characters at positions $i$ and $i + k$, that is, change $0$ to $1$ and $1$ to $0$.
For example, if $s = 10110$ and $k = 2$, then by choosing $i = 2$, Arseniy inverts the characters at positions $2$ and $4$: $[ 10110 \rightarrow 11100 ]$
Arseniy wants to get the main prize — $1\,000\,000$ tugriks. To do this, he needs to make the entire string $s$ equal to zero.
Help Arseniy determine whether he can get his prize, or if he will have to return to Omsk with nothing.
**Input**
The first line contains a single integer $t$ ($1 \le t \le 10^4$) — the number of test cases.
The first line of each test case contains two integers $n$ and $k$ ($1 \le k \le n \le 2 \cdot 10^5$).
The second line of each test case contains a binary string $s$ of length $n$.
It is guaranteed that the sum of $n$ over all test cases does not exceed $2 \cdot 10^5$.
**Output**
For each test case, output "YES" if Arseniy can make the string entirely zero, and "NO" otherwise.
You can output "YES" and "NO" in any case (for example, "yES", "yes", and "Yes" will be accepted).
## 提交详情
- 提交时间:(比赛时)00:23:35 状态:AC 详细状态:`-`
> 寻找每个位置的`1`，按模 $k$ 进行分类  
然后找每一类的字符个数是否有奇数的情况，只要出现奇数就不行(因为你操作不能跨越模 $k$，即 $i$ 只能到 $i+k$)  
*学长的做法*是直接暴力模拟，检查最后是否有`1`存在即可

# 2236C.Omsk Programmers
[Rating]1000
[Tag]暴力枚举、贪心、数学
[Time]补题22:40
## 题目详情
An annual programmers fair is taking place on the main square of Omsk. You, as the main programmer of Omsk, decided to take part in this wonderful event and went there. At the entrance, a guard decided to check your skills and gave you a problem:
You are given three integers $a$, $b$, $x$. You want to make $a$ and $b$ equal. In order to do so, you can apply the following operations:
1.  Choose one of the integers $a$ or $b$ and add $1$ to it.
2.  Choose one of the integers $a$ or $b$ and divide it by $x$ with rounding down.
You need to find the minimum number of operations after which $a$ becomes equal to $b$. Can you prove your skills, or will you have to go back home?
**Input**
The first line contains a single integer $t$ $(1 \le t \le 10^4)$ — the number of test cases.
Then $t$ test cases follow.
Each test case consists of a single line containing three integers $a$, $b$, $x$ ($1 \le a, b \le 10^9$, $2 \le x \le 10^9$).
**Output**
For each test case, output a single integer — the minimum number of operations required to make $a$ and $b$ equal.
## 提交详情
- 提交时间:(比赛时)01:43:54 状态:WA 详细状态:`Wrong answer on test 2`
> 学长给了提示:一个数被2一直除，最多除多少次除到0  
然后我发现，假设这个数是m，$m\in [2^i, 2^{i+1})$ ，那就是i次  
我就去寻找在 $x$ 的多少次方下，$a$ 和 $b$ 能被整除，即找到这两个数除到0的次数  
但最后还是陷入到找规律中，仅仅过了样例  
我还从库里找到了之前写过的类似题目:
```md
*源于7月4日的ABC465*
# D.X to Y
## 题目详情:
给与两个整数`X`、`Y`和一个至少为2的整数`K`  
有一个变量 $x$ ，其初始值为 $x=X$ ，可以对 $x$ 执行以下操作零次或多次:
- 选择满足 $\lfloor \frac{x}{K} \rfloor = y$ 或 $\lfloor \frac{y}{K} \rfloor = x$的整数 $y$ ，并将 $x$ 的值替换为 $y$  
求得到 $x=Y$ 所需的最少操作次数，在给定的约束条件下，总存在一种方法使得用有限操作次数得到 $x=Y$   
> $1<=T<=2\times 10^5, 0<=X,Y<=10^18, 2<=K<=10^18$
## 题目思考:
题目只存在两种方法让 $x$ 得到新值:
1. $x = x / K$
2. $x = x * K$  
用暴力枚举解决:
- 当 $x>Y$ 时，用第一种操作将 $x$ 缩小
- 当 $x<Y$ 时，用第二种方法将 $x$ 放大
    - 注意，第二种方法要枚举目标Y的值为 $x*K$ 加上 $0,1,\dot,K-1$
判断条件是 $x$ 等于 $Y$ ,或 $Y$ 在 $[x*K,(x+1)*K]$ 之内  
但我感觉，这题可能存在能秒杀的数学理论
## 补题:
题目延后，经Gemini分析后，题目为一颗`K`叉树，需要使用`LCA`最近公共祖先
```
- 提交时间: 状态: 详细状态:``
> 经*GPT*指导后，我发现作者是逐步除最大的数，然后计算每步的最小代价  
即设`p`为`a`和`b`中最大的数，`cnt`为已经除的次数  
每一步除的时候，都记录 $|a-b|+cnt$ ，然后保持`a`始终为两数之间较大值，直到找到最小代价  
作者是除法先行，再算加法  
学长做法是枚举两个数的除法链，并逐步记录两个数的代价

# 2236D.Brand New Tatar TV Show
[Rating]1300
[Tag]二分、构造、动态规划、博弈论、数学
[Time]-
## 题目详情
Dabir and Egor were not satisfied with the fame from the previous episode, so they decided to make another TV show: the guys play their favorite game on an array $a$ with their favorite integer $k$.
Dabir moves first. On the first move, any element from the array can be chosen and removed. Let the previous chosen element be equal to $x$. Then on the current move, except the very first one, a player must choose an element $y$ from the array such that $0 \leq y - x \leq k$ and remove it from the array. The player who cannot make a move loses.
But since this was not just a game, but a real show-match, Arseniy (aka MAKAN) — the main celebrity of Omsk was invited again. As a guest celebrity, Arseniy was given the opportunity to make the first move in this match, that is, to make the very first move in the game **instead of Dabir**. However, it turns out Arseniy is a fan of Egor, so he wants his first move to guarantee Egor a winning strategy against any response from Dabir.
Determine whether Arseniy can make the first move for Dabir so that, no matter how Dabir plays, Egor wins.
**Input**
Each test consists of multiple test cases. The first line contains a single integer $t$ ($1 \leq t \leq 10^4$) — the number of test cases.
The first line of each test case contains two integers $n$ and $k$ ($1 \leq n, k \leq 2 \cdot 10^5$) — the length of the array and the favorite integer of Dabir and Egor.
The second line contains $n$ integers $a_1, a_2, \ldots, a_n$ ($1 \leq a_i \leq n$).
It is guaranteed that the sum of $n$ over all test cases does not exceed $2 \cdot 10^5$.
**Output**
For each test case, if there exists such a first move that with optimal play by both players Egor wins, output "YES", otherwise output "NO".
You can output "YES" and "NO" in any case (for example, "yES", "yes", and "Yes" will be accepted).
## 提交详情
- 提交时间:(比赛时)01:59:55 状态:WA 详细状态:`Wrong answer on test 2`
> 去寻找所有符合条件的序列，同时做好标记，确保同一个元素只在一个序列中出现  
然后判断这个序列的长度是否为偶数，或长度超过2时，最后和倒数第二个字符是否相等
- 提交时间:(比赛时)02:21:53 状态:AC 详细状态:`-`
> 第一次提交搜索的模拟太麻烦，我就推倒重来了  
优化搜索序列的流程，然后保存每个数字出现的次数，根据压缩后的数组进行序列枚举  
然后对得到的序列进行判断，关键在于最大的字符数量  
如果最大字符数量为偶数，那只要第一个拿最大的，接下来必须且只能拿最大的，无论怎样，另一个人就会失败  
如果最大字符数量为奇数，那么此时必须要存在其他字符，使得第一个人拿其他字符，第二个人拿最大的字符，这样就还是另一个人失败  
其他所有情况皆失败，以下是我的代码:  
```cpp
string solve() {
    int n, k; cin >> n >> k;
    vector<int> a(n);
    for (int& t: a) cin >> t;
    sort(range(a));
    vector<int> b, len;
    for (int& t: a) {
        if (b.empty() || b.back() != t) {
            b.push_back(t);
            len.push_back(1);
        } else len.back()++;
    }
    int sz = b.size();
    if (sz == 1) return len[0] % 2 ? "NO" : "YES";
    vector<bool> vis(sz, 0);
    for (int i = 0; i < sz; i++) {
        if (vis[i]) continue;
        int l = len[i];
        int mx = i;
        for (int j = i + 1; j < sz; j++) {
            if (b[j] - b[j - 1] > k) break;
            if (vis[j]) continue;
            vis[j] = 1;
            mx = j;
            l += len[j];
        }
        if (l > 1) {
            if (len[mx] % 2 && l > len[mx]) return "YES";
            if (!(len[mx] % 2)) return "YES"; 
        }
    }
    return "NO";
}
```
学长的代码:
```cpp
void Fly_Young() {
    int n, k;
    cin >> n >> k;
    map<int,int> cnt;
    vi a(n);
    FOR(i,0,n) {
      cin >> a[i];
      cnt[a[i]]++;
    }
 
    sort(all(a));
    UNIQUE(a);

    FOR(i,0,a.sz()) {
      if (cnt[a[i]] % 2 == 0 && (i == a.sz() - 1 || a[i + 1] - a[i] > k)) {
        yes;
        return ;
      }
      if (i != a.sz() - 1 && a[i + 1] - a[i] <= k && (cnt[a[i + 1]] & 1)) {
        yes;
        return ;
      }
    }
 
    no;
    // FOR(i,0,a.sz())  cout << a[i] << ':' << cnt[a[i]] << ' ';
    // cout << ed;
}
```

4. ABC 470
> 比赛名称: ABC 470
> 比赛时间: 2026/08/08 20:00-21:40
> 比赛结果: [Rank]4698 [Performance]876 [New-Rating]564 [Diff]+42
# A.Fizz
## 题目详情
You are given a positive integer $N$.
Output $N$ lines. The $i$\-th line $(1\le i\le N)$ should contain `Fizz` if $i$ is a multiple of $3$, and $i$ if $i$ is not a multiple of $3$.
*Constraints*
-   $N$ is an integer between $1$ and $100$, inclusive.
## 提交详情
- 提交时间:(比赛时)20:02:10 状态:AC 详细状态:`-`
> 模拟题，直接模拟输出即可

# B.Monocolor
## 题目详情
There are $N$ balls. Each ball is painted in one of $N$ colors: color $1$ through color $N$. The color of the $i$\-th ball $(1\le i\le N)$ is $C_i$.
In one operation, you can change the color of any one ball to any of the $N$ colors.
Find the minimum number of operations required to make all the balls the same color.
*Constraints*
-   $1\le N\le 100$
-   $1\le C_i\le N$
-   All input values are integers.
## 提交详情
- 提交时间:(比赛时)20:05:21 状态:AC 详细状态:`-`
> 找到颜色最多的个数，用总数减去这个个数即可

# C.Inc, Dec, Xor
[Time]补题32:00
## 题目详情
There is a length-$N$ integer sequence $A=(A_1,A_2,\ldots,A_N)$. Initially, all elements of $A$ are $0$.
You will be given $Q$ queries, which should be processed in order. There are two types of queries, each given in one of the following formats:
-   `1 x`: Increase the value of $A_x$ by $1$.
-   `2`: For each $i=1,2,\ldots,N$, if $A_i \geq 1$, decrease the value of $A_i$ by $1$.
Find the bitwise $\mathrm{XOR}$ of $A_1,A_2,\ldots,A_N$ immediately after processing each query.
What is bitwise $\mathrm{XOR}$?
The bitwise $\mathrm{XOR}$ of non-negative integers $A$ and $B$, denoted $A \oplus B$, is defined as follows:
-   In the binary representation of $A \oplus B$, the digit in the $2^k$ ($k \geq 0$) place is $1$ if exactly one of the digits in the $2^k$ place of $A$ and $B$ in their binary representations is $1$, and $0$ otherwise.
For example, $3 \oplus 5 = 6$ (in binary: $011 \oplus 101 = 110$).  
More generally, the bitwise $\mathrm{XOR}$ of $k$ non-negative integers $p_1, p_2, p_3, \dots, p_k$ is defined as $(\dots ((p_1 \oplus p_2) \oplus p_3) \oplus \dots \oplus p_k)$, and it can be proved that this value does not depend on the order of $p_1, p_2, p_3, \dots, p_k$.
*Constraints*
-   $1\le N\le 5\times 10^5$
-   $1\le Q\le 5\times 10^5$
-   $1\le x\le N$
-   All input values are integers.
## 提交详情
- 提交时间:(比赛时)20:16:13 状态:WA 详细状态:`AC*9 TLE*6`
> 我用了不带懒标记的线段树，并且魔改线段树的节点合并为异或
- 提交时间: 状态: 详细状态:``
> 经*GPT*指导下，设`v`为变化前的元素值，`w`为变化后元素的值  
`R`为除该变化元素外其他所有元素的异或和，`ans`为总异或和  
由题可知修改前为 $ans=R\oplus v$ ，修改后 $ans'=R\oplus w$  
则 $ans'=R\opus w=R\oplus 0\oplus w=R\oplus v\oplus v\oplus w=ans\oplus v\oplus w$  
故加一和减一都可以这样操作，只要快速保存要变化的值即可，且用不上线段树(太费时间了)

# D.Inverse and Swap 
## 题目详情
You are given a permutation $P = (P_1, \dots, P_N)$ of $(1, \dots, N)$.
Process $Q$ queries in order. There are two types of queries as follows:
-   `1 x y`: Swap the values of $P_x$ and $P_y$.
-   `2`: Construct the permutation $P' = (P'_1, \dots, P'_N)$ of $(1, \dots, N)$ satisfying the following condition, and replace the values of $P_1, \dots, P_N$ with $P'_1, \dots, P'_N$, respectively. (One can prove that such $P'$ uniquely exists.)
    -   $P_{P'_i} = i$ for every integer $i$ satisfying $1 \leq i \leq N$.
Output the values of $P_1, \dots, P_N$ after processing all queries.
*Constraints*
-   $2 \leq N \leq 5 \times 10^5$
-   $1 \leq Q \leq 5 \times 10^5$
-   $(P_1, \dots, P_N)$ is a permutation of $(1, \dots, N)$.
-   $1 \leq x < y \leq N$ for queries of type $1$.
-   All input values are integers.
## 提交详情
- 提交时间:(比赛时)20:31:38 状态:TLE 详细状态:`AC*20 TLE*16`
> 我一开始想着直接模拟输出，然后为了避免构造时创建数组所带来的更大时间复杂度  
故使用了两个数组，构造时直接给另一数组赋值，再标记交换即可
- 提交时间:(比赛时)21:28:05 状态: 详细状态:``
> 直接用两个`map`做对应，一个是`{值, pos}`，另一个是`{pos, 值}`，构造时标记交换即可  
这里花了近一小时，主要是我犯了个错误  
在*GPT*指导下(我这里作弊了)，我是两个`map`没有做好引用  
这两个`map`我习惯写在一行，然后只在map后面加`&`，但其实只引用到了第一个，第二个只是复制  