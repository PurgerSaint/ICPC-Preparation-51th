1. 模拟
# 104639A.Qualifiers Ranking Rules
[Source]2023-网赛-I
[Time]理解+尝试34:09
## 题目详情
The following is the current ranking rules for the ICPC Asia EC Online Qualifiers, and there will be two online contests.
1\. In each contest, only the rank of the top-ranked team from each university will be taken as the score of that university;
2\. In each contest, participating universities will be ranked according to their scores;
3\. The two rankings of universities are combined using the merge sorting method. For any two universities that obtain the same ranking in different contests, the university that received this ranking in the first contest will be ranked first.
4\. Delete duplicate universities and obtain the final ranking of all participating universities (only the highest rankings for each university are retained).
Now assuming that there are $n$ teams in the first contest and $m$ teams in the second contest.
For each contest, given the ranking of each team and the university to which it belongs, please output the final ranking of all participating universities according to the above rules.
You can better understand this process through the sample.
**Input**
The first line contains two integers $n,m\ (1\le n,m\le 10^4)$ , representing the number of teams participating in the first contest and the second contest.
Then following $n$ lines, the $i$\-th line contains a string $s_i\ (1\le |s_i|\le 10)$ only consisting of uppercase letters, representing the abbreviation of the university to which the $i$\-th ranked team in the first contest belongs.
Then following $m$ lines, the $i$\-th line contains a string $t_i\ (1\le |t_i|\le 10)$ only consisting of uppercase letters, representing the abbreviation of the university to which the $i$\-th ranked team in the second contest belongs.
It's guaranteed that each university has only one abbreviation.
## 提交详情
- 提交时间:22:09 状态:AC 详细状态:`-`
> 我先是给两场比赛的每个学校去重保存，并保存到一个总的学校花名册中，然后保存每个学校在每一场的排名  
最后我想着对这个学校花名册进行排名，但是不好操作，分别有学校1只在第一场、只在第二场、两场均在，学校2也是如此三种情况  
我还不如直接按序拿取，第一场的第一个放前面，接着就是第二场的第一个，依次类推，拿取注意去重即可

2. 学习
2.1 模版题 
# 洛谷P3367 【模板】并查集
[Rating]普及
[Tag]并查集
[Time]理解+尝试08:46
## 题目详情
*题目背景*
**本题数据范围已经更新到 $1\le N\le 2\times 10^5$，$1\le M\le 10^6$。**
*题目描述*
如题，现在有一个并查集，你需要完成合并和查询操作。
*输入格式*
第一行包含两个整数 $N,M$ ,表示共有 $N$ 个元素和 $M$ 个操作。
接下来 $M$ 行，每行包含三个整数 $Z_i,X_i,Y_i$ 。
当 $Z_i=1$ 时，将 $X_i$ 与 $Y_i$ 所在的集合合并。
当 $Z_i=2$ 时，输出 $X_i$ 与 $Y_i$ 是否在同一集合内，是的输出  `Y` ；否则输出 `N` 。
*输出格式*
对于每一个 $Z_i=2$ 的操作，都有一行输出，每行包含一个大写字母，为 `Y` 或者 `N` 。
*说明/提示*
对于 $15\%$ 的数据，$N \le 10$，$M \le 20$。
对于 $35\%$ 的数据，$N \le 100$，$M \le 10^3$。
对于 $50\%$ 的数据，$1\le N \le 10^4$，$1\le M \le 2\times 10^5$。
对于 $100\%$ 的数据，$1\le N\le 2\times 10^5$，$1\le M\le 10^6$，$1 \le X_i, Y_i \le N$，$Z_i \in \{ 1, 2 \}$。
## 提交详情
- 提交时间:22:28:06 状态:AC 详细状态:`-`
> 模版题，我默写并查集模版失败，失败在于漏了查询时的按所属合并(路径压缩)，其余全默写出来了，看了模版的代码才补全通过    
这题用并查集的原因在于，是集合合并，而非区间合并

2.2 应用题
# 力扣547. 省份数量
[Rating]中等
[Time]理解+尝试07:53
## 题目详情
有 n 个城市，其中一些彼此相连，另一些没有相连。如果城市 a 与城市 b 直接相连，且城市 b 与城市 c 直接相连，那么城市 a 与城市 c 间接相连。
省份 是一组直接或间接相连的城市，组内不含其他没有相连的城市。
给你一个 n x n 的矩阵 isConnected ，其中 isConnected[i][j] = 1 表示第 i 个城市和第 j 个城市直接相连，而 isConnected[i][j] = 0 表示二者不直接相连。
返回矩阵中 省份 的数量。
> 1 <= n <= 200
n == isConnected.length
n == isConnected[i].length
isConnected[i][j] 为 1 或 0
isConnected[i][i] == 1
isConnected[i][j] == isConnected[j][i]
## 提交详情
- 提交时间:22:49 状态:AC 详细状态:`-`
> 这题用并查集的原因，在于他是求点的连通性，标准并查集已解决

# 洛谷P1551 亲戚
[Rating]普及
[Tag]并查集
[Time]理解+尝试05:43
## 题目详情
若某个家族人员过于庞大，要判断两个是否是亲戚，确实还很不容易，现在给出某个亲戚关系图，求任意给出的两个人是否具有亲戚关系。
规定：$x$ 和 $y$ 是亲戚，$y$ 和 $z$ 是亲戚，那么 $x$ 和 $z$ 也是亲戚。如果 $x$，$y$ 是亲戚，那么 $x$ 的亲戚都是 $y$ 的亲戚，$y$ 的亲戚也都是 $x$ 的亲戚。
*输入格式*
第一行：三个整数 $n,m,p$，（$n,m,p \le 5000$），分别表示有 $n$ 个人，$m$ 个亲戚关系，询问 $p$ 对亲戚关系。
以下 $m$ 行：每行两个数 $M_i$，$M_j$，$1 \le M_i,~M_j\le n$，表示 $M_i$ 和 $M_j$ 具有亲戚关系。
接下来 $p$ 行：每行两个数 $P_i,P_j$，询问 $P_i$ 和 $P_j$ 是否具有亲戚关系。
*输出格式*
$p$ 行，每行一个 `Yes` 或 `No`。表示第 $i$ 个询问的答案为“具有”或“不具有”亲戚关系。
## 提交详情
- 提交时间:22:56:58 状态:Accepted 详细状态:`-`
> 本题用并查集的关键，还是所属关系，即将问题从`两个人是否为亲戚`化为了`这个人是否是那个人的亲戚`  
从原本要检查连通性，降级到只需检查所属即可

# P1111 修复公路
[Rating]普及
[Tag]二分、并查集、排序、生成树
[Time]理解+尝试08:03
## 题目详情
*题目背景*
A 地区在地震过后，连接所有村庄的公路都造成了损坏而无法通车。政府派人修复这些公路。
*题目描述*
给出 A 地区的村庄数 $N$，和公路数 $M$，公路是双向的。并告诉你每条公路的连着哪两个村庄，并告诉你什么时候能修完这条公路。问最早什么时候任意两个村庄能够通车，即最早什么时候任意两条村庄都存在至少一条修复完成的道路（可以由多条公路连成一条道路）。
*输入格式*
第 $1$ 行两个正整数 $N,M$。
下面 $M$ 行，每行 $3$ 个正整数 $x,y,t$，告诉你这条公路连着 $x,y$ 两个村庄，在时间 $t$ 时能修复完成这条公路。
*输出格式*
如果全部公路修复完毕仍然存在两个村庄无法通车，则输出 $-1$，否则输出最早什么时候任意两个村庄能够通车。
*说明/提示*
$1\leq x, y\leq N \le 10 ^ 3$，$1\leq M, t \le 10 ^ 5$。
## 提交详情
- 提交时间:23:08:22 状态:Accepted 详细状态:`-`
> 本题用并查集，是因为要查看两个点(村庄)是否连通，即村庄是否有所属  
然后必须要排序，是因为要获取第一个连通后的最小时间  
最后根据并查集的大小是否为1判断是否全部联通即可

3. 盲训
# 1201C.Maximum Median
[Rating]1400
[Tag]二分、贪心、数学、排序
[Time]理解+思考20:40，补题10:33
## 题目详情
You are given an array $a$ of $n$ integers, where $n$ is odd. You can make the following operation with it:
-   Choose one of the elements of the array (for example $a_i$) and increase it by $1$ (that is, replace it with $a_i + 1$).
You want to make the median of the array the largest possible using at most $k$ operations.
The median of the odd-sized array is the middle element after the array is sorted in non-decreasing order. For example, the median of the array $[1, 5, 2, 3, 5]$ is $3$.
**Input**
The first line contains two integers $n$ and $k$ ($1 \le n \le 2 \cdot 10^5$, $n$ is odd, $1 \le k \le 10^9$) — the number of elements in the array and the largest number of operations you can make.
The second line contains $n$ integers $a_1, a_2, \ldots, a_n$ ($1 \le a_i \le 10^9$).
**Output**
Print a single integer — the maximum possible median after the operations.
## 思考
> 最大化某值，猜答案，必定要用二分  
二分范围为数组最小值，到数组最大值加 $k$  
判断方法为数组内，从中位数到最后一个目标值的所有元素，每个元素相差目标值的总和是否不超过 $k$  
如果符合，就将左边界抬高，继续寻找更大的；否则就降低右边界，代表当前中位数太大了
## 提交详情
- 提交时间:23:38 状态:AC 详细状态:`-`
> 经*GPT*提示，我的二分方法是对的，错在更新边界错误  
我用的是`l=mid,r=mid`，而非`l=mid+1,r=mid-1`  
由于是闭区间写法，`l=mid`导致`l==mid`反复执行  
故我只打算记住以下二分模版:
```cpp
while (l <= r) {
    ll mid = l + (r - l) / 2;
    if (check(mid)) {
        ans = max(ans, mid);    // 视条件更新
        l = mid + 1;            // 视条件更新
    } else {
        r = mid - 1;            // 视条件更新
    }
}
```

# 2137D.Replace with Occurrences
[Rating]1200
[Tag]构造
[Time]理解+尝试16:00，二次尝试+补题08:53
## 题目详情
Given an array $a$, let $f(x)$ be the number of occurrences of $x$ in the array $a$. For example, when $a=[1,2,3,1,4]$, then $f(1)=2$ and $f(3)=1$.
You have an array $b$ of size $n$. Please determine if there is an array $a$ of size $n$ such that $f(a_i)=b_i$ for all $1 \leq i \leq n$. If there is one, construct it.
**Input**
Each test contains multiple test cases. The first line contains the number of test cases $t$ ($1 \le t \le 10^4$). The description of the test cases follows.
The first line of each test case contains an integer $n$ ($1 \leq n \leq 2\cdot 10^5$).
The second line contains $n$ integers $b_1,b_2,\ldots,b_n$ ($1 \leq b_i \leq n$).
It is guaranteed that the sum of $n$ over all test cases does not exceed $2\cdot 10^5$.
**Output**
For each test case, output $-1$ if there is no valid array $a$.
Otherwise, print the array $a$ of $n$ integers on a new line. The elements should satisfy $1 \leq a_i \leq n$.
## 提交详情
- 提交时间:(08/04)00:03 状态:WA 详细状态:`Wrong answer on test 2`
> 我先将`b`中每个数压缩成 ${出现次数cnt, 该次数自该位置连续出现的次数time}$  
对于每个`cnt`，其`time`必须为`cnt`的倍数，不然就无法构成  
且对于构造好的`a`，内对应该`cnt`的数也必然连续，即这些连续的数都必须为同一个  
根据构造时选的数是否超过 $n$ ，以及上述是否为其倍数，决定是否可以构造即可
- 提交时间:(08/04)00:16 状态:WA 详细状态:`Wrong answer on test 2`
> 经*GPT*提醒下，我错在`b`中的`cnt`不一定是连续出现的，可能交错出现  
那么此时只需保存所有`cnt`的位置，根据位置按第一次提交的方法构造即可
- 提交时间:(08/04)00:19 状态:AC 详细状态:`-`
> 经*GPT*提醒下，我错在无需判断构造的数字`g`是否超过 $n$ ，因为第一次提交的推论若成立，必然不可能超过