> **过题数降序、Rating为1300**
# 4C.Registration system
[Rating]1300
[Tag]数据结构、哈希表、模拟
[Time]思考+尝试04:31
## 题目详情
A new e-mail service "Berlandesk" is going to be opened in Berland in the near future. The site administration wants to launch their project as soon as possible, that's why they ask you to help. You're suggested to implement the prototype of site registration system. The system should work on the following principle.
Each time a new user wants to register, he sends to the system a request with his name. If such a name does not exist in the system database, it is inserted into the database, and the user gets the response OK, confirming the successful registration. If the name already exists in the system database, the system makes up a new user name, sends it to the user as a prompt and also inserts the prompt into the database. The new name is formed by the following rule. Numbers, starting with 1, are appended one after another to name (name1, name2, ...), among these numbers the least i is found so that namei does not yet exist in the database.
> $1<=n<=10^5$
## 提交详情
- 提交时间:16:38 状态:AC 详细状态:`-`
> 直接用`map<string,int>`计数和保存，然后哈希表查看是否存在和存在数量多少  
存在直接输出`名字`加已有数量即可(最小数量)

# 25A.IQ test
[Rating]1300
[Tag]暴力枚举
[Time]思考+尝试02:44
## 题目详情
Bob is preparing to pass IQ test. The most frequent task in this test is to find out which one of the given n numbers differs from the others. Bob observed that one number usually differs from the others in evenness. Help Bob — to check his answers, he needs a program that among the given n numbers finds one that is different in evenness.
> $3<=n<=100$
## 提交详情
- 提交时间:16:45 状态:AC 详细状态:`-`
> 分别用两个不同的`vector`保存奇偶性质不同的坐标即可  
输出那个大小为1的容器中唯一值即可

> **过题数降序、Rating为1300、题型为DP**
# 189A.Cut Ribbon
[Rating]1300
[Tag]暴力枚举、动态规划
[Time]思考26:47,补题13:20
## 题目详情
Polycarpus has $a$ ribbon, its length is $n$. He wants to cut the ribbon in a way that fulfils the following two conditions:
- After the cutting each ribbon piece should have length $a$, $b$ or $c$.
- After the cutting the number of ribbon pieces should be maximum.
Help Polycarpus and find the number of ribbon pieces after the required cutting.
> $1<=n,a,b,c<=4000$
## 思考
> 有三种构造方法:
> 1. 寻找 $gcd(a,b)$ 在不超过 $n$ 的所有数，找到其中 $n-i$ ($i=n*a+m*b$) 是 $c$ 的倍数，这个数的答案为 $(n-i)/c+n+m$  
> 2. 同上，对 $gcd(a,c)$ 寻找 $b$  
> 3. 同上，对 $gcd(b,c)$ 寻找 $a$  
> 但当前麻烦之处，在于我知道任何 $n*a+m*b$ 都是 $gcd(a,b)$ 的倍数，但是我不好解决  
> 尤其是这题明牌DP的情况下，DP枚举和推荐没想到
## 提交详情
- 提交时间:17:34 状态:AC 详细状态:`-`
> 经GPT指导下，我照GPT做出来前推DP版本  
设`dp[i]`是当前长度 $i$ 下最大方案数  
如果绳子足够 $a$ ,当前就是`dp[i] = max(dp[i], dp[i - a] + 1)`  
对于 $b,c$ 同理，最后答案就是`dp[n]`(因为题目说必定存在一个方案)
- 提交时间:17:37 状态:AC 详细状态:`-`
> 暴力下当前数据范围内必定可解，之前不行是因为我的暴力方法有误  
我应该对于 $i=na+mb$ 去枚举 $n,m$ 而不是 $i$ 本身  
由于在得到 $i$ 的情况下自然可得到 $c$ 的倍数  
故不用想我之前一样去重复枚举`a,b,c`、`a,c,b`、`b,c,a`  
因为这三种得到的答案都是一样的
- 提交时间:17:34 状态:AC 详细状态:`-`
> 相较于DP前推的方法，我更喜欢后推的版本  
`dp[i]`的定义如之前一样  
那么当前，只要剩余的足够`a`、`b`或`c`，我就去选择对应的绳子段  
然后修改`dp[i+a]`(例如我选择了 $a$)即可

# 476B.Dreamoon and WiFi
[Rating]1300
[Tag]状态压缩、暴力枚举、组合数学、动态规划、数学
[Time]思考20:45,补题16:27
## 题目详情
Dreamoon is standing at the position 0 on a number line. Drazil is sending a list of commands through Wi-Fi to Dreamoon's smartphone and Dreamoon follows them.
Each command is one of the following two types:
1. Go 1 unit towards the positive direction, denoted as '+'
2. Go 1 unit towards the negative direction, denoted as '-'
But the Wi-Fi condition is so poor that Dreamoon's smartphone reports some of the commands can't be recognized and Dreamoon knows that some of them might even be wrong though successfully recognized. Dreamoon decides to follow every recognized command and toss a fair coin to decide those unrecognized ones (that means, he moves to the 1 unit to the negative or positive direction with the same probability 0.5).
You are given an original list of commands sent by Drazil and list received by Dreamoon. What is the probability that Dreamoon ends in the position originally supposed to be final by Drazil's commands?
## 思考
> 如果按明牌动态规划来看，可以设置`dp[i][0]`为Dreamoon执行第 $i$ 个指令时，选择`+`的最终位置  
然后我初始设置为0，每一步都选择前一步最靠近的那一点  
```cpp
// 当指令为`?`时
int a1 = dp[i - 1][0] + 1, b1 = dp[i - 1][1] + 1;
dp[i][0] = abs(a1 - loc) <= abs(b1 - loc) ? a1 : b1;
int a2 = dp[i - 1][0] - 1, b2 = dp[i - 1][1] - 1;
dp[i][1] = abs(a2 - loc) <= abs(b2 - loc) ? a2 : b2;
```
可问题是，对于长达 $k$ 的`?`序列，拥有的结果可有 $2^k$ 个  
我这样的贪心未免有点决绝了
## 提交详情
- 提交时间: 状态: 详细状态:``
> **GPT**告诉我的思路是，定义`dp[i][pos]`为执行第 $i$ 个指令到达位置 $pos$ 的概率  
由于可能到达最左的`-n`以及最右的`n`,故数组二维要开`2n+1`的大小,并定义好偏移量`n`  
然后对于每条指令，都要枚举所有位置  
```cpp
// 当前指令为`+`
dp[i + 1][pos + 1 + offset] += pro;
// 当前指令为`-`
dp[i + 1][pos - 1 + offset] += pro;
// 当前为未知指令
dp[i + 1][pos + 1 + offset] += pro * 0.5L;
dp[i + 1][pos - 1 + offset] += pro * 0.5L;
```