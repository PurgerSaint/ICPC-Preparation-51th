1. 训练
# (1115 Div.2)2252C.Risky Tower
[Rating](来自CList)1311
[Tag]二分、暴力枚举、数据结构、贪心、排序
[Time]理解+尝试45:50，补题07:44
## 题目详情
You are playing a 2D Jenga game represented by a grid with $n$ rows and $m$ columns. The $1$\-st row is the top level of the tower, and the $n$\-th row is the bottom level.
Each piece at row $i$ and column $j$ has a destabilization factor $a_{i,j}$. Additionally, each row $i$ has an initial stability index of $v_i$.
When you remove a piece, it is completely discarded from the game. Removing a piece from row $i$ damages all levels at and above it. Specifically, for every row $k$ such that $1 \le k \le i$, its stability index is decreased by $a_{i,j}$.
The tower collapses if either of the following conditions is met:
-   The stability index of any level drops to $0$ or less.
-   Any level is left with exactly $0$ pieces (even if it is the topmost level).
Find the minimum number of pieces you must remove such that the tower collapses.
**Input**
Each test contains multiple test cases. The first line contains the number of test cases $t$ ($1 \le t \le 10^4$). The description of the test cases follows.
The first line of each test case contains two integers $n$ and $m$ ($1 \le n, m \le 10^6$) — the number of rows and columns of the tower.
The second line contains $n$ integers $v_1, v_2, \ldots, v_n$ ($1 \le v_i \le 10^9$) — the initial stability indices of each level from top to bottom.
Each of the next $n$ lines contains $m$ integers. The $j$\-th integer on the $i$\-th line is $a_{i, j}$ ($1 \le a_{i, j} \le 10^9$) — the destabilization factor of the piece at row $i$ and column $j$.
It is guaranteed that the sum of $n \cdot m$ over all test cases does not exceed $10^6$.
**Output**
For each test case, output a single integer — the minimum number of pieces that must be removed to collapse the tower.
## 提交详情
- 提交时间:11:40 状态:WA 详细状态:`Wrong answer on test 2`
> 存在源于学长的提示:*维护K大堆*  
初始想法为寻找一个稳定值最低的一层 $i$ ，然后在 $n$ 层到 $i$ 层中选择若干块，使得这些快的不稳定因子总和大于或等于 $i$ 层稳定值  
亦或是，对于自 $n$ 层到 $1$ 层的每层 $k$ ，是否在 $n$ 层到 $k$ 层中存在若干块不稳定因子总和大于或等于 $k$ 层不稳定值  
由于本题每层块数相等，且由题目条件可知，存在一个最少答案 $m$   
故上述问题还要改成，是否存在比 $m$ 块少的块数满足条件  
设定`ans`初始为 $m$ , 然后把每层的不稳定值放到`multset`中  
然后自底向上遍历每一块，维护一个大小不大于或等于 $m$ 的最小堆  
当大小未达到 $m$ 或堆顶元素比当前元素还小时，(堆顶比当前元素小)删除堆顶，插入当前元素  
同时维护这个堆的不稳定因子总和  
遍历完每块，再根据当前总和在`multset`中寻找是否有当前总和一样或更小的，如果有，当前堆大小就是答案之一，将其与`ans`选更小的替换即可  
遍历完每一层，要在`multset`中删除当前层的不稳定值
- 提交时间:11:53 状态:AC 详细状态:`-`
> 经*GPT*指导，我发现我的错误如下:
> 1. 堆缩小的条件应该为`pq.size()>ans`，即缩到最大为`ans`，这样才能保证更大的才能被有效替换  
> 2. 入队后不是根据当前总和去查，而是要缩小堆，看看是否有更小的答案

# (1103Div.3)2236E.Friendly Gifts
[Rating]1700
[Tag]暴力枚举、DP
[Time]尝试+理解39:38
## 题目详情
Arseniy decided to make his friends Dabir and Egor happy. For this, he decided to give each of them an array of numbers **of the same length**. An array $b$ is called good if its elements can be rearranged so that for all $i \gt 1$ the condition $b_i - b_{i - 1} = 1$ holds.
Arseniy wants Dabir and Egor to be able to play with these arrays. For this, the following conditions must be satisfied:
1.  Each of the given arrays is good.
2.  If you write one array after the other (in other words, concatenate them), the resulting array is also good.
Arseniy already has an array $a$ of length $n$. He plans to cut both arrays from $a$, that is, to choose two non-overlapping subsegments of **the same length**. Help Arseniy determine the maximum possible length of the resulting arrays.
**Input**
The first line contains a single integer $t$ $(1 \le t \le 1000)$ — the number of test cases.
Then $t$ test cases follow.
The first line of each test case contains a single integer $n$ $(1 \le n \le 6000)$.
The second line contains $n$ integers $a_1, a_2, \dots, a_n$ $(1 \le a_i \le n)$.
It is guaranteed that the sum of $n$ over all test cases does not exceed $6000$.
**Output**
For each test case, output a single integer — the maximum possible length of the arrays.
## 提交详情
- 提交时间:14:11 状态:WA 详细状态:`Wrong answer on test 2`
> 经学长提醒，这题可能涉及BIT  
从`a`中找到一个最长序列的好数组，然后要使得这个序列长度为偶数，能够可以对半完整平分  
用两个`set`，一个`st1`存储当前数，另一个`st2`就存储待用的数  
遍历每个数，如果当前数的前一个或后一个在`st1`中，就将这个数放到`st1`中，否则放到`st2`中  
然后再将`st2`的数倒回到`st1`，根据`st1`的大小决定答案  
可是我没注意到题目条件，要求分开后的两个等长度小数组也是好数组  
即需要保持原次序分成两等部分后，每部分依旧是好数组
- 提交时间:14:57 状态:AC 详细状态:`-`
> 经*GPT*分析后，我的问题依旧如之前所说的一样  
要求一个好数组均分两部分后，每部分都能依照原次序构成一个好数组  
而我的`set`直接抛弃了位置信息，仅寻找了数组中最长的好数组  
学长和作者的思路都一样，都是寻找每个好数组的区间，然后寻找两个等长且相邻的区间即可  
两区间值域不交必然区间无交集，因为每部分都是好数组(相邻差为1且升序)，且要求拼接后还是好数组，必然不可能相交  
按作者的思路，只要找到每个好数组并标记，然后从长到短枚举长度即可  
这题只能枚举值域，因为`a`可以由两段不相邻的小好数组拼接而成

# (1103 Div.3)2236F1.Elections in Saransk (easy version)
[Rating]1700
[Tag]数论
[Time]理解+思考20:58，补题31:53
## 题目详情
**This is the easy version of the problem. The only difference is that $x = 1$**
On the way home after buying his favorite soda "Zola Cero", Egor saw that elections for the position of "Best Number" are taking place in Saransk.
There are $n$ people at the polling station. Each person brought a number $a_i$. When the $i$\-th person enters the voting booth, they choose a candidate that is a **divisor** of the number $a_i$. Let the chosen candidate be $p_i$.
After everyone has voted, we get an array of votes $[p_1, p_2, \ldots, p_n]$.
Egor really likes the number $x$ and considers the voting ideal if $x \cdot {lcm}(p_1, p_2, \ldots, p_n)$$^{\text{∗}}$ = $p_1 \cdot p_2 \cdot \ldots \cdot p_n$. Help him find the number of different$^{\text{†}}$ arrays $p$ modulo $10^9 + 7$ that are ideal.
$^{\text{∗}}$$lcm$ — [least common multiple](https://codeforces.com/r/lcm-wiki).
$^{\text{†}}$Two arrays of votes are considered **different** if there exists an index $i$ where the two arrays have different elements.
**Input**
The first line contains a single integer $t$ ($1 \leq t \leq 10^4$) — the number of test cases.
Then $t$ test cases follow.
The first line of each test case contains two integers $n$ and $x$ ($1 \leq n \leq 10^5$, $x = 1$) — the number of voters at the polling station and Egor's favorite number.
The second line of each test case contains $n$ integers: $a_1, a_2, \dots, a_n$ ($1 \leq a_i \leq 5 \cdot 10^5$) — the numbers brought by the voters.
It is guaranteed that the sum of $n$ over all test cases does not exceed $10^5$.
**Output**
For each test case, output the number of ways modulo $10^9 + 7$ to vote so that the resulting array of votes satisfies the condition.
## 提交详情
- 提交时间: 状态: 详细状态:``
> $lcm(p_{1},p_{2},\dots,p_{n})=p_{1}\times p_{2}\times \dots\times p_{n}$  
上述式子成立，当且仅当 $p_{1},p_{2},\dots,p_{n}$ 之间互质，即任意两数之间的最大公因数仅为1  
对于题目式子，可转换为 $lcm(p_{1},p_{2},\dots,p_{n})=\frac{p_{1}\times p_{2}\times \dots\times p_{n}}{x}$ ，即对于某数 $p_{i}=x\times p_{i}$ 仅 $x=1$ 时于 $p_{i}\in R$ 成立  
那么就是对于本题恰好 $x=1$ ，寻找长度为 $n$ 且 $p_{1},p_{2},\dots,p_{n}$ 之间互质的数组 $p$ 数量  
对于 $a,b$ ，$lcm(a,b)=\frac{a\times b}{gcd(a,b)}$ ，扩展到本题，恰好是 $gcd(p_{1},p_{2},\dots,p_{n})=1$ ，任意两个相邻数的最大公因数为1  
*以上仅思考，未写出具体代码*  
经*GPT*指导，`p`实际上是由`a`中每个数的因子组成的，那么就需要枚举`a`中每个元素 $a_{i}$ ，寻找 $a_{i}$ 的质因数有哪些，根据每个元素的质因数进行构造`p`，得到`p`的可行数量  
前面推导中，任意两相邻数互质是错误的，必须要任意两个 $p_{i},p_{j}$ 满足互质  
即对于`a`中每个因数的质数因子，其必然只能出现一次  
则答案就为 $\prod_{\text{prime}}\left(1+\sum_i v_{\text{prime}}(a_i)\right)$

2. 题型训练
# 力扣62. 不同路径
[Rating]中等
[Tag]-
[Time]理解+尝试08:56
## 题目详情
一个机器人位于一个 m x n 网格的左上角 （起始点在下图中标记为 “Start” ）。
机器人每次只能向下或者向右移动一步。机器人试图达到网格的右下角（在下图中标记为 “Finish” ）。
问总共有多少条不同的路径？
## 提交详情
- 提交时间:16:46 状态:AC 详细状态:`-`
> 一开始我想着四方向，然后就是递归结果加一，在终点直接返回1  
但其实不用递归结果加一，直接根据终点返回即可  
然后题目要的是两方向，保留向右和向上即可  
直接提交超时，然后就对枚举的坐标 ${i,j}$ 设置为两个状态，进行记忆化保存即可  
用DP提交，可得到状态转移为`dp[i][j]=dp[i-1][j]+dp[i][j-1]`

# P2842 纸币问题 1
[Rating]普及−
[Tag]动态规划 DP、背包 DP
[Time]理解+尝试09:34
## 题目详情
某国有 $n$ 种纸币，每种纸币面额为 $a_i$ 并且有无限张，现在要凑出 $w$ 的金额，试问最少用多少张纸币可以凑出来？（保证可以凑出对应金额）
*输入格式*
第一行两个整数 $n,w$，分别表示纸币的种数和要凑出的金额。  
第二行一行 $n$ 个以空格隔开的整数 $a_1, a_2, a_3, \dots a_n$ 依次表示这 $n$ 种纸币的面额。
*输出格式*
一行一个整数，表示最少使用的纸币张数。
*说明/提示*
对于 $40\%$ 的数据，满足 $n\le 10$，$w\le 100$；  
对于 $100\%$ 的数据，满足 $1\le n\le 10^3$，$1\le a_i , w\le 10^4$。
## 提交详情
- 提交时间:17:03:03 状态:AC 详细状态:`-`
> 不限个数的背包DP，用DFS+记忆化实现  
DFS中对当前金额枚举所有钱币即可  
记忆化只用开一维数组，记录某个金额下的最少币量即可
- 提交时间:17:08:05 状态:AC 详细状态:`-`
> 正向反向DP都可以，设当前枚举的金额为 $i$ ，当前枚举的硬币为 $c$ ，得到状态转移为`dp[i+c]=min(dp[i+c],1+dp[i])`

# P2840 纸币问题 2
[Rating]普及−
[Tag]动态规划 DP、背包 DP
[Time]理解+尝试18:01
## 题目详情
*题目背景*
你是一个非常有钱的小朋友。
*题目描述*
你有 $n$ 种面额互不相同的纸币，第 $i$ 种纸币的面额为 $a_i$ 并且有无限张，现在你需要支付 $w$ 的金额，求问有多少种方式可以支付面额 $w$，答案对 $10^9+7$ 取模。  
注意在这里，同样的纸币组合如果支付顺序不同，会被视作不同的方式。例如支付 $3$ 元，使用一张面值 $1$ 的纸币和一张面值 $2$ 的纸币会产生两种方式（$1+2$ 和 $2+1$）。
*输入格式*
第一行两个正整数 $n,w$，分别表示纸币的种数和要凑出的金额。  
第二行一行 $n$ 个以空格隔开的正整数 $a_1, a_2, \dots a_n $ 依次表示这 $n$ 种纸币的面额。
*输出格式*
一行一个整数，表示支付方式的数量。
*说明/提示*
对于 $40\%$ 的数据，满足 $n\le 10$，$w\le 100$；  
对于 $100\%$ 的数据，满足 $1\le n\le 10^3$，$1\le a_i , w\le 10^4$。
其实小朋友并没有那么多钱。
## 提交详情
- 提交时间:17:24:34 状态:AC 详细状态:`-`
> 同力扣62一样，使用DFS进行递归迭代，到终点(反向就是金额为0)时返回1，每一步枚举所有金币并计数即可  
然后对金额加入记忆化，这题是金额对应的凑钱方案数量  
- 提交时间:17:32:13 状态:AC 详细状态:`-`
> DP改写成功，当前金额的方案数应该是金额减去每个金币面额得到的所有新金额方案数的总额  
即 $dp_{i}=dp_{i-c_{n}}+\dots+dp_{i-c_{n}}$  
为什么前面钱币数量要加一，因为加一代表多使用了一张钱币  
为什么这里方案数不用加一，因为对于当前金额 $m$ 的方案数，是从另一金额加钱币金额得来的，即 $dp_{i}=dp_{i-c_{n}}+\dots+dp_{i-c_{n}}$    
而0元的方案数是1，代表直接使用了一张纸币，往后推到每个金额即可

# P2834 纸币问题 3
[Rating]普及−
[Tag]动态规划 DP、背包 DP
[Time]理解+补题43:29
## 题目详情
*题目背景*
你是一个非常有钱的小朋友。
**注意：** 本题和《进阶篇》的对应题目，输入格式略有差异。
*题目描述*
你有 $n$ 种面额互不相同的纸币，第 $i$ 种纸币的面额为 $a_i$ 并且有无限张，现在你需要支付 $w$ 的金额，请问有多少种纸币组合能恰好支付金额 $w$，答案对 $10^9+7$ 取模。
*输入格式*
第一行两个正整数 $n,w$，分别表示纸币的种数和要凑出的金额。  
第二行一行 $n$ 个以空格隔开的正整数 $a_1, a_2, \dots a_n$ 依次表示这 $n$ 种纸币的面额。
*输出格式*
一行一个整数，表示能恰好凑齐面额 $w$ 的纸币组合数量。
*说明/提示*
对于 $40\%$ 的数据，满足 $n\le 10$，$w\le 100$；  
对于 $100\%$ 的数据，满足 $1\le n\le 10^3$，$1\le a_i , w\le 10^4$。
其实小朋友并不有钱。
## 提交详情
- 提交时间:18:19:51 状态:AC 详细状态:`-`
> 经*GPT*指导后，这题用DFS比DP更加直观，更易理解  
在DFS中，只要当前剩余金额和选择的硬币，计算两种情况的方案数总额  
分别是使用当前硬币的情况，加上不使用当前硬币的情况  
记忆化则是开二维，分别对应当前剩余金额和选择的硬币这两种状态

# P1115 最大子段和
[Rating]普及-
[Tag]动态规划 DP 贪心 递推 分治 前缀和 线性 DP
[Time]理解+尝试10:12
## 题目详情
*题目描述*
给出一个长度为 $n$ 的序列 $a$，选出其中连续且非空的一段使得这段和最大。
*输入格式*
第一行是一个整数，表示序列的长度 $n$。
第二行有 $n$ 个整数，第 $i$ 个整数表示序列的第 $i$ 个数字 $a_i$。
*输出格式*
输出一行一个整数表示答案。
*数据规模与约定*
- 对于 $40\%$ 的数据，保证 $n \leq 2 \times 10^3$。
- 对于 $100\%$ 的数据，保证 $1 \leq n \leq 2 \times 10^5$，$-10^4 \leq a_i \leq 10^4$。
## 提交详情
- 提交时间:21:41:47 状态:AC 详细状态:`-`
> 我原本想用滑动窗口，在和变小或为负数时缩短左边界，然后逐步扩大右边界  
但这里还是只能用DP，保存每一段的和  
如果上一段的和加上当前值还没有当前值大，就舍弃上一段和创造以当前元素开头的新子段  
咨询*GPT*后发现这叫Kadane算法