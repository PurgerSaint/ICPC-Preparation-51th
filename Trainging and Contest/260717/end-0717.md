> **过题数降序、Rating为1200的题目**
# 1857C.Assembly via Minimums
[Rating]1200  
[Tag]贪心、排序  
[Time]思考+尝试39:22  
## 题目详情
Sasha has an array $a$ of $n$ integers. He got bored and for all $i$, $j$ ($i \lt j$), he wrote down the minimum value of $a_i$ and $a_j$. He obtained a new array $b$ of size $\frac{n\cdot (n-1)}{2}$.
For example, if $a=$ \[$2,3,5,1$\], he would write \[$\min(2, 3), \min(2, 5), \min(2, 1), \min(3, 5), \min(3, 1), min(5, 1)$\] $=$ \[$2, 2, 1, 3, 1, 1$\].
Then, he randomly **shuffled** all the elements of the array $b$.
Unfortunately, he forgot the array $a$, and your task is to restore any possible array $a$ from which the array $b$ could have been obtained.
**The elements of array $a$ should be in the range $[-10^9,10^9]$**.
> $1<=t<=200, 2<=n<=10^3, -10^9<=b_{i}<=10^9$
## 提交详情
- 提交时间:14:57 状态:AC 详细状态:-
> 由 $1<=i<j<=n$ 下构建 $min(a_{i},a_{j})$ 组成长度为 $\frac{n\cdot (n-1)}{2}$ 的数组 $b$  
对数组 $b$ 进行排序，每个 $a_{i}(1<=i<=n)$ 对应的序列 $len=n-i$ 在 $b$ 中必然全部相等  
例如 $a$ 的第一个元素，对应 $b$ 中排序后的前 $n-1$ 个元素，这些元素必然相等  
如果不相等，出现了一个更小或更大的，就完全无法构建了  
而题目说明必然存在一个 $a$ ,那么上述条件必然成立  
故取得 $b$ 中每段作为 $a$ 的对应元素即可

# 1872D.Plus Minus Permutation
[Rating]1200
[Tag]数学
[Time]思考+理解用时23:09, 补题03:29
## 题目详情
You are given $3$ integers — $n$, $x$, $y$. Let's call the score of a permutation$^\dagger$ $p_1, \ldots, p_n$ the following value:
$$
(p_{1 \cdot x} + p_{2 \cdot x} + \ldots + p_{\lfloor \frac{n}{x} \rfloor \cdot x}) - (p_{1 \cdot y} + p_{2 \cdot y} + \ldots + p_{\lfloor \frac{n}{y} \rfloor \cdot y})
$$
In other words, the score of a permutation is the sum of $p_i$ for all indices $i$ divisible by $x$, minus the sum of $p_i$ for all indices $i$ divisible by $y$.
You need to find the maximum possible score among all permutations of length $n$.
For example, if $n = 7$, $x = 2$, $y = 3$, the maximum score is achieved by the permutation $[2,\color{red}{\underline{\color{black}{6}}},\color{blue}{\underline{\color{black}{1}}},\color{red}{\underline{\color{black}{7}}},5,\color{blue}{\underline{\color{red}{\underline{\color{black}{4}}}}},3]$ and is equal to $(6 + 7 + 4) - (1 + 4) = 17 - 5 = 12$.
$^\dagger$ A permutation of length $n$ is an array consisting of $n$ distinct integers from $1$ to $n$ in any order. For example, $[2,3,1,5,4]$ is a permutation, but $[1,2,2]$ is not a permutation (the number $2$ appears twice in the array) and $[1,3,4]$ is also not a permutation ($n=3$, but the array contains $4$).
> $1<=t<=10^4, 1<=n<=10^9, 1<=x,y<=n$
## 提交详情
- 提交时间:15:27 状态:TLE 详细状态:`Time limit exceeded on test 4`
> 使得
> $$
> \sum_{\substack{i=1\\i\equiv 0\pmod{x}}}^{n} a_i
> -
> \sum_{\substack{j=1\\j\equiv 0\pmod{y}}}^{n} a_j
> $$
> 最大化, 那么就直接在 $a_{i}(i=x,2x,\dots,\lfloor \frac{n}{x} \rfloor*x)$ 放最大元素，然后在 $a_{j}(j=y,2y,\dots,\lfloor \frac{n}{y} \rfloor*y)$ 放最小的元素  
即对于下标为 $x$ 倍数的元素，放 $[1,n]$ 中后 $\lfloor \frac{n}{x} \rfloor$ 个元素;  
对于下标为 $y$ 倍数的元素，放 $[1,n]$ 中前 $\lfloor \frac{n}{y} \rfloor$ 个元素即可  
但同时要注意，若下标同时为 $x,y$ 的倍数，这个元素放啥都可以  
判断同时为 $x,y$ 倍数，不能只用之前的裴蜀定理，必须是 $lcm(x,y)$ 的倍数
- 提交时间:15:44 状态:AC 详细状态:-
> **GPT补题**
> 经GPT指导下，我应该使用等差数列求和公式，而不是之前的循环逐个加减  
> 等差数列求和公式: $S_{n}=\frac{n\times (a_{1}+a_{n})}{2}$  
> 那么对于下标为 $x$ 倍数的元素，其总和为 $\frac{\lfloor \frac{n}{x} \rfloor\times(n-\lfloor \frac{n}{x} \rfloor+1+n)}{2}$  
那么对于下标为 $y$ 倍数的元素，其总和为 $\frac{\lfloor \frac{n}{y} \rfloor\times(1+\lfloor \frac{n}{y} \rfloor)}{2}$  

> **过题数降序、Rating为1300、题型为二分的题目**
# 1538C.Number of Pairs
[Rating]1300
[Tag]二分、数据结构、数学、双指针
[Time]理解+思考22:58,补题
## 题目详情
You are given an array $a$ of $n$ integers. Find the number of pairs $(i, j)$ ($1 \le i \lt j \le n$) where the sum of $a_i + a_j$ is greater than or equal to $l$ and less than or equal to $r$ (that is, $l \le a_i + a_j \le r$).
For example, if $n = 3$, $a = [5, 1, 2]$, $l = 4$ and $r = 7$, then two pairs are suitable:
-   $i=1$ and $j=2$ ($4 \le 5 + 1 \le 7$);
-   $i=1$ and $j=3$ ($4 \le 5 + 2 \le 7$).
> $1<=t<=10^4, 1<=n<=2\times 10^5, 1<=l<=r<=10^9$
## 提交详情
- 提交时间:16:28 状态:WA 详细状态:`Wrong answer on test 2`
> 先将 $a$ 进行升序排序，然后用迭代器遍历 $a$  
对于每个迭代器`it`，用二分搜寻后续元素中第一个大于或等于的元素  
左边界为`l-*it` ,右边界为`r-*it`, 当前元素的可用就是两边界的差值
- 提交时间:16:40 状态:AC 详细状态:`-`
> **GPT补题**, 我左边界用`lower_bound`是对的，因为要取第一个大于或等于 $l-a_{i}$ 的元素位置  
但右边界我用的还是`lower_bound`, 此时会取得第一个 $r-a_{i}$ 的位置  
应该要取最后一个符合条件的位置，应使用`upper_bound`，并且答案正好是右边界减去左边界的总和

# 1927D.Find the Different Ones!
[Rating]1300
[Tag]二分、暴力枚举、数据结构、动态规划、并查集、贪心、双指针
[Time]思考30:23，补题09:29
## 题目详情
You are given an array $a$ of $n$ integers, and $q$ queries.
Each query is represented by two integers $l$ and $r$ ($1 \le l \le r \le n$). Your task is to find, for each query, two indices $i$ and $j$ (or determine that they do not exist) such that:
-   $l \le i \le r$;
-   $l \le j \le r$;
-   $a_i \ne a_j$.
In other words, for each query, you need to find a pair of different elements among $a_l, a_{l+1}, \dots, a_r$, or report that such a pair does not exist.
> $1<=t<=10^4, 1<=n<=2\times 10^5, 1<=a_{i}<=10^9, 1<=l<=r<=n$
## 思考
> 对查询区间进行排序，然后从每一个小区间开始往后找  
如果一个小区间有答案，覆盖他的大区间必定有答案  
否则就枚举整个区间，找到两个不相同的答案即可
## 作者题解
> 作者是对每个坐标优先保存左侧第一个不同元素的下标  
即不同的元素中最近的下标  
然后每个查询就用右边界去查询

> **Rating区间为[1100,1200]、随机选题的盲训**
# 2195C.Dice Roll Sequence
[Rating]1100
[Tag]动态规划、贪心
[Time]思考21:32(+1.5h)
## 题目详情
Consider the following cube $D$ where numbers $x$ and $7-x$ **lie on opposite sides**:
A sequence $b$ of integers from $1$ to $6$ is called a dice roll sequence if it satisfies the following condition:
-   All pairs of adjacent elements lie on adjacent$^{\text{∗}}$ sides of the cube.
For example, $[1,4,2]$ is a dice roll sequence, while $[3,4,6,3]$ is not because $3$ and $4$ are not on adjacent sides of the dice. Additionally, $[2,2,4]$ is not a dice roll sequence because $2$ and $2$ are on the same (not adjacent) side of the dice.
Given a sequence $a$ of $n$ integers from $1$ to $6$, you can perform the following operation any number of times (possibly zero).
-   Select an index $1 \le i \le n$ and an integer $1 \le x \le 6$. Then, change the value of $a_i$ to $x$.
Please determine the **minimum number of operations** required to make $a$ a dice roll sequence.
$^{\text{∗}}$Two sides of the cube $S$ and $T$ are called adjacent if they share **exactly** one edge of the cube. **Do note that** this condition implies $S \neq T$ as well.
> $1<=t<=10^4, 1<=n<=3\times 10^5, 1<=a_{i}<=6$
## 提交详情
- 提交时间:18:39 状态:WA 详细状态:`Wrong answer on test 2`
> 只要检查每相邻两个是否是在骰子中相邻的即可  
是否会产生涟漪，即当前修改影响到下一个？不会  
假设存在三个相邻的`1`、`2`、`3`  
如果`1`、`2`位于对立面(不同)，修改完全影响不了`3`  
因为无论`1`和`3`是什么，是对立、相同还是相邻  
由于骰子每个面都与4个面相邻，由于`1`和`2`对立，`3`不管是啥，都能直接修改`2`达到这三个平衡(符合题目条件)
- 提交时间:20:06 状态:WA 详细状态:`Wrong answer on test 2`
> 那就照前面所说，我只进行两两检查即可，即检查每对是否要修改(原为对立或相同)  
如果要修改，就对这一对进行标记  
然后再检查每对之间的，如果每对之间要修改，且之前没修改标记，就计数，否则跳过
- 提交时间:22:06 状态:AC 详细状态:`-`
> 看作者题解和GPT指导下，这题是图论中的**最小边覆盖**，代码如下:
```cpp
int solve() {
    int n, cnt = 0; cin >> n;
    vector<int> a(n);
    for (int& t: a) cin >> t;
    for (int i = 0; i + 1 < n; i++)
        if (a[i] + a[i + 1] == 7 || a[i] == a[i + 1]) cnt++, i++;
    return cnt;
}
```

# 2184B.Hourglass
[Rating]1100
[Tag]数学
[Time]思考01:09:10,补题10:32
## 题目详情
Vadim's hourglass measures $s$ minutes. He flipped it over, and the time started. Every $k$ minutes, Vadim will flip his hourglass again. He will do this even if the sand is still falling. Also, if the sand has already finished falling but there are still some minutes left, Vadim will wait for the required time and only then flip the hourglass. Each flip is done instantly.
However, Vadim needs to leave for errands in $m$ minutes, and he will stop flipping the hourglass (if Vadim needs to flip the hourglass in the last minute, he will do so). Determine how many minutes the sand will continue to fall after Vadim leaves.
> $1<=t<=10^4, 1<=s,k,m<=10^9$
## 提交详情
- 提交时间:21:24 状态:WA 详细状态:`Wrong answer on test 1`
> 如果 $s==k$ , 直接返回 $m%s$  
如果 $s<k$ ,沙漏会在 $k$ 的偶数倍时回到初始，因为每个 $k$ 奇数倍前翻转的 $k%s$ 会在翻转后回来  
如果 $s>k$ ,沙漏也会在 $k$ 的偶数倍时回到初始，因为前面翻转的 $k$ 会在之后翻转回来，而后强制翻转  
在 $s>k$ 下,如果 $m$ 最大是 $k$ 的偶数倍，那么最终答案就是`s-(m%k)`,奇数倍就是`k-(m%k)`,因为此时前面翻下去的 $k$ 被翻转回来了  
在 $s<k$ 下,如果 $m$ 最大是 $k$ 的偶数倍，那么答案还是`s-(m%k)`；如果是奇数倍，那么就要看`m%k`是否大于`k%s`  
如果大于，答案就是`s-(m%k-k%s)`;否则就是`k%s-m%k`
- 提交时间:21:42 状态:AC 详细状态:`-`
> 经GPT指导下，我犯了一个错误  
即题目只能在时间为 $k$ 的倍数时翻转沙漏，沙漏满了是不会继续翻转的  
在 $s>k$ 下,答案跟之前的相同  
在 $s<k$ 下，如果剩余($m\mod k$)的大于 $s$ ,最终就是0(因为不会再翻转了)，否则就是`s-(m%k)` 