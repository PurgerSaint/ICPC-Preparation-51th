# ABC 468
> 比赛名称: ABC 469
> 比赛时间: 2026/08/01 20:00-21:40
> 比赛结果: [Rank]7578 [Performance]436 [New-Rating]522 [Diff]-9
# A.Train Car
## 题目详情
There is a train consisting of $N$ cars. What is the position, counted from the back, of the car that is $K$\-th from the front of this train?
## 提交详情
- 提交时间:20:01:33 状态:AC 详细状态:`-`
> 模拟题，直接输出倒数第 $K$ 个数即可

# B.Isolated Seats
## 题目详情
You are given a string $S$ of length $N$ consisting of `o` and `x`.  
$N$ chairs are arranged in a row from left to right. If the $i$\-th character of $S$ is `o`, a person is sitting in the $i$\-th chair from the left; if it is `x`, no person is sitting there.  
Find the number of chairs that satisfy all of the following conditions.
-   No person is sitting in it.
-   There is no chair to its left, or no person is sitting in the chair to its left.
-   There is no chair to its right, or no person is sitting in the chair to its right.
## 提交详情
- 提交时间:20:10:19 状态:AC 详细状态:`-`
> 模拟题，枚举中间的所有座位，注意左右两侧

# C.Cantrip
[Time]补题23:25
## 题目详情
You are given a string $S$ of length $N$ consisting of `o` and `x`.  
$N$ bags are arranged in a row, and each bag contains one sweet.  
On the $i$\-th bag, "hit" is written if the $i$\-th character of $S$ is `o`, and "miss" is written if it is `x`.  
For each $k=1,2,\dots,N$, solve the following problem.
> Takahashi receives the first $k$ bags from the front of the row, eats the sweets inside them, and keeps the bags.  
> Then, he repeats the following action as many times as possible.
> 
> -   He discards one bag marked "hit" that he is holding, receives the bag at the front of the row, eats the sweet inside it, and keeps that bag. This action can only be performed when there is still a bag remaining in the row and he is holding a bag marked "hit".
> 
> Find the number of sweets he can eat.  
> Note that when he receives a bag, that bag is removed from the row.
> $$
*Constraints*
-   $1 \leq N \leq 8 \times 10^5$
-   $N$ is an integer.
-   $S$ is a string of length $N$ consisting of `o` and `x`.
## 提交详情
- 提交时间:21:19:17 状态:TLE 详细状态:`AC*5 TLE*13`
> 题目是纯模拟题，但是我卡在了不知道该怎么进行模拟  
对于当前已拿取的队列，如果队列中还有中奖的袋子被拿取了，可以消耗已拿取的中奖袋子数量继续拿取  
但麻烦的是，未来可以拿取的袋子中还有中奖的可以继续拿取  
由于这个，我无法根据数量进行枚举或扩展，因为后置不可测  
此次提交为纯模拟版本，当且仅当拿取队列中有中奖袋子时，继续扩展，直到队列中没有中奖袋子
- 提交时间:22:15:36 状态:AC 详细状态:`-`
> 经*GPT*指导下，我可以只保存所有`x`的位置，设`o`的数量为`hit`，则`x`的数量为`n-kit`  
然后枚举所有 $k$ ，并正确计数`o`得到每个 $k$ 的`hit`  
再对每个 $k$ 的`hit`而言，如果`hit`为0，则当前 $k$ 答案为 $k$  
如果`hit`的数量大于所有`x`的数量，那么当前`hit`能直接吃完所有的，为 $n$  
如果`hit`的数量小于或等于`x`的数量，那么当前 $k$ 的答案为第 $k$ 个`x`的位置  
因为当前`x`的数量为 $k-hit$ ，所有`o`可以进行补偿 $hit$ 个`x`，故可以吃到第 $(k-hit)+hit=k$ 个  
我一开始想的是模拟，但必定不行  
然后我想收集所有的`o`，然后对每个`o`根据接下来的`x`的数量得到答案，但是部分`x`的答案不能由这个得到(因为可以扩展)  
再然后我计算每个 $k$ 中`o`和`x`的数量，答案必定是`hit`的两倍左右，妄图通过 $k+hit$ 的递推得到当前 $k$ 的答案，以正序或倒序的顺序  
接着我又想通过队列模拟，但是字符串来回复制，比单纯模拟还要耗时  
最后由于走投无路，导致我直接交了模拟的

# D.The Big Two
[Time]补题40:35
## 题目详情
There is a game with $N$ players $1,2,\dots,N$.  
This game is played in a format where two players face each other one-on-one.  
A tournament among the $N$ players was held $M$ times, and the two players $A_m$ and $B_m$ advanced to the final of the $m$\-th tournament.  
Find the number of pairs of integers $x$ and $y$ satisfying the following conditions.
-   $1 \leq x \lt y \leq N$
-   In every tournament, at least one of players $x$ and $y$ advanced to the final.
*Constraints*
-   $2 \leq N \leq 2 \times 10^5$
-   $1 \leq M \leq 2 \times 10^5$
-   $1 \leq A_i \lt B_i \leq N$
-   All input values are integers.
## 思考
> 我以为是任选两个 $x,y$ ，满足都出现过  
但不对，这样数量更多  
如果是选出现次数最多的前两个？也不对，有样例不符合  
没看懂题目  
在*GPT*帮助下，我先理解了一次题目意义，做了一遍  
先保存每个玩家出现的次数  
然后根据次数保存对应的玩家  
再枚举所有次数对应的玩家集`a`，寻找是否有对应的另一个玩家集`b`  
`b`要满足其大小加上`a`的大小要大于或等于 $n$ ，以覆盖所有场次的比赛  
但是我忘记了，这样可能会导致遗漏
## 提交详情
- 提交时间:00:54:37 状态:AC 详细状态:`-`
> 这里作者用了贪心的方法，既然要求 $x,y$ 都必须至少出现一次在每一场比赛中，那贪心地直接取第一场比赛就行  
然后对第一场比赛的两个选手分别统计出非他们各自出场的所有比赛，取这些比赛的交集，得到其他选手  
最后再去重，得到最终答案

# E.Pro Exam Eligibility
[Time]补题01:11:12
## 题目详情
You are given a string $S$ of length $N$ consisting of `o` and `x`.  
It is guaranteed that $S$ contains at least $K$ occurrences of `o`.  
Takahashi played a certain game $N$ times.  
In the $i$\-th game, he won if the $i$\-th character of $S$ is `o`, and lost if it is `x`.
Takahashi chooses a pair of integers $l$ and $r$ satisfying the following conditions.
-   $1 \leq l \leq r \leq N$
-   He won at least $K$ times in the games from the $l$\-th through the $r$\-th.
Find the maximum possible value of the win rate in the games from the $l$\-th through the $r$\-th.
*Constraints*
-   $1 \leq K \leq N \leq 10^6$
-   $N$ and $K$ are integers.
-   $S$ is a string of length $N$ consisting of `o` and `x`.
-   $S$ contains at least $K$ occurrences of `o`.
## 提交详情
- 提交时间:21:57:54 状态:WA 详细状态:`AC*20 WA*15`
> 我先导入了最大公因数和最小公倍数的模版，然后做了比较两个分式的比较函数  
如果分母相等，分子大的分式必然更大；如果分子相等，分母小的分式必然更大；  
否则就取两者分母的最小公倍数，然后约分他们的分子，返回分子较大的那个分式即可  
题目中我先保存所有`o`的位置  
想要胜率最大化，在取胜场次固定的情况下，只能最小化总场次  
我以窗口大小为 $k$ 进行滑动窗口枚举所有连续`o`的位置，得到该窗口下的总场次  
然后取得所有窗口下总场次与 $k$ 的最小分式，输出答案即可  
对 $k=1$ 和`o`的数量小于 $k$ 的情况进行特例判断
- 提交时间:22:49:40 状态:WA 详细状态:`AC*21 WA*14`
> 既然知道分子 $k$ 固定，那我就找分子最小的那个，最后输出结果即可
- 提交时间:22:55:45 状态:WA 详细状态:`AC*13 WA*1 TLE*21`
> 忘记题目条件为*至少K个*，就根据第一次提交的，往后枚举，找到最大可能胜率即可
- 提交时间:23:30:13 状态:AC 详细状态:`-`
> 经*GPT*解读作者题解后，发现作者是用二分答案+线性判定  
用二分的原因是作者将题目原本的问题`猜一个胜率p`，转移到了`是否存在一个包含至少K个o的连续区间，其胜率至少为p`  
设该区间长度为 $len$ ,包含 $w$ 个`o`，对于胜率 $p\ge \frac{w}{len}$ ，将其转化为 $w-len\times p\ge 0$  
然后将其扩展 $w-wp-len\times p+wp=w(1-p)+(len-w)(-p)$  ，即 $w(1-p)+(len-w)(-p)\ge 0$
那么就可以对每个字符进行赋值，对`o`字符赋值 $1-p$ ，对`x`字符赋值 $-p$  
所以一个区间的胜率至少为 $p$ ，等价于这个区间的权值和至少为0  
同时将问题变成了是否存在一个至少K个`o`的区间，使权值和非负  
获取区间的权值和可以用前缀和的加减快速获取，区间内`o`的个数同理  
然后用滑动窗口的方法，枚举得到是否存在一个区间满足胜率至少为 $p$  
枚举 $p$ ，作者是用二分在 $[0,1]$ 中进行枚举，以上面的方法进行判断