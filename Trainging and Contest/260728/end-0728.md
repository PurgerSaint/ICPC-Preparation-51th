1. 模拟
# (2024-I)M.Find the Easiest Problem
[Source]2024-网赛-第一场
[Time]理解+思考14:13
## 题目详情
You are given all the submissions from an ICPC-style competition. For each submission, you know the team name, the problem ID, and whether the submission was accepted or rejected. Your task is to determine which problem was the easiest.
You may not be familiar with the ICPC rules, so here’s a brief explanation:
• Each team can make multiple submissions for a problem. A team is considered to have solved a problem if at least one of their submissions for that problem is accepted.
• The number of teams that solved a problem is the number of distinct teams that have at least one accepted submission for that problem.
We define the easiest problem in the competition as the one that has been solved by the most teams.
If there are multiple problems with the same number of teams solving them, the problem with the lexicographically smallest ID is considered the easiest.
Two teams are considered different if and only if their team names are different. Similarly, two problems are considered different if and only if their problem IDs are different.
> $1<=t<=1000, 1<=n<=10^5$
## 提交详情
- 提交时间:20:23:35 状态:AC 详细状态:`-`
> 我先用`vector<pair<char, set<string>>>`存储每道题通过的队伍名，给与 $26$ 的大小  
当且仅当提交状态为`accepted`时，才将该队伍存入到这个容器中  
`set<string>`自带的去重会避免重复  
最后排序时，先根据`set<string>`进行降序排序，若相等，则按`char`进行升序排序  
容器中的第一个必定为答案

2. 盲训
# 2161C.Loyalty
[Rating]1200
[Tag]构造、贪心、排序、双指针
[Time]尝试+理解35:56
## 题目详情
No Loyalty No Royalty (c)
You are a customer in a store and want to buy $n$ items. Each item $i$ has a price $a_i$ such that $1 \leq a_i \leq X$, where $X$ is a **loyalty factor**.
Your **loyalty level** in the store is defined as $\lfloor {S \over X} \rfloor$, where $S$ is the total cost of items purchased so far. Initially, $S = 0$.
If you buy an item with price $p$ and your loyalty level increases as a result of this purchase, you earn $p$ bonus points.
Your task is to find the **maximum number of bonus points** you can earn by choosing an optimal order of purchase for the items.
> $1<=t<=2\times 10^4, 1<=n<=10^5, 1<=X<=10^9, 1<=a_{i}<=X$
## 提交详情
- 提交时间:15:47 状态:AC 详细状态:`-`
> 我一开始是直接排序的，但是我发现达不到样例的答案  
然后我想了想，如果 $a_{i}$ 是大于或等于 $X$ ,那么 $a_{i}$ 这分是必然加的，且无论怎么放都能吃到    
如果 $a_{i}$ 小于 $X$ ，那么这部分就要尽可能凑，使得凑出多个 $X$  
对于题目要求 $\frac{S+p}{X}>\frac{S}{X}$ ,我一开始想的是将其化为 $\frac{p}{X}>0$  
但这样对于小于那一部分的所有元素，都没有任何贡献了  
那么就改为 $S\bmod X+a_{i}>X$ 了，即我的构造方法为，先排序小于那一部分 $a$  
然后先凑 $a$ 中足够多的小的，一旦 $a$ 的当前最大值能符合上述条件，就加入最终序列  
这样就能使得 $a$ 这一部分吃到的奖励足够大  
剩下大于或等于 $x$ 的那一部分 $b$ ，就直接将其放在最终序列尾部，$b$ 的排列次序任意，因为不管怎么放都能吃到  

# 2149D.A and B
[Rating]1200
[Tag]字符串处理
[Time]理解28:14，补题28:14
## 题目详情
Given a string $s$ of length $n$, consisting only of the characters 'a' and 'b'.
In one operation, you can choose a position $i$ ($1 \le i \le n-1$) and swap the neighboring characters $s_i$ and $s_{i+1}$.
You need to perform the minimum number of operations to ensure that all characters of one type (either $a$ or $b$) are located strictly together, forming exactly one continuous block.
Characters of the other type can be positioned either before or after this block, forming two (possibly empty) blocks.
Examples of valid final forms:
-   'aaabbbaaa' — all 'b's are located together (one block), 'a's can be both before and after this block;
-   'bbbaaaaaabbb' — all 'a's together, 'b's are at the edges of the string;
-   'aaaaabbbb' or 'bbbbaaaaa' — both types of characters form one continuous block each.
You need to find the minimum number of described operations required to achieve the specified state.
> $1<=t<=10^4, 1<=n<=2\times 10^5, s\in{'a','b'}$
## 思考
> 如果把`a`和`b`按位置分开来看，取得的答案必定是他两各自答案的最小值  
那么对于`a`和`b`的其中一种，要么是分开到两侧，要么是向中间集中  
而再细化到其中每个字符，有左右两个方向可选  
局部贪心能否扩展到全局贪心，不一定，因为集中下选择的中心点不同，就会导致多个答案  
故这题想的很乱，未写出
## 提交详情
- 提交时间:17:39 状态:AC 详细状态:`-`
> 经*GPT*解读**作者题解**，发现作者是直接控制最终变量:*字符最终要占用的连续区间*  
设所有`a`字符的位置都存在 $p$ 数组中， $p_{i}$ 代表第 $i$ 个`a`字符的位置  
再设`a`字符构成的连续块起始于位置 $L$ ,那么第 $i$ 个`a`字符要到达 $L+i$ 位置, 移动步数为 $p_{i}-(L+i)$  
那么这样就得到`a`字符下的总代价 $\displaystyle \sum_{m-1}^{i=0} |p_{i}-(L+i)|$  
可变形为 $\displaystyle \sum_{m-1}^{i=0} |(p_{i}-i)-L|$ ,此时设 $c_{i}=p_{i}-i$  
要使得代价最少，那么就使得 $|(p_{i}-i)-L|=|c_{i}-L|$ 最小，即使得每个 $c_{i}$ 都距离 $L$ 最小, 唯一可行方法就是使得 $L$ 取值为 $c$ 的中位数  
最后按以上方法得到`a`和`b`的代价后，两代价最小值即为答案

3. 题型训练 *Dijkstar*
# 力扣743.网络延迟时间
[Source]力扣
[Rating]中等
[Time]07:32
## 题目详情
有 n 个网络节点，标记为 1 到 n。
给你一个列表 times，表示信号经过 有向 边的传递时间。 `times[i] = (ui, vi, wi)`，其中 ui 是源节点，vi 是目标节点， wi 是一个信号从源节点传递到目标节点的时间。
现在，从某个节点 K 发出一个信号。需要多久才能使所有节点都收到信号？如果不能使所有节点收到信号，返回 -1 。
> $1 <= k <= n <= 100, 1 <= times.length <= 6000, times[i].length == 3, 1 <= ui, vi <= n, ui != vi, 0 <= wi <= 100$
## 提交详情
- 提交时间:20:40 状态:AC 详细状态:`-`
> 带权最短路的标准题，有两次提交错误，错误在于我没有识别是否不可达，还有我没有使用优先队列

# 洛谷P4779 【模板】单源最短路径（标准版）
[Source]洛谷
[Rating]普及
[Time]03:08
## 题目详情
*题目背景*
2018 年 7 月 19 日，某位同学在`NOI Day 1 T1 归程`一题里非常熟练地使用了一个广为人知的算法求最短路。
然后呢？
$100 \rightarrow 60$；
$\text{Ag} \rightarrow \text{Cu}$；
最终，他因此没能与理想的大学达成契约。
小 F 衷心祝愿大家不再重蹈覆辙。
*题目描述*
给定一个 $n$ 个点，$m$ 条有向边的带非负权图，请你计算从 $s$ 出发，到每个点的距离。
数据保证你能从 $s$ 出发到任意点。
$1 \leq n \leq 10^5$；
$1 \leq m \leq 2\times 10^5$；
$s = 1$；
$1 \leq u_i, v_i\leq n$；
$0 \leq w_i \leq 10 ^ 9$,
$0 \leq \sum w_i \leq 10 ^ 9$
## 提交详情
- 提交时间:21:23:26 状态:AC 详细状态:`-`
> 使用*GPT*给我最终版本，默写成功

# (2025-I)I.Knapsack Problem
[Source]2025-网赛-I
[Time]思考52:19, 补题08:34
## 题目详情
Given an undirected graph with n vertices and m edges, each edge holds an item with weight wi. Xiao S has a compulsion: every time he traverses an edge, he must put the item on that edge into his backpack.
The backpack has a capacity V . Initially, he starts with a new backpack. If the remaining capacity of the current backpack is insufficient to carry wi
, he switches to a new backpack (discarding the old one).
For each starting vertex from 1 to n, Xiao S chooses a path to reach a designated vertex T. Your task is to determine the minimum number of backpacks needed for each starting vertex to reach T. If it is impossible to reach T, output −1.
> $1<=n,m,V<=10^5, 1<=T<=n, 1<=x_{i},y_{i}<=n, 1<=w_{i}<=V$
## 思考
> 我知道这题是Dijkstar，但是要魔改  
如果纯用Dijkstar，会导致一件物品分两半放   
我先是加入了`背包`这个因素，发现无法控制算法去选择背包最少的路径  
然后再加入路径追踪，即收集算法收集了哪些物品，最后再计算所需背包数，发现也不行，问题如上
## 提交详情
- 提交时间:22:47:33 状态:AC 详细状态:`-`
> 经*GPT*补题后，我发现我的错误如下：
> 1. 结构体必须要加入对`背包`的比较，只有背包相同时才比较负载，否则比较背包大小
> 2. 更新时要同步比较新的背包和新的负载，只有当背包更少，或背包一样、负载更少时才更新，而不是像我之前直接比较少背包才进入更新  
> 即我需要背包和负载两个可控量同步走，非仅通过背包或负载的单一变量魔改Dijkstar