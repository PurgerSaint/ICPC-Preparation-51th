#include<bits/stdc++.h>
using namespace std;
using ll = long long;
#define ios ios::sync_with_stdio(false);cin.tie(0);
#define mod(t, m) (t % m + m) % m
#define range(a) a.begin(),a.end()

ll solve() {
    
}

int main() {
    ios;
    int tt; cin >> tt;
    while (tt--) {
        cout << solve() << (tt ? "\n" : "");
    }
    return 0;
}