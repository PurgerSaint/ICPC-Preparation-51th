> **过题数降序、Rating为1300的舒适区题目**
# 451B.Sort the Array
[Rating]1300
[Tag]模拟、排序
[Time]思考+尝试21:00
## 题目详情
Being a programmer, you like arrays a lot. For your birthday, your friends have given you an array a consisting of n distinct integers.
Unfortunately, the size of $a$ is too small. You want a bigger array! Your friends agree to give you a bigger array, but only if you are able to answer the following question correctly: is it possible to sort the array a (in increasing order) by reversing exactly one segment of a? See definitions of segment and reversing in the notes.
> $1<=n<=10^5$
## 提交详情
- 提交时间:15:38 状态:AC 详细状态:`-`
> 用另一个数组 $b$ 复制 $a$ ,然后对 $b$ 进行排序,并在数组 $c$ 中记录 $a$ 中每个元素的相对位置  
对于 $n$ 个不同元素的 $a$ ,保持升序必然使得每个元素都在其相对位置上  
记录每一段不等位置段，即当前元素所在位置非目标位置、当前位置元素比前一位置元素小  
如果存在多个不等位置段，必然不行  
如果不等位置段中存在差值超过1的相邻差值，必然不行  
最后对唯一不等位置段直接排序，输出这一段的段位位置和段头位置即可
- 提交时间:15:45 状态:AC 详细状态:`-`
> 其实看差值还是太麻烦了，这个不等位置段必然降序，元素降序且位置也降序  
那么该段中第一个元素必然是最大的位置，如果后续都不是与第一位置保持一定差值的，必然错误

# 459B.Pashmak and Flowers
[Rating]1300
[Tag]组合数学、模拟、排序
[Time]思考+尝试06:18,补题
## 题目详情
Pashmak decided to give Parmida a pair of flowers from the garden. There are n flowers in the garden and the i-th of them has a beauty number bi. Parmida is a very strange girl so she doesn't want to have the two most beautiful flowers necessarily. She wants to have those pairs of flowers that their beauty difference is maximal possible!
Your task is to write a program which calculates two things:
The maximum beauty difference of flowers that Pashmak can give to Parmida.
The number of ways that Pashmak can pick the flowers. Two ways are considered different if and only if there is at least one flower that is chosen in the first way and not chosen in the second way.
> $2<=n<=2\times 10^5, 1<=b_{i}<=10^9$
## 提交详情
- 提交时间:15:55 状态:WA 详细状态:`Wrong answer on test 4`
> 对 $b$ 进行排序，第一个元素为最小，第二个元素为最大  
然后记录最小元素和最大元素的出现次数，次数为组合数学的最小元素出现次数乘以最大元素出现次数
- 提交时间:15:58 状态:WA 详细状态:`Wrong answer on test 4`
> 均为相同元素时，其方法数应该是`n*(n-1)/2`，上一提交忘记除以2了
- 提交时间:15:58 状态:AC 详细状态:`-`
> 经*GPT*指导，问题在于，当 $n$ 为 $2\times 10^5$ 时， $n*(n-1)$ 以及 $cnt_mx*cnt_nx$ 均会超出`int`的范围  
理应在乘除时将其类型转换

> **Rating1100-1200、随意选择的**
# 2184C.Huge Pile
[Rating]1100
[Tag]二分、DFS及其变种、动态规划、图论、数学
[Time]理解+尝试26:13，补题32:15
## 题目详情
Andrei has a huge pile of $n$ apples. He can divide the pile into two smaller piles: if there are $x$ apples in the pile, he will get piles with $\lfloor \frac{x}{2} \rfloor$$^{\text{∗}}$ and $\lceil \frac{x}{2} \rceil$$^{\text{†}}$ apples. This division takes Andrei $1$ minute.
Andrei wants to eat $k$ apples, but he doesn't want to count them at all. That is why he wants to obtain a pile that contains exactly $k$ apples. Determine whether it is possible to achieve this by performing pile divisions. If it is possible, find the minimum possible time for Andrei to obtain a pile with exactly $k$ apples.
$^{\text{∗}}$$\lfloor \frac{x}{2} \rfloor$ — the largest integer $\le \frac{x}{2}$.
$^{\text{†}}$$\lceil \frac{x}{2} \rceil$ — the smallest integer $\ge \frac{x}{2}$.
> $1<=t<=10^4, 1<=n,k<=10^9$
## 提交详情
- 提交时间:16:34 状态:WA 详细状态:`Wrong answer on test 2`
> 直接模拟相除，如果是奇数，会有`n/2`和`n/2+1`两个答案；如果是偶数，仅有`n/2`一个答案  
模拟相除中，如果 $k$ 大于答案中的奇数，就不行  
然后再给 $n$ 取得奇数那个答案，否则另一个
- 提交时间:16:37 状态:WA 详细状态:`Wrong answer on test 2`
> 用二分的方法模拟操作，调整左边界的情况代表不行  
右边界直接调整为`mid`
- 提交时间:16:41 状态:WA 详细状态:`Wrong answer on test 2`
> 但其实应该取最小那个值，使得最快到达目标堆数量
- 提交时间:17:08 状态:AC 详细状态:`-`
> 应该走奇数答案的那一边，因为如果只取两答案的最小，奇数那个会包含偶数的分支，而偶数不会包含奇数的另一分支
取奇数会避免漏掉可能的答案  
然后修复边界判断问题，应该先判断是否相等(相等直接返回0,即不用分割)，然后再判断 $k$ 是否大于 $n/2+1$
- 提交时间:17:09 状态:AC 详细状态:`-`
> 对二分按上一提交优化，将右边界取为奇数的一边即可

# 2190A.Sorting Game
[Rating]1200
[Tag]构造、博弈论、贪心
[Time]思考+理解24:46，补题27:27
## 题目详情
Alice and Bob play a game on a binary string $s$ of length $n$ (a string consisting only of characters $\mathtt{0}$ and $\mathtt{1}$). Alice moves first, and the players take alternate turns.
In one turn, a player chooses a sequence of indices $i_1, i_2, \ldots, i_m$ ($1 \le i_1 \lt i_2 \lt \ldots \lt i_m \le n$) such that the characters at these positions form a **non-increasing** sequence (that is, $s_{i_1} \ge s_{i_2} \ge \ldots \ge s_{i_m}$). The player then rearranges the characters at these positions to be sorted in **non-decreasing** order.
Formally, let the chosen characters consist of $z$ zeros and $o$ ones (where $z + o = m$). The move replaces the characters at positions $i_1, i_2, \ldots, i_m$ with a sequence of $z$ zeros followed by $o$ ones. A move is valid if and only if it strictly **modifies** the string $s$ (which implies $z \ge 1$ and $o \ge 1$).
The player who cannot make a valid move loses.
Assuming both players play optimally, determine the winner. If Alice wins, output a valid first move that is part of a winning strategy.
> $1<=t<=10^4, 1<=n<=2\times 10^5, |s|=n, s_{i}\in {0,1}$
## 提交详情
- 提交时间:22:20 状态:WA 详细状态:`Wrong answer on test 2`
> 一个降序子段，必然先由若干个连续的1开头，后续必然借着若干个0  
那么先找到并记录所有连续1子段的开头，根据每一段的开头，其下一段开头的前者是否为0，判断当前是否为降序子段  
然后找到所有降序子段，如果降序子段数量为偶数倍,必然是`Bob`胜利(因为按序此时Alice无段可选),否则为`Alice`胜利  
这里不管选择哪一段，以最优来看，即让对方无段可选，那么就只能选一段降序序列，使得剩下的降序序列尽可能少
- 提交时间:22:43 状态:AC 详细状态:`-`
> 经**GPT**指导下，我发现我的方法完全错误的  
*错误1*如果是按最优的情况，即对于降序段数量非偶数倍，那么Bob就会拆其中一段，即只选该段中1个1和所有0，使得增加一次次数，让Bob在最后一次可选赢得比赛；对于Alice，也可以执行同样的套路，使得获胜。但这样就相互驳论了啊，只要存在至少一个1的数量大于1的，他两就能互相博弈啊。故不能手动模拟。  
设 $s$ 中`0`的个数为 $cnt$ ,那么`1`的个数为 $n-cnt$  
Alice和Bob所做，必然使得 $s$ 变为一个整体升序的序列  
即使得 $s$ 前 $cnt$ 个字符为`0`,后 $n-cnt$ 个字符必然为`1`  
*错误2*由于我忘记看他们选的是序列(非连续子数组)，即可以记录所有非法元素的位置(即`1`在前 $cnt$ 个字符中)  
Alice可以直接一步搞定，即选中所有非法元素的位置，一次性选择并排序他们  
*错误3*这里我还忘记看，他们的操作是排序而不是翻转  
Bob赢的条件，仅有 $s$ 初始就为升序序列，让Alice无法可走，否则Alice就会像上面所说的一样，一步结束比赛

# 2182C.Production of Snowmen
[Rating]1200
[Tag]暴力枚举、组合数学、动态规划
[Time]思考+尝试19:31，补题32:26
## 题目详情
To create a truly festive atmosphere, Santa's helpers have opened a factory for producing snowmen!
Each snowman consists of three snowballs — a head, a torso, and legs. For the snowman to be stable, the ball forming the legs must be strictly larger than the ball forming the torso; in turn, the ball forming the torso must be strictly larger than the ball forming the head. Formally, if we denote the sizes of the head, torso, and legs as $a, b, c$ respectively, the snowman will be stable if and only if $a \lt b \lt c$.
At the snowman production factory, there are three conveyors, each containing $n$ snowballs. The first conveyor contains balls of sizes $a_1, a_2, \dots, a_n$; the second contains balls of sizes $b_1, b_2, \dots, b_n$; the third contains balls of sizes $c_1, c_2, \dots, c_n$. The conveyors are cyclic; in other words, after the element numbered $n$, the element numbered $1$ follows again, and we can consider that the ball numbered $i$ and the ball numbered $i+n$ (as well as the ball numbered $i+2n$, $i+3n$, and so on) are the same ball.
To produce snowmen, it is necessary to specify three parameters $i, j, k$ ($1 \le i, j, k \le n$), indicating which balls on each conveyor the production starts from. After that, the snowmen will be assembled from the balls as follows:
-   the first snowman will have a head of size $a_i$, a torso of size $b_j$, and legs of size $c_k$;
-   the second snowman will have a head of size $a_{i+1}$, a torso of size $b_{j+1}$, and legs of size $c_{k+1}$;
-   and so on;
-   the $n$\-th (last) snowman will have a head of size $a_{i+n-1}$, a torso of size $b_{j+n-1}$, and legs of size $c_{k+n-1}$.
The parameters $i, j, k$ must be chosen in such a way that all $n$ snowmen are stable. Your task is to count the number of suitable combinations of parameters.
> $1<=t<=5000, 1<=n<=5000. 1<=a_{i},b_{i},c_{i}<=3*n$
## 思考
> 将 $a,b,c$ 均排序，然后先枚举 $a$ 中每一个元素  
以当前枚举的 $a$ 中元素，通过`upper_bound`去枚举 $b$ 中所有符合的元素  
在以当前枚举的 $b$ 中元素，通过`upper_bound`去计算 $c$ 中符合元素的个数  
然后计算所有符合元素个数的总数，即为答案  
可这题貌似要我找出所有在符合 $a_{i}<b_{j}<c_{k}$ 下 $(i,j,k)$ 的组合数，并确保当前组合下其它元素能按严格递增条件组合出 $n-1$ 组，思路不符，故放弃
## 提交详情
- 提交时间:23:50 状态:AC 详细状态:`-`
> 作者是固定身体起点为 $t=0$ ,然后去枚举头部有多少个 $i$ 符合条件，再去枚举腿部有多少个 $k$ 符合条件  
以复杂度 $O(2*n^2)$ 解决