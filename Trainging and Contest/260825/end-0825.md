1. 复盘
# P3379 【模板】最近公共祖先（LCA）
[Rating]普及
[Tag]最近公共祖先 LCA
[Time]默写代码11:49
## 题目详情
如题，给定一棵有根多叉树，请求出指定两个点之间最近的公共祖先。
*输入格式*
第一行包含三个正整数 $N,M,S$，分别表示树的结点个数、询问的个数和树根结点的序号。
接下来 $N-1$ 行每行包含两个正整数 $x, y$，表示 $x$ 结点和 $y$ 结点之间有一条直接连接的边（数据保证可以构成树）。
接下来 $M$ 行每行包含两个正整数 $a, b$，表示询问 $a$ 结点和 $b$ 结点的最近公共祖先。
*输出格式*
输出包含 $M$ 行，每行包含一个正整数，依次为每一个询问的结果。
## 提交详情
- 提交时间:15:38:09 状态:AC 详细状态:`-`
> 对于一个树`1 -> 2 -> 3 -> 4 -> 5 -> 6`  
初始给与每个节点设置其父节点`f[1][0] = 1, f[2][0] = 1, f[3][0] = 2, f[4][0] = 3, f[5][0] = 4, f[6][0] = 5`  
由于初始是`DFS`递归设置，数组`f[i][j]`二维`j`代表自点`i`往上第 $2^j$ 个祖先是谁  
从最后一个点`6`开始往前设置，如已经知道`f[6][0]=1`  
自然就可以知道跳 $2^1$ 步的祖先就是先跳到`6`的祖先`5`，`5`再跳到其祖先`4`就是从`6`跳 $2^1$ 步的结果  
那要知道自`6`跳 $2^2$ 步的祖先，那自然可以沿用`f[6][1]`的结果，即从`4`往上再跳两步即可，方法同上，得到`2`(`2 -> 3 -> 4`)  
往后的所有 $2^i$ 步的结果，皆是在上一步的结果`f[u][i-1]`上再往前跳两步`f[f[u][i-1]][i-1]`得到，公式为 $2^{i-1}*2=2^{i}$  
如`f[6][2]=2, f[2][2]=1`，则`f[6][3]=f[f[6][2]][2]=f[2][2]=1`  
然后在寻找公共祖先的过程中，优先将两者上升到同一层(起点相同)，即对较深的节点进行祖先递归，同层后再同步推进，直至两者节点相同  
> 默写代码失败，错误在于DFS递归时是递归他所有子节点，条件是递归的节点不是当前的父节点，不是这个子节点的父节点就是当前节点的父节点(这怎么可能实现，如果实现，就是有环图了)

2. 训练
# P3128 [USACO15DEC] Max Flow P
[Rating]普及+/提高−
[Tag]最近公共祖先 LCA 树链剖分 差分
[Time]思考+学习30:14，LCA代码默写03:18，抄写余下代码07:35
## 题目详情
Farmer John 在他的谷仓中安装了 $N-1$ 条管道，用于在 $N$ 个牛棚之间运输牛奶（$2 \leq N \leq 50,000$），牛棚方便地编号为 $1 \ldots N$。每条管道连接一对牛棚，所有牛棚通过这些管道相互连接。
FJ 正在 $K$ 对牛棚之间泵送牛奶（$1 \leq K \leq 100,000$）。对于第 $i$ 对牛棚，你被告知两个牛棚 $s_i$ 和 $t_i$，这是牛奶以单位速率泵送的路径的端点。FJ 担心某些牛棚可能会因为过多的牛奶通过它们而不堪重负，因为一个牛棚可能会作为许多泵送路径的中转站。请帮助他确定通过任何一个牛棚的最大牛奶量。如果牛奶沿着从 $s_i$ 到 $t_i$ 的路径泵送，那么它将被计入端点牛棚 $s_i$ 和 $t_i$，以及它们之间路径上的所有牛棚。
*输入格式*
输入的第一行包含 $N$ 和 $K$。
接下来的 $N-1$ 行每行包含两个整数 $x$ 和 $y$（$x \ne y$），描述连接牛棚 $x$ 和 $y$ 的管道。
接下来的 $K$ 行每行包含两个整数 $s$ 和 $t$，描述牛奶泵送路径的端点牛棚。
*输出格式*
输出一个整数，表示通过谷仓中任何一个牛棚的最大牛奶量。
*说明/提示*
$2 \le N \le 5 \times 10^4,1 \le K \le 10^5$。
## 提交详情
- 提交时间:17:14:44 状态:AC 详细状态:`-`
> 从 $s_i$ 到 $t_i$ ，路径上每个牛棚都要增加一单位牛奶  
问运送 $K$ 对后，最大牛奶量的牛棚有多少牛奶  
> 提前预知题目Tag: LCA、树上差分  
手写模拟下来就是差分，但是要找到 $s_i$ 到 $t_i$ 的路径，通过这个路径区间进行差分  
要对区间进行加减，要么线段树、BIT，要么就是差分，目前最好方法都是差分
但上述三个操作，都需要将这个图拍扁成一维数组，并且拍扁一维后，对应区间正好是两点的路径经过的所有点  
不能以点编号进行拍扁，因为子父节点没有编号上的关联性  
> 引入*GPT*和*Gemini*对树上差分进行学习  
普通差分是对区间左端点加，右端点减，最后用前缀和的方法获得最终结果  
树上差分本质上是对路径起始点和终点的所在区间按普通差分的操作进行  
> 为什么本题给与的图一定是树: 对于有 $N$ 节点的无向图，有 $N-1$ 条边一定是树  
但由于两点的路径必然要经过两点的最近公共祖先(父节点)，故可以将差分操作换成树上的操作  
即把两点看作是左端点，把公共父节点(LCA)看做是右端点  
然后还要对LCA的父节点也要做一次差分减，因为自底向上汇总时，会使得其子树的值传给父节点(多余了)  
这样才能做到差分一样的汇总方向，即树的汇总方向是自下而上  
最后汇总时是对所有节点往前推进  
> 花费03:18，默写LCA代码成功  
> 花费07:35，抄写*GPT*代码成功  
> 这里*GPT*用栈来实现DFS，因为DFS本质就是递归先处理，然后再处理其子节点，用栈来实现也是如此  
然后最后汇总时是自底向上，即从所有叶子节点向上，而这样就需要先行记录DFS序  
即从根节点逐步往后枚举推进，实现这颗树的先序遍历  
由于DFS先天然满足`父节点在前，子节点在后`的次序，因而最后倒序必然也能做到自底向上的操作(先子节点再父节点)  
- 提交时间:17:29:50 状态:AC 详细状态:`-`
> 按*GPT*的方法用栈模拟了LCA的DFS初始化，同时存储了树的先序遍历(父前子后)  
这里用栈模拟DFS，是因为避免树链过大导致爆栈  
DFS递归用的是系统调用栈，系统调用栈空间天生较小，及其容易爆栈

# P2052 [NOI2011] 道路修建
[Rating]普及
[Tag]树形数据结构 广度优先搜索 BFS 深度优先搜索 DFS
[Time]思考+尝试13:05
## 题目详情
在 W 星球上有 $n$ 个国家。为了各自国家的经济发展，他们决定在各个国家之间建设双向道路使得国家之间连通。但是每个国家的国王都很吝啬，他们只愿意修建恰好 $n - 1$ 条双向道路。
每条道路的修建都要付出一定的费用，这个费用等于道路长度乘以道路两端的国家个数之差的绝对值。例如，在下图中，虚线所示道路两端分别有 $2$ 个、$4$ 个国家，如果该道路长度为 $1$，则费用为 $1×|2 - 4|=2$。图中圆圈里的数字表示国家的编号。
由于国家的数量十分庞大，道路的建造方案有很多种，同时每种方案的修建费用难以用人工计算，国王们决定找人设计一个软件，对于给定的建造方案，计算出所需要的费用。请你帮助国王们设计一个这样的软件。
*输入格式*
输入的第一行包含一个整数 $n$，表示 W 星球上的国家的数量，国家从 $1$ 到 $n$ 编号。
接下来 $n - 1$ 行描述道路建设情况，其中第 $i$ 行包含三个整数 $a_i$，$b_i$ 和 $c_i$，表示第 $i$ 条双向道路修建在 $a_i$ 与 $b_i$ 两个国家之间，长度为 $c_i$。
*输出格式*
输出一个整数，表示修建所有道路所需要的总费用。
*说明/提示*
对于 $100\%$ 的数据，$1\leq a_i, b_i\leq n$，$0\leq c_i\leq10^6$，$2\leq n\leq 10^6$。
|测试点编号|$n=$|
|:-:|:-:|
|$1$|$2$|
|$2$|$10$|
|$3$|$100$|
|$4$|$200$|
|$5$|$500$|
|$6$|$600$|
|$7$|$800$|
|$8$|$1000$|
|$9$|$10^4$|
|$10$|$2\times 10^4$|
|$11$|$5\times 10^4$|
|$12$|$6\times 10^4$|
|$13$|$8\times 10^4$|
|$14$|$10^5$|
|$15$|$6\times 10^5$|
|$16$|$7\times 10^5$|
|$17$|$8\times 10^5$|
|$18$|$9\times 10^5$|
|$19,20$|$10^6$|
## 提交详情
- 提交时间:18:57:32 状态:WA 详细状态:`AC*1 WA*19`
> 由题目可知，一条路的对费用的贡献为，这条路的价格乘以分割左右的国家(点)的差值  
设一条路连接 $(u,v)$ ，且 $u$ 为 $v$ 的父节点，设此路权值为 $w$ ，已通过递归得知以 $v$ 作为根节点的子树大小 $b$  
那么这条路的实际费用为 $w\times |(n - b) - b|=w\times |n - 2 * b|$ ，最后计算总和即可  
- 提交时间:19:03:44 状态:AC 详细状态:`-`
> 可能出现 $2b\ge n$ ，那么此时这个值就不对了  
出现这种情况，只能是一点为大树的子节点，那么此时就要反过来，真正计算的子树大小应该是另一半反过来的大小，即 $n-b$  
由于这样判断过于麻烦，直接设 $b=min(b, n-b)$ 即可

# 力扣3718. 缺失的最小倍数
[Rating]简单
[Tag]-
[Time]理解+尝试20:45
## 题目详情
给你一个整数数组 nums 和一个整数 k，请返回从 nums 中缺失的、最小的正整数 k 的倍数。
倍数 指能被 k 整除的任意正整数。
提示：
1 <= nums.length <= 100
1 <= nums[i] <= 100
1 <= k <= 100
## 提交详情
- 提交时间:21:42 状态:WA 详细状态:`-`
> 先找到所有 $k$ 的倍数，然后将其存到数组中，并排序和去重  
然后用二分搜索，根据区间值大于区间数量决定转向哪一侧  
- 提交时间:21:45 状态:AC 详细状态:`0ms 23.64MB`
> 但由于大小不超过 $100$ ，我直接逐个枚举数组中两相邻是否超过`1`即可
- 提交时间:21:46 状态:AC 详细状态:`0ms 23.7MB`
> 我应该调整我的二分方法，应先向左转移，因为题目要的最小的，而非唯一缺失的  
- 提交时间:23:25 状态:AC 详细状态:`-`
> 先对给与的数组进行排序和去重，然后我用数组`a`来存储枚举到每一个倍数  
由于这里我去重了，故拿取到的倍数必然唯一  
我再枚举给与数组中每一个可能的倍数，如果第一个拿到的倍数大于1，就返回1即可  
将所有枚举到的倍数放入到`a`中，确保`a`中是自当前元素前的所有倍数，且升序  
之后如果枚举到一个倍数与`a`最后一个倍数的间距大于1，直接返回`a`最后一个倍数即可  
因为`a`最后一个倍数代表自当前元素往前的最近、最大倍数，代表存在题目所要求的倍数  
如果枚举完，`a`为空，就返回1，代表给与数组中没有对应数的倍数  
否则就返回`a`最后一个倍数  
注意，上述倍数返回均需要乘以 $k$  

# P1253 扶苏的问题
[Rating]普及+/提高−
[Tag]线段树
[Time]理解+尝试+修改42:46
## 题目详情
给定一个长度为 $n$ 的序列 $a$，要求支持如下三个操作：
1. 给定区间 $[l, r]$，将区间内每个数都修改为 $x$。
2. 给定区间 $[l, r]$，将区间内每个数都加上 $x$。
3. 给定区间 $[l, r]$，求区间内的最大值。
*输入格式*
第一行是两个整数，依次表示序列的长度 $n$ 和操作的个数 $q$。  
第二行有 $n$ 个整数，第 $i$ 个整数表示序列中的第 $i$ 个数 $a_i$。  
接下来 $q$ 行，每行表示一个操作。每行首先有一个整数 $op$，表示操作的类型。
- 若 $op = 1$，则接下来有三个整数 $l, r, x$，表示将区间 $[l, r]$ 内的每个数都修改为 $x$。
- 若 $op = 2$，则接下来有三个整数 $l, r, x$，表示将区间 $[l, r]$ 内的每个数都加上 $x$。
- 若 $op = 3$，则接下来有两个整数 $l, r$，表示查询区间 $[l, r]$ 内的最大值。
*输出格式*
对于每个 $op = 3$ 的操作，输出一行一个整数表示答案。
*说明/提示*
*数据规模与约定*
- 对于 $10\%$ 的数据，$n = q = 1$。
- 对于 $40\%$ 的数据，$n, q \leq 10^3$。
- 对于 $50\%$ 的数据，$0 \leq a_i, x \leq 10^4$。
- 对于 $60\%$ 的数据，$op \neq 1$。
- 对于 $90\%$ 的数据，$n, q \leq 10^5$。
- 对于 $100\%$ 的数据，$1 \leq n, q \leq 10^6$，$1 \leq l, r \leq n$，$op \in \{1, 2, 3\}$，$|a_i|, |x| \leq 10^9$。
*提示*
请注意大量数据读入对程序效率造成的影响。
## 提交详情
- 提交时间:23:05:04 状态:AC 详细状态:`-`
> 花费15:54，默写出线段树的代码，含覆盖与区间修改的懒操作  
> 经*Gemma4*指导，我的问题出在如下方面:
> 1. 线段树数组、懒标记数组大小开反了，是`n << 2`不是`n >> 2`，且加减比位运算运算级别更高，直接使用`n * 4 + 1`即可  
> 2. 区间覆盖`lazy_fix`对树应该是直接换成 $k$ ，即`tree[p]=k`，同时要清空加法的懒标记`lazy_add[p]=0`  
> 3. 查询时忘记更新懒标记，更新递归时应该不是分支，而是符合条件两边都要走  
> 4. 不能按之前加分懒标记一样加入区间份的`k`，而是直接给`tree[p]`加上`k`即可，因为`tree[p]`就代表区间最大值  
> 5. 对于覆盖要加入额外数组进行标记，因为 $|x| \leq 10^9$ ，不能单单靠`lazy_fix[p]`是否为零进行判断

3. 复盘
# E.Odd Cycle
[History]26-08-22
[Time]学习+理解47:40，默写07:48
## 题目详情
You are given a simple connected undirected graph with $N$ vertices numbered $1$ through $N$ and $M$ edges. The $i$\-th edge connects vertices $a_i$ and $b_i$.
Determine whether there exists a cycle consisting of an odd number of vertices, and if one exists, find one such cycle.
Formally, determine whether there exists an integer sequence $(v_1,v_2,\ldots,v_K)$ satisfying all of the following conditions, and if one exists, find one such sequence.
-   $K$ is an odd number at least $3$.
-   $v_1,v_2,\ldots,v_K$ are all distinct.
-   For every integer $i$ with $1\le i \le K$, there is an edge between vertices $v_i$ and $v_{i+1}$, where $v_{K+1} = v_1$.
You are given $T$ test cases; solve each of them.
*Constraints*
-   $1 \le T \le 2\times10^5$
-   $1 \le N, M \le 2\times10^5$
-   The sum of $N$ over all test cases is at most $2\times10^5$.
-   The sum of $M$ over all test cases is at most $2\times10^5$.
-   $1 \le a_i,b_i \le N$
-   $a_i\ne b_i$
-   The given graph is a simple connected undirected graph.
-   All input values are integers.
## 提交详情
- 提交时间:(08/26)00:44:26 状态:AC 详细状态:`-`
> 经*Gemma-4*指导，本题关键是*二分染色法*，我的理解就是接连染色法  
二分图染色可以找到奇数环，是因为二分染色的接连染色机制，即奇偶步染的颜色不相同  
如果存在奇数环的话，其中必定存在相邻节点颜色相同  
本题是先对所有点进行染色（枚举所有点确保所有连通分量都能被染色），然后寻找所有边中是否存在颜色相同的边  
如果存在，就代表题目存在奇数环  
再根据这个边的两点，寻找这两点的LCA，因为环存在、且该边为环的一边，那么边两点的LCA必定存在于环中  
那么只要让这个两点分别找到通向LCA的路径，就能找到这个环的完整路径了  
染色时也在做BFS生成树，使得LCA能够存在  
> 花费07:48，代码默写成功