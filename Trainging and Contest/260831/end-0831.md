1. 复盘
# P3379 【模板】最近公共祖先（LCA）
[Rating]普及
[Tag]最近公共祖先 LCA
[Time]默写代码18:39
## 题目详情
**历史做题如0823、0825、0826**  
如题，给定一棵有根多叉树，请求出指定两个点之间最近的公共祖先。
*输入格式*
第一行包含三个正整数 $N,M,S$，分别表示树的结点个数、询问的个数和树根结点的序号。
接下来 $N-1$ 行每行包含两个正整数 $x, y$，表示 $x$ 结点和 $y$ 结点之间有一条直接连接的边（数据保证可以构成树）。
接下来 $M$ 行每行包含两个正整数 $a, b$，表示询问 $a$ 结点和 $b$ 结点的最近公共祖先。
*输出格式*
输出包含 $M$ 行，每行包含一个正整数，依次为每一个询问的结果。
## 提交详情
- 提交时间:17:44:57 状态:AC 详细状态:`-`
> 初始版本代码默写失败:
> 1. 是倒序自 $LOGN-1$ 到1，不是正序的反过来  
> 2. 寻找LCA时是根据祖先本体判断，不是根据祖先深度判断  
然后改写BFS也失败了，失败之处在于给根节点`s`设置的根节点为1，而并非他自己

# P3128 [USACO15DEC] Max Flow P
[Rating]普及+/提高−
[Tag]最近公共祖先 LCA 树链剖分 差分
[Time]复盘13:48
## 题目详情
Farmer John 在他的谷仓中安装了 $N-1$ 条管道，用于在 $N$ 个牛棚之间运输牛奶（$2 \leq N \leq 50,000$），牛棚方便地编号为 $1 \ldots N$。每条管道连接一对牛棚，所有牛棚通过这些管道相互连接。
FJ 正在 $K$ 对牛棚之间泵送牛奶（$1 \leq K \leq 100,000$）。对于第 $i$ 对牛棚，你被告知两个牛棚 $s_i$ 和 $t_i$，这是牛奶以单位速率泵送的路径的端点。FJ 担心某些牛棚可能会因为过多的牛奶通过它们而不堪重负，因为一个牛棚可能会作为许多泵送路径的中转站。请帮助他确定通过任何一个牛棚的最大牛奶量。如果牛奶沿着从 $s_i$ 到 $t_i$ 的路径泵送，那么它将被计入端点牛棚 $s_i$ 和 $t_i$，以及它们之间路径上的所有牛棚。
*输入格式*
输入的第一行包含 $N$ 和 $K$。
接下来的 $N-1$ 行每行包含两个整数 $x$ 和 $y$（$x \ne y$），描述连接牛棚 $x$ 和 $y$ 的管道。
接下来的 $K$ 行每行包含两个整数 $s$ 和 $t$，描述牛奶泵送路径的端点牛棚。
*输出格式*
输出一个整数，表示通过谷仓中任何一个牛棚的最大牛奶量。
*说明/提示*
$2 \le N \le 5 \times 10^4,1 \le K \le 10^5$。
## 提交详情
- 提交时间:18:02:06 状态:AC 详细状态:`-`
> 我记得是两个节点加，然后他两的LCA以及LCA的父节点减，也记得用DFS来获取树的DFS序  
看之前的代码，才知道还原是DFS序倒序，节点值加回其根节点的值

# E.Odd Cycle
[Time]复盘10:20
## 题目详情
**ABC472-E，历史做题参考0822、0825、0826**  
You are given a simple connected undirected graph with $N$ vertices numbered $1$ through $N$ and $M$ edges. The $i$\-th edge connects vertices $a_i$ and $b_i$.
Determine whether there exists a cycle consisting of an odd number of vertices, and if one exists, find one such cycle.
Formally, determine whether there exists an integer sequence $(v_1,v_2,\ldots,v_K)$ satisfying all of the following conditions, and if one exists, find one such sequence.
-   $K$ is an odd number at least $3$.
-   $v_1,v_2,\ldots,v_K$ are all distinct.
-   For every integer $i$ with $1\le i \le K$, there is an edge between vertices $v_i$ and $v_{i+1}$, where $v_{K+1} = v_1$.
You are given $T$ test cases; solve each of them.
*Constraints*
-   $1 \le T \le 2\times10^5$
-   $1 \le N, M \le 2\times10^5$
-   The sum of $N$ over all test cases is at most $2\times10^5$.
-   The sum of $M$ over all test cases is at most $2\times10^5$.
-   $1 \le a_i,b_i \le N$
-   $a_i\ne b_i$
-   The given graph is a simple connected undirected graph.
-   All input values are integers.
## 提交详情
- 提交时间:22:31:15 状态:AC 详细状态:`-`
> 本题就是先二分染色，找到相同颜色的边，因为二分是两步染色，若存在奇数点必然会有颜色冲突  
然后要找到这个环的路径，必然是从这个边的两侧出发，然后往同一个点走  
这个点就是这边的两点LCA，故还要手动做树，寻找这个点  
重现代码成功

2. 训练
# E.K-Divisible Subarrays
[Time]理解+尝试31:18，点题+优化09:29，补题15:32
## 题目详情
**源于ABC473**  
You are given a sequence of length $N$ consisting of non-negative integers: $A=(A _ 1,A _ 2,\ldots,A _ N)$ .
Define the **score** of a sequence of length $1$ or more consisting of sequences of non-negative integers, $S=(S _ 1,S _ 2,\ldots,S _ k)$, as the number of sequences $S _ i\ (1\le i\le k)$ whose sum of elements is divisible by $K$.
Find the maximum possible score of a sequence of non-negative integer sequences obtained by dividing $A$ into one or more contiguous subsequences. Here, dividing a sequence $A$ of length $N$ into one or more contiguous subsequences means choosing a sequence of integers $l=(l _ 1,l _ 2,\ldots,l _ k)\ (1=l _ 1\lt l _ 2\lt\cdots\lt l _ k\le N)$ of length $1$ or more, and forming the following $k$ sequences.
-   $(A _ {l _ i},A _ {l _ i+1},\ldots,A _ {l _ {i+1}-1})\ (1\le i\le k)$ (Here, let $l _ {k+1}=N+1$.)  
-   $1\le N\le2\times10 ^ 5$
-   $1\le K\le10 ^ 9$
-   $0\le A _ i\lt K$
-   All input values are integers.
## 提交详情
- 提交时间:23:32:30 状态:TLE 详细状态:`AC*6 TLE*44`
> 由*GLM-5.3*帮我打了预防针，整除/倍数可以用余数  
那么用DFS看看  
我先根据`a`中所有元素做了余数的前缀和  
然后寻找每个起点 $i(1\le i\le n)$ 可以构成的所有连续序列，即总和满足`k`倍数的区间  
对每个区间做图论建模，然后问题就转变成，在 $[1,n]$ 的范围内最多不重叠的区间量  
DFS内对每个起点`s`往后枚举所有点`i`，枚举`i`可达的所有区间，然后递归区间下一位置 $r+1$ 的数量  
记忆化处理，对每个起点`s`保存区间数量  
> 请教*GPT*:是否有快速的方法，寻找范围内不重叠的区间数量呢？
- 提交时间:23:45:49 状态:TLE 详细状态:`AC*10 TLE*40`
> *GPT*给与的方法是对所有区间的右端点进行升序排序，然后逐个拿取  
根据*GPT*给与的方法，我进行了代码构建  
- 提交时间:23:51:32 状态:TLE 详细状态:`AC*22 TLE*28`
> 我进行了两项优化:  
> 1. 每个起点`i`只要枚举一个区间即可，因为要使得整个范围内区间数量足够多，那就使得区间大小尽可能小即可  
> 2. 枚举`k`的倍数`j`设置终点为`(b[n] - b[i - 1]) / k`  
- 提交时间:00:12:02 状态:AC 详细状态:`-`
> 正确题解是根据相同余数进行计算，即两个位置的余数相同，必定代表这两个位置构成的区间和是`k`的倍数  
对于当前余数能得到的得分，必然是上一余数的得分加1，这里的1代表这个两个余数构成的区间  
定义`dp[r]`为余数`r`下的得分，递推公式为`dp[r]=dp[last_r]+1`

# P1803 凌乱的yyy / 线段覆盖
[Rating]普及-
[Tag]模拟、贪心、排序
[Time]复盘02:23
## 题目详情
**历史做题250811**
*题目描述*
现在各大 oj 上有 $n$ 个比赛，每个比赛的开始、结束的时间点是知道的。
yyy 认为，参加越多的比赛，noip 就能考的越好（假的）。
所以，他想知道他最多能参加几个比赛。
由于 yyy 是蒟蒻，如果要参加一个比赛必须善始善终，而且不能同时参加 $2$ 个及以上的比赛。
*输入格式*
第一行是一个整数 $n$，接下来 $n$ 行每行是 $2$ 个整数 $a_{i},b_{i}\ (a_{i}<b_{i})$，表示比赛开始、结束的时间。
*输出格式*
一个整数最多参加的比赛数目。
*说明/提示*
- 对于 $20\%$ 的数据，$n \le 10$；
- 对于 $50\%$ 的数据，$n \le 10^3$；
- 对于 $70\%$ 的数据，$n \le 10^{5}$；
- 对于 $100\%$ 的数据，$1\le n \le 10^{6}$，$0 \le a_{i} < b_{i} \le 10^6$。
## 提交详情
- 提交时间:00:22:25 状态:AC 详细状态:`-`
> 由于上题*不重叠区间*想到去年做过的这题，温习一下