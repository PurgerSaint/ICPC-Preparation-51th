#include<bits/stdc++.h>
using namespace std;
#define ios ios::sync_with_stdio(false);cin.tie(0);
int main() {
    ios;
    /*
    int n; cin >> n;
    for (int i = n; i >= 1; i--)
        cout << i << (i != 1 ? "," : "");
        */
       cout << (1000000000000000000 - 1000000000000000000);
    return 0;
}