1. 复盘
# P1629 邮递员送信
> 花费05:54，代码默写成功。题目是正向Dijkstar+反向Dijkstar共同使用的。  
其精妙点在于，正向恰好为邮递员从邮局到每个点的距离，但反向需要每个点到邮局的最短距离  
然后不可能用多源点去求每个点到邮局的最短距(会超时)  
恰好每个点到邮局的最短距，恰好就是反向后邮局到每个点的最短距

# P1346 [CHCI 2002 Regional Competition Juniors] 电车
> 花费03:18，代码默写成功。题目关键点是不用记录保存开关开启后切换的具体道口  
默认每个路口的第一个轨道不用切换，其余全要通过1次开启开关进行切换  
由于本题是0/1权重，可以使用0-1BFS，我用02:22写出了0-1BFS的代码并通过

# P4554 小明的游戏
> 花费09:53，代码默写成功，我将这题用Dijkstar和0-1BFS再次实现了  
前往同类型点不耗费代价，否则耗费1代价，方向为四周  
然后按0-1BFS或Dijkstar从起点开始遍历，直到遇到终点即可

# P4667 电路维修
> 花费22:09，代码重写失败，主要原因在于我忘记图是双向的，只根据对应电路交点建立了单向图  
本题关键点在于以电路交点建立图，必须要二维降一维来建图

# P1332 血色先锋队
> 花费06:44，代码默写成功，这题多源点Dijkstar关键是将所有感染者的点放到优先队列中  
最后枚举所有领主的最短距离即可

# P1828 香甜的黄油
> 花费05:04，代码默写成功，这题本质上是求得一个牧场i，使得所有奶牛到该牧场的最短距总和最小  
故枚举所有牧场，对每个牧场用Dijkstar求最短距，计算当前牧场最短距总和  
然后找到总和最小的即可

# P1825 Corn Maze S
> 花费22:24，代码默写成功，题目是魔改Dijkstar   
关键点有两个，一个是到达传送门时，如果能传就必须传
另一个是从起点起步时，就带一次步行记录（毕竟不可能原地传送）  

2. 复盘总结
# 反向Dijkstar
> 关键识别点:从终点到每个点
# 0-1BFS
> 关键点是`边权仅为0和1`，否则无法使用
# 多源点Dijkstar
> 他分有两类，第一类是将所有源点加入到队列中，第二类是对所有源点求最短距  
对于第一类，本质上是求别的点到所有源点的最小距离，例子就是P1332  
对于第二类，本质上是求某点到所有源点的总距离最小，例子就是P1828

1. Div.2盲训
# 2248A.You Delete, I Delete
[Tag]贪心
[Time]思考+理解07:33
## 题目详情
Alice and Bob are given a binary string$∗$$^{\text{∗}}$ $s$$s$ of length $n$$n$. It contains at least one $0$$\mathtt{0}$ and at least one $1$$\mathtt{1}$.
They each perform exactly **one** operation in the following order:
-   First, Alice chooses an occurrence of $0$$\mathtt{0}$ in $s$$s$ and deletes it.
-   Then, Bob chooses an occurrence of $1$$\mathtt{1}$ in the resulting string and deletes it.
Alice wants the final string to be lexicographically$†$$^{\text{†}}$ as large as possible, while Bob wants it to be lexicographically as small as possible. Determine the final string if both players act optimally.
$∗$$^{\text{∗}}$A binary string is a string consisting only of the characters $0$$\mathtt{0}$ and $1$$\mathtt{1}$.
$†$$^{\text{†}}$For two distinct binary strings $a$$a$ and $b$$b$ of the same length, $a$$a$ is lexicographically smaller than $b$$b$ if, at the first position where they differ, $a$$a$ has the smaller digit.
**Input**
Each test contains multiple test cases. The first line contains the number of test cases $t$$t$ ($$). The description of the test cases follows.
The only line of each test case contains a binary string $$ of length $$ ($$).
It is guaranteed that $$ contains at least one $$ and at least one $$.
## 提交详情
- 提交时间:18:18 状态:AC 详细状态:`-`
> 既然Alice要使得字典序更大，Bob要使得字典序更小  
那Alice就删除第一个出现的0就行了，使得更多的1出现在左侧  
那Bob就删除第一个出现的1就行了，使得更多的0出现在左侧  
那么最终字符串就删除第一个出现的1和0就行

# 2248B.Merge to Match=
[Tag]贪心、排序
[Time]思考+尝试58:40，补题18:58
## 题目详情
You are given two arrays $a$ and $b$ of lengths $n$ and $m$, respectively. All $n + m$ integers in these arrays are **distinct**.
You can perform the following operation on $a$ any number of times (possibly, zero):
-   Choose two elements of $a$ with values $x$ and $y$, where $x \le y$.
-   Delete these two elements from $a$.
-   Insert one integer $z$ into $a$ such that $x \le z \le y$.
After performing all operations, you may arrange the elements of $a$ in any order.
Determine whether it is possible to make $a$ equal to $b$.
**Input**
Each test contains multiple test cases. The first line contains the number of test cases $t$ ($1 \le t \le 10^4$). The description of the test cases follows.
The first line of each test case contains two integers $n$ and $m$ ($1 \le n, m \le 2 \cdot 10^5$) — the lengths of the arrays $a$ and $b$.
The second line contains $n$ integers $a_1, a_2, \ldots, a_n$ ($1 \le a_i \le 10^9$).
The third line contains $m$ integers $b_1, b_2, \ldots, b_m$ ($1 \le b_i \le 10^9$).
It is guaranteed that all $n + m$ integers in the arrays $a$ and $b$ are **distinct**.
It is guaranteed that the sum of $n$ over all test cases does not exceed $2 \cdot 10^5$.
It is guaranteed that the sum of $m$ over all test cases does not exceed $2 \cdot 10^5$.
## 提交详情
- 提交时间:18:48 状态:WA 详细状态:`Wrong answer on test 2`
> 先特例特判，`a`的大小小于或等于`b`的大小、`b`中存在小于`a`最小值或大于`a`最大值的数，就都不行  
然后对每个`b`里的数 $i$ ，寻找每个`a`中第一个大于或等于 $i$ 的数 $j$ ，使得 $j$ 和一个更大或更小的数进行配对  
计数`same`为`a`中与`b`相同的数，`match`为匹配到的数  
然后计算`a`中剩余量为 $n-same-match$ ，判断剩余量大于或等于`match`即可  
- 提交时间:19:46 状态:WA 详细状态:`Wrong answer on test 2`
> 第一次提交可能遗漏在我都是找大的，有可能大的没有足够小的来适配  
现在我直接寻找可以满足`b`的上下限，即对`a`和`b`排序后，寻找 $b_{0}$ 在`a`中第一个大于或等于的位置  
然后从这个位置到`a`的最后面，就是`b`可以做到的区间，设这个区间长度为`len`  
再寻找一遍`b`中与`a`相同元素的数量，设这个数量为`same`  
那么就可以得到`a`中剩余数`less`为 $n-len$  
如果 $len>m$ ，那么 $less+=(len-m),len=m$ ，然后`len`减去`same`的  
最后直接判断`len`加`same`是否等于 $m$ ，且`less`是否大于或等于`len`即可
- 提交时间:21:20 状态:AC 详细状态:`-`
> 作者的方法非常简单  
对于`b`中第 $i$ 个元素 $b_{i}$ ，要使得`a`中元素能合并到 $b_{i}$ ，必然在`a`中存在 $i+1$ 个元素先合并到 $b_{0},b_{1},\dots,b_{i-1}$ 这 $i$ 个元素，则使得必须满足 $a_{i}<b_{i}$  
而对于 $b_{i}$ 后 $m-i$ 个元素 $b_{i+1},b_{i+2},\dots,b_{m-1}$ ，这些数要成立，也必然要在`a`中有 $m-i$ 个数供他们匹配，即必须要使得`a`中 $m-i$ 个元素大于 $b_{i}$ ，因此必须满足 $b_{i}<a_{n-(m-i)}$  
以上两个条件，加上`a`的大小必须至少为`b`大小的两倍，故能使得`a`推出`b`