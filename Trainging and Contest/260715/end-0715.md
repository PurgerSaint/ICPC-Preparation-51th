# 1109 Div.3 比赛补题:
> 比赛名称: Codeforces Round 1109 (Div. 3)  
> 比赛结果: [Rank]12008 [Solved]2 [Rating-Change]+77 [New-Rating]973 
# A.Iskander and Drawings
[Tag]动态规划、博弈论、贪心、字符串处理
## 题目介绍
During a geometry lesson, Iskander got very bored, so he decided to draw in Yura's notebook. To do this, he took a row and drew horizontal lines on it. Some lines are long, some are short, and some parts of the page remain empty.
The page is represented by a string $s$, where the character '\*' denotes an empty part of the paper, and the character '#' denotes one centimeter of a drawn line. A continuous sequence of '#' characters forms a single line.
Yura decided to erase all the lines and made Iskander help him: they will erase one of the lines from both ends simultaneously.
-   Each second, Iskander erases $1$ centimeter from the right end of the line, and Yura erases $1$ centimeter from the left end.
-   If the current length of the line is $1$ or $2$ centimeters, then in the next second it is erased completely, and the process ends.
Yura wants to choose a line so that, together with Iskander, they erase it for as long as possible. Help him determine this maximum time. If there are no lines on the page, the answer is $0$ seconds.
> $1<t<=2500, 1<=n<=10$
## 提交详情
- 提交时间:(07/14)22:39 状态:AC 详细状态:`-`
> 使得擦除时间最长，那么就选择线最长的即可  
找到字符串中线最长的，注意对余数1的处理

# B. Nikita and Books
[Tag]贪心、排序、数学
## 题目介绍
As is well known, Nikita loves reading. Today, he made a mess in his room and arranged his books into $n$ stacks in a row, numbered from $1$ to $n$ from left to right. The $i$\-th stack contains $a_i$ books. This arrangement is called neat if, in every stack except the rightmost one, the number of books is strictly less than in the stack to its right; that is, the array $a$ is strictly increasing.
Yura wants to make the arrangement neat by performing the following operation any number of times:
1.  Choose a stack $i$ such that $1 \le i \lt n$ and $a_i \gt 1$.
2.  Take $1$ book from the top of stack $i$, so $a_i$ decreases by $1$.
3.  Put this book on top of stack $i + 1$, so $a_{i+1}$ increases by $1$.
Determine whether Yura can make the arrangement neat.
> $1<=t<=10^4, 1<=n<=2\times 10^5, 1<=a_{i}<=10^9$
## 提交详情
- 提交时间:(07/14)23:04 状态:AC 详细状态:`-`
> 一堆书的数量为 $x$ ,当 $x>1$ ,可以往后移动 $x-1$ 本  
那么就使得 $[1,n-1]$ 的每堆都尽量为 $i$  
即使得第一堆仅剩一本，后续每堆都严格比前面一堆多一本  
然后把剩余的都堆到最后一堆  
那么移动时就会有移动量`mv`,如果当前堆无法堆到前一堆多一本(使得严格大于前一堆),就从移动量中拿取一部分使得将其填满,否则就根本构不成  
如果比目标(前一堆多一本)还多,剩余量直接丢移动量中  
本题是问是否存在,而不要求特定序列,因此只要保证从第二堆开始,能通过移动量构造的情况下严格大于前一堆的就行  

# C. Stepan and Permutation
[Tag]构造，并查集，贪心，数学，数论，排序
## 题目介绍
Stepan found a permutation $p$ of length $n$. Of course, he decided to sort it. To make the process more interesting, he chose two positive integers $x$ and $y$ $(x + y \le n)$ and defined a rule for swapping elements.
In one move, Stepan can choose two indices $i$ and $j$ $(1 \le i, j \le n)$ and swap the elements $p_i$ and $p_j$ if **at least one** of the following conditions holds:
-   $|i - j| = x$
-   $|i - j| = y$
Stepan wants to know whether it is possible to sort the permutation in ascending order using any number of such operations. Help him answer this question.
> $1<=t<=10^4, 1<=x,y<=n<=2\times 10^5, 1<=p_{i}<=n$
## 提交详情
- 提交时间:(07/14)23:58 状态:WA 详细状态:`Wrong answer on test 4`
> 排列中可以让 $p_{i}$ 元素与 $p_{i}+x,p_{i}+y,p_{i}-y,p_{i}-x$ 进行交换  
如果要手动模拟，必然出现更优方案无法找出的结果  
既然是排列，且与其他元素进行互换，那么最终就只能排列构成 $[1,n]$ 的序列  
那么对于每个元素，判断其与目标元素的差值，例如第 $i$ 个元素 $p_{i}$ 与 $i$ 的差值  
如果该差值是 $x,y,|x-y|$ 的差值，必然成立
- 提交时间:(07/15)00:04 状态:WA 详细状态:`Wrong answer on test 4`
> 添加对序列长度为1的特殊判断，结果必然为真
- 提交时间:(07/15)00:26 状态:WA 详细状态:`Wrong answer on test 4`
> 添加对元素超出范围的判断，使得 $p_{i}+x,p_{i}+y,p_{i}-y,p_{i}-x$ 不超出 $[1,n]$
- 提交时间:(07/15)00:28 状态:WA 详细状态:`Wrong answer on test 4`
> 上一个对超出范围的判断，对象选择错了，是目标元素 $p_{i}$ 不是差值
- **补题后尝试**
- 提交时间:(07/15)23:15 状态:WA 详细状态:`Wrong answer on test 4`
> 加入差值对 $x+y$ 的判断
- 提交时间:(07/15)23:44 状态:WA 详细状态:`Wrong answer on test 4`
> 不对，题目好像是根据位置的差值进行判断  
那么我先存好每个元素的位置，然后跟当前不同元素与目标元素的位置差值按之前的逻辑进行判断
- 提交时间:(07/15)23:52 状态:AC 详细状态:`-`
> 经GPT指导下，这题应该是用最大公约数 $gcd(x,y)$ 进行判断  
> **裴蜀定理**:所有形如 $a\times x+b\times y$ 的整数，恰好是 $gcd(x, y)$ 的倍数  
> 然后我想的是，如果位置差值为 $x,y,x+y$ 的倍数，必定能通过移动特定步长与目标值交换  
对于 $n\times x+m\times y$ 这样的，我想的是必定构成 $|x-y|$ 的倍数  
但是GPT举例的 $x=6,y=14$ 完全错开我的例子，仅有**裴蜀定理**才能解决这题

# D.Yaroslav and Productivity
[Tag]
## 题目介绍
Yaroslav's productivity during the day is described by an array $a$ of length $n$. His productivity can be negative if he watches short videos, or positive if he works. The total productivity is defined as the **sum** of all values in the array.
Sometimes Yaroslav reads motivational posts. He has $m$ posts, where the $j$\-th post has an impact value $b_j$. If Yaroslav reads a post with value $b_j$, then all productivity values from the beginning of the day up to position $b_j$ change sign, that is, all integers $a_1, a_2, a_3, \dots, a_{b_j}$ are multiplied by $-1$.
For example, let $a = [1, -4, 3, -4]$, and Yaroslav reads a post with value $3$. Then the signs of the first three elements are flipped, and the array becomes $[-1, 4, -3, -4]$. If he then reads a post with value $1$, the first element changes sign again, and the array becomes $[1, 4, -3, -4]$.
What is the **maximum** possible total productivity Yaroslav can achieve by reading any (possibly none) of the posts?
> $1<=t<=10^4, 1<=m<=n<=2\times 10^5, -10^9<=a_{i}<=10^9, 1<=b_{i}<=n$
## 提交详情
- 提交时间:(07/15)00:28 状态:MLE 详细状态:`Memory limit exceeded on test 3`
> 先对 $a$ 数组做正前缀和(原数字符号)和负前缀和(每个数都加负号)  
然后对 $b$ 数组用DFS找到其所有的子数组(含非连续)  
最后对这些子数组倒序进行处理，每个子数组 $c$ 的 $[c_{i-1}+1,c_{i}]$ 必定是不会被影响的  
同时保留一个布尔值变量，意为是否翻转，初始为翻转，遍历子数组每个元素时就翻转  
如果该变量为翻转，就用负前缀和区间的，否则就用正前缀和区间的  
保留每个子数组最后的和，输出最大的和即可
## 作者题解
> 作者同我一样，将每个帖子 $b_{k}$ 影响的区间扩展为 $[b_{k}, b_{k+1}-1]$  
然后关键点是作者没有像我一样去用DFS寻找所有可能的帖子组合  
而是直接计算每个区间的最大值  
虽然 $k$ 帖子会影响 $[1,b_{k}-1]$ ,但若 $k$ 帖子值得选( $[b_{k},b_{k+1}-1]$ 区间翻转后总和更大)  
我直接选 $b_{k-1}$ 个，把前面 $[1,b_{k}-1]$ 的翻转过来就可以了  
所以每个区间的选择可以独立化，不受其它区间影响  