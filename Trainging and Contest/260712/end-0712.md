# 2193D.Monster Game
[Rating]1100
[Tag]二分、排序、双指针
[Time]思考+理解57:43
## 题目介绍
You have been gifted a new game called "Elatyh". In the game, you are given $n$ swords, each with its own strength. In particular, the sword numbered $i$ has a strength of $a_i$. The game consists of $n$ levels, each of which features a monster.
You start at level $1$ and progress further. To pass level $i$ and move on to level $i + 1$, you need to defeat the monster at level $i$. To defeat the monster at level $i$, you need to deal it $b_i$ sword strikes. The swords in the game are very fragile, so they can only deal **one** strike before breaking. If you complete level $n$ or run out of swords, you can finish the game and proceed to score calculation.
Before the game, you are allowed to choose the difficulty level. If you choose difficulty $x$, swords with a strength less than $x$ will not affect the monsters. The game score in this case is equal to $x$ multiplied by the number of levels completed. Your task is to choose the game difficulty in such a way as to maximize the game score.
> $1<=t<=10^4, 1<=n<=2\times 10^5, 1<=a_{i}<=10^9, 1<=b_{i}<=n$
## 提交情况
- 提交时间:22:24 状态:AC
> 我一开始想的是使用`离散化BIT`  
即我一开始以为难度 $x$ 是 $1<=x<=n$ 的，即跟坐标一样的  
用BIT统计每个难度之前的大于或等于难度的可用数量  
然后再对怪物过关需要刀数做前缀和  
最后依次对难度、每个的难度能使用的刀数量、能过关数(用二分去寻找)计算得分，统计最高分即可  
但这样就发现了一个问题，即这样永远无法达到样例中的百万得分  
思考良久后，我发现这里选择的难度 $x$ ,是对应的刀的威力  
即 $\min(a_{1},a_{2},\dots,a_{n}) <= x <= \max(a_{1},a_{2},\dots,a_{n})$  
那么只要统计每个威力下有几把刀，然后对威力进行排序，倒序做每个威力下能使用的刀数量(后缀和)  
最后根据刀的威力(即难度)、可使用刀的数量、通关数(跟之前一样使用二分)来统计每个难度下的得分，记录最高分即可  
这题算我半AC通过，因为我向`GPT-5.6-Luna`求助了两次:
> 1. 第一次我向他询问如何通过BIT离散化记录出现次数
> 2. 第二次我向他询问代码的BUG出现在哪

# 1108 Div.2 
> **===比赛题目分割线===**
# A.farmpiggie and Subset Sum
## 题目介绍
For a permutation $^{\text{∗}}$ $p$ of even length, you can do the following process:
-   Initialize a counter $c = 0.$
-   For each $i$ from $1$ to $n,$ either add $i \cdot p_i$ to $c$, subtract $i \cdot p_i$ from $c$, or do nothing.
Let the final value of the counter be $c_{\mathrm{final}}.$
Formally, for each $i \in \{1,\ldots,n\},$ consider the set $S_i = \{-i \cdot p_i, 0, i \cdot p_i\}$ and choose some $x_i \in S_i.$ Set $c_{\mathrm{final}} = \sum_{i = 1}^{n}x_i.$
You are given a single **even** integer $n$. Find any permutation of length $n$ so that regardless of the operations chosen, the final value $c_{\mathrm{final}}$ will not be $1.$
$^{\text{∗}}$A permutation of length $n$ is an array consisting of $n$ distinct integers from $1$ to $n$ in arbitrary order. For example, $[2,3,1,5,4]$ is a permutation, but $[1,2,2]$ is not a permutation ($2$ appears twice in the array), and $[1,3,4]$ is also not a permutation ($n=3$ but there is $4$ in the array).
> $1<=t<=25, 2<=n<=50$
## 思考
> 构造一个长度为 $n$ 的序列 $p$ ,使得 $x_{i} \in \{-i \cdot p_i, 0, i \cdot p_i\}$ 且 $c_{\mathrm{final}} = \sum_{i = 1}^{n}x_i \neq 1$  
感觉同B一样，必定存在秒杀理论  
因为如果真的去构造，没想到有什么方法可以避免任何操作下不会使得总和为1  
目前想到就这个，避免构造出来的数组任意位置前缀和和后缀和的差值为1

# B.ezraft and Array
## 题目介绍
You are given a single integer $n.$ Construct an array of $n$ distinct positive integers $a_1, \ldots, a_n$ such that for all $i \, (1 \le i \le n)$, $a_1 + a_2 + a_3 + \ldots + a_n$ is divisible by $a_i,$ or determine that no such array exists.
> $1<=n<=50$
## 思考
> 寻找一个数,使得其因子数大于或等于 $n$ ,且其因子中有 $n$ 个因子的总和为其本身  
由于其存在多个样例同时输入，因此必定有数学规律等可以直接O(n)或以下秒杀的方法
如果单纯去统计因子数，可以用 $n$ 的上限50去统计  
但是如果用埃氏筛去统计，必定MLE或TLE，且其中数字必定会拉的特别大

# C.0mar and Alternating Sums
## 题目介绍
Define the alternating sum of an array $b$ of length $k$ to be $\sum_{i = 1}^{k}(-1)^{i+1}b_i$. 
You are given a non-decreasing$^{\text{∗}}$ array $a$ of length $n$ such that for all $1 \le i \le n,$ either $a_i = -1$ or $a_i$ is a positive integer. Find the number of sequences $1 \le i_1 \lt i_2 \lt \ldots \lt i_k \le n$ such that the alternating sum of the sequence $a_{i_1}, a_{i_2}, \ldots, a_{i_k}$ is $0.$ Since this number may be large, output it modulo $10^9+7$. 
Two sets $i_1, \ldots, i_{k_1}$ and $i'_1, \ldots, i'_{k_2}$ of indices are considered different if $k_1 \neq k_2$ or there exists a $j$ such that   $i_j \neq i'_j.$
$^{\text{∗}}$A sequence $a_1, \ldots, a_n$ is non-decreasing if $a_1 \le a_2 \le \ldots \le a_n$.
> $1<=t<=10^4, 1<=n<=2\times 10^5$
## 思考
> 寻找 $a$ 存在多少个子数组，满足题目方法下计算总和为0  
暴力下可以使用DFS，但是无法处理剪枝及优化  
对于每个元素，有两种状态，选与不选  
即本题是一颗高度为 $n$ 的二叉树，父节点是数组第一个元素，然后两个分支，选当前节点和不选当前节点
使用DFS提交后，样例4超时
> **===比赛题目分割线===**

# 2202B.ABAB Construction
[Rating]1200
[Tag]动态规划，贪心，模拟
[Time]思考同尝试47:43,理解19:05
## 题目介绍
There is a string $T$ of length $n$, such that $T_i=$ 'a' for all odd $i$ and $T_i=$ 'b' for all even $i$.
One day, Bob generated a string $S$ with the following algorithm.
1.  Initialize $S$ to the empty string.
2.  Remove either the **first** letter or the **last** letter from $T$, and **append** it to $S$.
3.  If $T$ is empty, terminate and return the string $S$. Otherwise, go back to the second step.
Then, Bob wrote down the generated string $S$ on a note and forgot about it for a few years. The note was worn out when Bob found it, and someone might have secretly changed some letters. Of course, Bob wants to know if someone did alter the string!
You are given a string $X$ of length $n$, which consists of 'a', 'b', and '?'.
Please determine if there exists a string $A$ which satisfies the following conditions:
-   $|A|=n$;
-   $A_i$ is either 'a' or 'b' for all $1 \le i \le n$;
-   $A_i=X_i$ for all $1 \le i \le n$ such that $X_i$ is not '?';
-   The string $A$ can be generated from the algorithm described above.
> $1<=t<=10^4, 1<=n<=200000$
## 提交情况
- 提交时间:(07/13)00:08 状态:WA 详细状态:`Wrong answer on test 2`  
> 字符串`S`中仅含`a`,`b`,`?`三种字母，其中`?`可以换成另两种其中一个  
不符合的情况仅有某一字符多于另一字符(`?`除外)，如果多于，必定连续  
那么只要判定连续即可
- 提交时间:(07/13)00:34 状态:WA 详细状态:`Wrong answer on test 2`  
> 计算每个`?`出现的连续次数，如果`?`两边字符不同且次数为偶数、两边字符相同为奇数，就不符合
**尝试**:记录每个字符出现的次数，倒序记录，通过某种次数间的规律来快速判题，但失败
- 提交时间:(07/13)00:56 状态:AC 详细状态:-
> 借助gpt帮助，我理解了作者的思路是直接根据长度判断  
若长度为奇数，从第二位往后判断是否与下一位相同；若为偶数，则从第一位开始判断  
如果相同，就不能构成  
因为奇数长度下，对于`ababa`，无论取头还是尾，下两步的选择必然不能相同  
偶数长度下，对于`abab`，无论取头还是尾，当前取出来的字符就不能与下一个相同  
这样扩展下，就能解决问题  
同时我还问了GPT为什么奇数开头一定为`a`，因为我没看题(题目要求)