1. 补题
# (2025CCPC高职高专)B 爬山
[Time]理解+尝试39:59，补题02:55，复盘改写03:18
## 题目详情
小 C 喜欢爬山，但他的体能一般，不能连续爬升太高。
这天小 C 决定去爬比特峰。比特峰共有 $n$ 个景点，第 $i$ 个景点的海拔为 $h_i$ 米。
景点之间共有 $m$ 条双向线路，第 $i$ 条线路连通景点 $u_i$ 和景点 $v_i$ ，小 C 从任意方向走过第 $i$ 条线路所需的时长均为 $t_i$ 分钟。
小 C 在爬山过程中会累积疲劳值，初始的疲劳值为 $0$ ，并且小 C 能够忍受的最大疲劳值为 $H$ 。
假设当前小 C 位于某个景点 $x$，且存在一条从景点 $x$ 到景点 $y$ 的线路：
+ 如果景点 $x$ 比景点 $y$ 的海拔高，那么这是一段轻松的下坡路，小 C 如果选择走这条线路，体力得到了恢复，疲劳值被清空。
+ 否则，如果小 C 选择走这条线路，疲劳值会累积 $h_y-h_x$ 。小 C 只能在保证积累的疲劳值不超过 $H$ 的前提下走这条线路。
现在小 C 从 $1$ 号景点出发，只能通过这些线路在景点间移动。对于每个景点，小 C 想知道他最快需要多少分钟可以到达该景点，或永远无法到达？
*输入格式*
第一行包含三个正整数 $n,m,H~(2\le n\le 10^4,~0\le m\le 2\times 10^4,~1\le H\le 100)$ ，分别表示景点的个数，线路的条数，以及小 C 能忍受的最大疲劳值。
第二行包含 $n$ 个正整数 $h_1,h_2,\cdots,h_n~(1\le h_i\le 10^9)$，代表每个景点的海拔。
接下来 $m$ 行，每行三个正整数 $u_i,v_i,t_i~(1\le u_i,v_i\le n,u_i\ne v_i,1\le t_i\le 10^9)$ ，代表一条线路连接的两个景点和所需的时长。
*输出格式*
输出一行 $n-1$ 个整数，第 $i$ 个整数代表小 C 到达第 $i+1$ 号景点所需的最短时间（以分钟为单位）。如果小 C 永远无法到达该景点，输出 `-1` 。
## 提交详情
- 提交时间:14:59:43 状态:WA 详细状态:`-`
> 存在两个值，一个是两景点之间的时间，另一个是体力(疲劳值)  
要求从1出发，求到达每个点需要的最少时间  
对于第二个值体力，有上限，且逆行(高海拔到低海拔)会清空  
使用Dijkstar，优先保证体力最小，再找时间最少  
Dijkstar对应结构体中先保证时间最少，再保证体力最小，与上面反着来  
因为本题主体变量为时间，而非体力  
开两个数组，分别存到达每个点的时间最小值和体力最小值  
Dijkstar中退出条件为状态过期(时间和体力)、体力超限  
然后选点时要计算新的体力和重量  
如果目标景点海拔跟当前景点一样，体力就不变  
如果目标景点海拔比当前景点还低，体力就清空  
如果目标景点海拔比当前景点还高，为了要到达这个景点  
必然要先退一步清空再走，所以进行退一步松弛  
选择当前景点四周海拔比当前景点还低的景点，走一个来回，体力清空再前往目标景点  
并加上这一个来回耗费的时间，如果目标景点比当前景点所需体力超过体力上限，就不走  
如果没有更低的景点供体力清空，也不行  
然后更新景点的逻辑是时间更少，亦或是在时间一样的情况下，体力更少，才更新
- 提交时间: 状态: 详细状态:``
> 经*GPT*指导，我应该用一个二维数组，而不是两个单独的数组  
因为到达某个点时，不同的疲劳度带来的时间耗费完全不同  
最重要的是，疲劳度的多少，会影响时间的长短  

# (2025-I)C.Canvas Painting
[Time]尝试+理解20:43，第一次补题16:04，二次尝试25:27，
## 题目详情
In the Kingdom of Flealand, there is a long canvas divided into $n$ segments, numbered from $1$ to $n$. Each segment is initially painted with a unique color, such that the color of segment $i$ is $a_i = i$.
The royal artist Crysflea has received $m$ magical paint spells. Each spell affects a continuous interval $[l, r]$ on the canvas. When a spell is cast, the artist may choose **any two positions** $u$ and $v$ within this interval ($l \leq u, v \leq r$), and repaint segment $u$ with the color of segment $v$, i.e., perform $a_u = a_v$.
The spells can be used in **any order**, and each spell may be used at most once.
After all spells are used, the artist wishes to admire the canvas. Help the artist **minimize the number of distinct colors** that remain on the canvas.
*Input*
The input contains multiple test cases.
The first line contains a single integer $T$ ($1 \leq T \leq 2\times 10^5$) --- the number of test cases.
For each test case:
- The first line contains two integers $m$ ($1 \leq m \leq 2 \times 10^5$) and $n$ ($1 \leq n \leq 10^9$) --- the number of spells and the length of the canvas.
- The next $m$ lines each contain two integers $l$ and $r$ ($1 \leq l \leq r \leq n$) --- describing one paint spell.
The total number of spells across all test cases satisfies $\sum m \leq 2 \times 10^5$
*Output*
For each test case, output a single integer --- the minimum number of distinct colors on the canvas after applying the spells.
## 提交详情
- 提交时间:18:23:19 状态:MLE 详细状态:`-`
> 执行 $m$ 次咒语，每个咒语含一个区间 $[l,r]$ ，可以使得区间内任意两个格子颜色相等  
尝试用并查集试试  
先行所有区间进行排序，先根据长度升序，然后同长度下以左边界和右边界升序
- 提交时间:18:23:19 状态:MLE 详细状态:`-`
> 我试着把并查集放到主函数内，然后每次都重置同一个并查集，依旧不行
- 提交时间:18:51:14 状态:MLE 详细状态:`-`
> 突然想到对一个区间操作，都是将这个区间内所有的方块变为同一个颜色 
那么就需要找一个对齐值，即所有的含这个对齐值为左或右边界的区间，都将其变为该对齐值的颜色  
如果设置右区间为对齐值，可能会有冲突的风险，设置为左区间试试  
样例能通过，但是MLE
- 提交时间: 状态: 详细状态:``
> 经*GPT*咨询和看去年AC的代码后，我发现MLE的主要原因就是在 $10^9$ 下数组超限了  
然后我打算用我想到的方法进行二次尝试  
> 二次尝试耗时25:27失败，我试图给每个左端点匹配一个最近的右端点，即对1优先匹配2  
然后保存每次左端点左侧能匹配的最大值，下次从该最大值或左端点开始  
经*GPT*二次指导，我的问题在于先前的左端点存在匹配占用当前左端点的匹配，前一个的左端点可以取当前左端点匹配的右侧  
即使得两个端点都能完全匹配  
然后我在考虑是否存在c $(a<c<b)$ 匹配到了区间(b,d)，即此时删除了d  
即设左端点为1的区间有4个，左端点为7的区间有2个，整体长度为8。这样就不会使得6去匹配左端点为7的区间吗？  
这样完全不会，因为是从左向右遍历的，完全不会触发超过


# 
[Time]
## 题目详情

## 提交详情
- 提交时间: 状态: 详细状态:``
> 

# 
[Time]
## 题目详情

## 提交详情
- 提交时间: 状态: 详细状态:``
> 

# 
[Time]
## 题目详情

## 提交详情
- 提交时间: 状态: 详细状态:``
> 

# A.01 Sequence
[Time]理解+思考07:39，
## 题目详情
Given a binary cyclic sequence $$S$$ of length $$n$$, whose elements are either $$0$$ or $$1$$, you can do the following operation any number of times.
-   **Operation**: if the length of $$S$$ is greater than or equal to $$3$$, choose a position $$i$$ ($$1\le i\le n$$) such that $$S_i=1$$, remove the element on position $$i$$ and both its adjacent elements (remove $$3$$ elements in total), and do not change the relative order of the other elements. Note that elements in the first and last positions are considered adjacent.
A binary cyclic sequence $$S$$ is called *good* if you can make $$S$$ empty using the operation above.
And the *beauty* of a binary cyclic sequence $$S$$, $$f(S)$$, is defined as the minimum number of modifications needed to make $$S$$ good. In a single modification you can flip an arbitrary element in $$S$$, that is, $$0$$ becomes $$1$$ and $$1$$ becomes $$0$$.
Given are a binary string $$a$$ of length $$n$$ and $$q$$ queries. For the $$i$$-th query you are given two integers $$l_i$$ and $$r_i$$, and you should answer $$f(a_{l_i..r_i})$$ (where we consider the substring $$a_{l_i..r_i}$$ as a cyclic sequence).
*输入格式*
The first line contains two integers $$n$$ and $$q$$ ($$3\le n\le 10^6$$, $$1\le q\le 10^6$$) — the length of string $$a$$ and the number of queries, respectively.
The second line contains the string $$a_1a_2\cdots a_n$$, where each $$a_i$$ is either $$0$$ or $$1$$.
Each of the following $$q$$ lines contains two integers $$l_i$$ and $$r_i$$ ($$1\le l_i\le r_i\le n$$, $$(r_i-l_i+1)\equiv 0\mod 3$$) describing the $$i$$-th query.
*输出格式*
Output $$q$$ lines, where the $$i$$-th line contains a single integer — the answer for the $$i$$-th query.
## 提交详情
- 提交时间: 状态: 详细状态:``
> 对于一个0/1字符串，如果能通过删除操作(选择一个`1`，删除其及左右两个字符)清空字符串，则叫其为好字符串  
然后要根据题目给与范围去判断这个范围内的子字符串是否是好的字符串  
我的想法是预选出所有要翻转操作的所有位置  
这里有个疑惑，疑惑于如何定义一次操作  
即翻转一个字符算一次操作，还是翻转连续多个字符算一次操作，亦或是翻转多个非连续字符算一次操作  
对于`00001000100000010000`，如果翻转定义为后两者，我都能仅执行一次操作(翻转整个字符串)使其为好字符串  
那么这里只能定义为翻转一个字符   
根据该字符串，取得 $[6,20]$ 得到`000100000010000`  
由于首尾相连，可以将其分为两部分，中间的`000000`和首尾相连的`0000000`  
如果去除`1`左右的`0`，这两部分可以变成中间的`0000`和首尾相连的`00000`  
这两部分如果要根据题目条件去删除，每个都需要执行两次操作，共四次操作，使其变为中间的`0110`和`01010`  
与题目给与的答案3不符

