# 8.17 学习日记

Codeforces Round 1034 (Div. 3)

- A. Blackboard Game
    
    Initially, the integers from $0$ to $n-1$ are written on a blackboard.
    
    In one round,
    
    - Alice chooses an integer $a$ on the blackboard and erases it;
    - then Bob chooses an integer $b$ on the blackboard such that $a+b \equiv 3 \pmod 4$$^{\text{∗}}$ and erases it.
    
    Rounds take place in succession until a player is unable to make a move — the first player who is unable to make a move loses. Determine who wins with optimal play.
    
    $^{\text{∗}}$We define that $x\equiv y\pmod m$ whenever $x-y$ is an integer multiple of $m$.
    
    给与0到n-1的n个数，Alice可以随意抽取一个数a，Bob也可以抽取一个数b，但b必须满足条件(a+b)-3%4==0。谁先无法拿取则对方获胜，请问谁会获胜？
    
    > 一血Accept
    > 
    
    ```cpp
    #include <bits/stdc++.h>
    using namespace std;
    #define IOS                                                                    \
      ios::sync_with_stdio(false);                                                 \
      cin.tie(0);
    // vector<int> iet;    //目标数(取余4等于3)
    string solve() {
      int n;
      cin >> n;
      /*
      // 找到第一个比他大的目标数
      int index = lower_bound(iet.begin(),iet.end(),n) - iet.begin();
      vector<bool> f(n+1,true);   //对所有进行状态标记
      vector<int> res;            //可用的数
      //取出来(避免下标失效)
          for(int i=0;i<index;i++)
                  if(iet[i]<=n)
                          res.push_back(iet[i]);
      // Alice赢的量和Bob赢的量
          int a=0,b=0;
          for(int i=1;i<=n;i++){
          if(!f[i]) continue;
          bool flag=false;
          //根据每个目标数找是否存在a+b
          for(int& t:res){
              if(f[i]&&f[t-i]){
                  a++;b++;
                  f[i]=false;
                  f[t-i]=false;
                  break;
              }
          }
      }
      return a>b?"Alice":"Bob";
      */
      // 后面我又研究了一下
      // 假设对于0-3来说
      /*
          0:Alice,因为Bob选啥都不行
          1:Alice,因为Bob选啥都不行
          2:Alice,因为如果Alice选了2,Bob就没法选了
                  如果Alice选了1或2,Bob就可以选
                  但还剩一个0,只能给Alice选Bob无法选
          3:Bob,里面存在1-2和0-3符合条件,即接下来Alice无法选
          4:Alice,0-3和1-2选完后,只剩一个数供Alice选
      */
      // 题目给的是n,要求在[0,n-1]里面选
      // 根据以上规律,可得每四个数中:
      // 若只有一两个数,必定Alice
      // 第三个数也是Alice,一对符合条件加一个数
      // 第四个数是Bob,选完后到Alice无法选了
      // 所以,根据规律,直接使用n%4判断是否Bob赢即可
      return n % 4 != 0 ? "Alice" : "Bob";
    }
    int main() {
      IOS;
      int t;
      cin >> t;
      /*
      //枚举所有取余4等于3的数
      for(int i=4;i<101;i+=4)
          iet.push_back(i-3);
      */
      while (t--)
        cout << solve() << (t != 0 ? "\n" : "");
      return 0;
    }
    ```
    
    重新思考版
    
    ```cpp
    #include<bits/stdc++.h>
    using namespace std;
    #define IOS ios::sync_with_stdio(false);cin.tie(0);
    string solve(){
      int n;cin>>n;
      // a任意条件
      // b必须满足((a+b)-3)%4==0
      // 4的倍数有:0   4   8    12
      // 满足的a+b:   3   7   11
      // 即任意a+b满足即可
      // 单拎前四个数来看: 0 1 2 3
      // 0-3和1-2都可以作为a和b
      // 通过贪心,任意一个数都能满足为4个中
      // 即对于第一、二个数,Bob找不到可以满足的
      // 第三个数也是一样
      // 第四个数是Bob选完后,Alice将无数可选
      // 第五个数也是选中两对数后,Alice可以选那个剩下的数,同第一个数
      // 第六个数也是同理
      // 故整个所有数结构可以优化成每四个数进行判断
      return n%4==0?"Bob":"Alice";
    }
    int main(){
      IOS;
      int t;cin>>t;
      while(t--)
        cout<<solve()<<(t!=0?"\n":"");
      return 0;
    }
    ```
    
- B. Tournament
    
    You are given an array of integers $a_1,a_2,\dots,a_n$. A tournament is held with $n$ players. Player $i$ has strength $a_i$.
    
    While more than $k$ players remain,
    
    - Two remaining players are chosen at random;
    - Then the chosen player with the lower strength is eliminated. If the chosen players have the same strength, one is eliminated at random.
    
    Given integers $j$ and $k$ ($1 \leq j,k \leq n$), determine if there is any way for player $j$ to be one of the last $k$ remaining players.
    
    给与n个选手的实力。每两个选手可以进行对决，实力较差的被淘汰，实力相同则随机淘汰。请问玩家j是否在剩下的k个玩家之中。
    
    > Wrong answer on test 2
    > 
    
    比赛时代码
    
    ```cpp
    #include<bits/stdc++.h>
    using namespace std;
    #define IOS ios::sync_with_stdio(false);cin.tie(0);
    string solve(){
      int n,j,k;cin>>n>>j>>k;
      vector<int> a(n,0);
      unordered_map<int,int> hash;
      set<int> num;
      for(int i=0;i<n;i++){
        cin>>a[i];
        hash[a[i]]++;
        num.insert(a[i]);
      }
      int mark=a[j-1];
      sort(a.begin(),a.end());
      // 该玩家当前排名
      int rank = 1 + lower_bound(a.begin(),a.end(),mark) - a.begin();
      n-=rank-1;    //消除比他实力低的玩家
      // 消除同档次其他玩家
      if(hash[mark]>1) n-=hash[mark]-1;
      // 该玩家所在分数位于前k名
      if(n <= k){
        // 如果同分只有他一人,自然可以
        // 如果不止他一人,假设他都胜利了
        return "YES";
      }
      for(auto it=num.end();it!=num.begin()&&(*it)>mark;it--){
        if(hash[*it]>1){
            n-=hash[*it]-1;
            if(n<=k)
                return "YES";
        }
      }
      return "NO";
    }
    int main(){
      IOS;
      int t;cin>>t;
      while(t--)
        cout<<solve()<<(t!=0?"\n":"");
      return 0;
    }
    ```
    
    重新思考版
    
    之前我过分考虑j失败的情况，认为只有模拟了所有的情况，才能使得j胜利，可是完全不用思考这么多。
    
    ```cpp
    #include<bits/stdc++.h>
    using namespace std;
    #define IOS ios::sync_with_stdio(false);cin.tie(0);
    string solve(){
        int n,j,k;cin>>n>>j>>k;
        vector<int> a(n);
        set<int> num;
        for(int i=0;i<n;i++) cin>>a[i];
        // 当玩家j实力最强时,他都会在k之中(永远第一个)
        // 如果此时拥有j实力的不止一人,假定他们都j随机淘汰了,只剩j
        // 如果k>1,就假定j之前的人都互相攻击掉了只剩1个了,那么此时就只剩j和比他强的两个了
        int m = *max_element(a.begin(),a.end());
        return (k > 1 || m == a[j-1])?"YES":"NO";
    }
    int main(){
        IOS;
        int t;cin>>t;
        while(t--)
            cout<<solve()<<(t!=0?"\n":"");
        return 0;
    }
    ```
    
- C. Prefix Min and Suffix Max
    
    You are given an array $a$ of **distinct** integers.
    
    In one operation, you may either:
    
    - choose a nonempty **prefix**$^{\text{∗}}$ of $a$ and replace it with its minimum value, or
    - choose a nonempty **suffix**$^{\text{†}}$ of $a$ and replace it with its maximum value.
    
    Note that you may choose the entire array $a$.
    
    For each element $a_i$, determine if there exists some sequence of operations to transform $a$ into $[a_i]$; that is, make the array $a$ consist of only one element, which is $a_i$. Output your answer as a binary string of length $n$, where the $i$\-th character is $1$ if there exists a sequence to transform $a$ into $[a_i]$, and $0$ otherwise.
    
    $^{\text{∗}}$A **prefix** of an array is a subarray consisting of the first $k$ elements of the array, for some integer $k$.
    
    $^{\text{†}}$A **suffix** of an array is a subarray consisting of the last $k$ elements of the array, for some integer $k$.
    
    **Input**
    
    The first line contains an integer $t$ ($1 \leq t \leq 10^4$)  — the number of test cases.
    
    The first line of each test case contains one integer $n$ ($2 \leq n \leq 2\cdot 10^5$) — the size of the array $a$.
    
    The second line of each test case contains $n$ integers, $a_1,a_2,\dots,a_n$ ($1 \leq a_i \leq 10^6$). It is guaranteed that all $a_i$ are distinct.
    
    It is guaranteed that the sum of $n$ over all test cases does not exceed $2\cdot 10^5$.
    
    **Output**
    
    For each test case, output a binary string of length $n$  — the $i$\-th character should be $1$ if there exists a sequence of operations as described above, and $0$ otherwise.
    
    给与一个数组a。你可以对数组a做操作，一个是选择一个非空前缀数组将其换成前缀数组内的最小值，另一个是选择一个非空后缀将其换成后缀数组内的最大值。请问，是否对于数组a内的每个元素，替换后是否与原数组相同，返回二进制字符串。
    
    > 一血Accept
    > 
    
    ```cpp
    #include<bits/stdc++.h>
    using namespace std;
    #define IOS ios::sync_with_stdio(false);cin.tie(0);
    string solve(){
        int n;cin>>n;
        vector<int> a(n),perMin(n,0),sufMax(n,INT_MAX);
        for(int i=0;i<n;i++) cin>>a[i];
        // i前最小
        perMin[0]=a[0];
        for(int i=1;i<n;i++)  perMin[i]=min(a[i],perMin[i-1]);
        // i后最大
        sufMax[n-1]=a[n-1];
        for(int i=n-2;i>=0;i--) sufMax[i]=max(a[i],sufMax[i+1]);
        unordered_set<int> res;
        for(int i=0;i<n;i++){
            vector<int> ans;
            // i前缀最小
            bool per = i>0;
            if(per) ans.push_back(perMin[i-1]);
            // i元素自己
            ans.push_back(a[i]);
            // i后缀最大
            bool suf = i<n;
            if(suf) ans.push_back(sufMax[i+1]);
            // 首元素 和 尾元素
            if(!per || !suf){
                // 大小为2的前缀替换
                res.insert(min(ans[0],ans[1]));
                // 大小为2的后缀替换
                res.insert(max(ans[0],ans[1]));
            }else{
                // 大小为2的前缀
                int p = min(ans[0],ans[1]);
                // 此时剩下：前缀 后最小
                // 前缀
                res.insert(min(p,ans[2]));
                // 后缀
                res.insert(max(p,ans[2]));
                // 大小为2的后缀
                int s = max(ans[1],ans[2]);
                // 此时剩下：前最大 后缀
                // 前缀
                res.insert(min(s,ans[0]));
                // 后缀
                res.insert(max(s,ans[0]));
            }
        }
        string s="";
        for(int& i:a){
            if(res.find(i)!=res.end()) s+='1';
            else s+='0';
        }
        return s;
    }
    int main(){
        IOS;
        int t;cin>>t;
        while(t--)
            cout<<solve()<<(t!=0?"\n":"");
        return 0;
    }
    ```
    
    优化版本
    
    ```cpp
    #include<bits/stdc++.h>
    using namespace std;
    #define IOS ios::sync_with_stdio(false);cin.tie(0);
    void solve(){
        int n;cin>>n;
        vector<int> a(n),perMin(n,0),sufMax(n,INT_MAX);
        for(int i=0;i<n;i++) cin>>a[i];
        // i前最小
        perMin[0]=a[0];
        for(int i=1;i<n;i++)  perMin[i]=min(a[i],perMin[i-1]);
        // i后最大
        sufMax[n-1]=a[n-1];
        for(int i=n-2;i>=0;i--) sufMax[i]=max(a[i],sufMax[i+1]);
        // 对于任何i位置元素
        // 最后都是剩余三个元素: 前缀最小 元素 后缀最大
        // 跟之前模拟分析一样
        // 该元素保留的条件,要么是包含元素所在前缀最小的,要么是包含元素所在后缀最大的
        for(int i=0;i<n;i++)
            cout<<(a[i]==perMin[i]||a[i]==sufMax[i])?1:0;
        cout<<"\n";
    }
    int main(){
        IOS;
        int t;cin>>t;
        while(t--)
            solve();
        return 0;
    }
    ```
    
- D. Binary String Battle
    
    Alice and Bob are given a binary string $s$ of length $n$, and an integer $k$ ($1\leq k < n$).
    
    Alice wins if she is able to transform all characters of $s$ into zeroes. If Alice is unable to win in a finite number of moves, then Bob wins.
    
    Alice and Bob take turns, with Alice going first.
    
    - On Alice's turn, she may choose any **subsequence**$^{\text{∗}}$ of length $k$ in $s$, then set all characters in that subsequence to zero.
    - On Bob's turn, he may choose any **substring**$^{\text{†}}$ of length $k$ in $s$, then set all characters in that substring to one.
    
    Note that Alice wins if the string consists of all zeros at any point during the game, including in between Alice's and Bob's turns.
    
    Determine who wins with optimal play.
    
    $^{\text{∗}}$A **subsequence** of a string $s$ is a set of characters in $s$. Note that these characters do not have to be adjacent.
    
    $^{\text{†}}$A **substring** of a string $s$ is a contiguous group of characters in $s$. Note that these characters must be adjacent.
    
    给与一个长度为n的字符串s，Alice会先操作，将长度为k、不相邻的子字符串换成0，然后Bob会将长度为k、相邻的子字符串换成1。当字符串全变为0时Alice胜利，请问Alice和Bob谁会胜利？
    
    > Wrong answer on test 2
    > 
    
    ```cpp
    #include<bits/stdc++.h>
    using namespace std;
    #define IOS ios::sync_with_stdio(false);cin.tie(0);
    string solve(){
        int n,k;cin>>n>>k;
        string s;cin>>s;
        unordered_map<int,int> hash;
        for(char c:s){
            hash[c-'0']++;
        }
        // Alice选择的子字符串可以不相邻
        // Bob选择的子字符串必须相邻
        // 1的次数同操作范围一样
        if(hash[1]==k)  return "Alice";
        // 操作范围超过字符串一半
        int mid = n/2 + (n&1?1:0);
        if(mid<=k) return "Alice";
        return "Bob";
    }
    int main(){
        IOS;
        int t;cin>>t;
        while(t--)
            cout<<solve()<<(t!=0?"\n":"");
        return 0;
    }
    ```
    
    重新思考版
    
    ```cpp
    #include<bits/stdc++.h>
    using namespace std;
    #define IOS ios::sync_with_stdio(false);cin.tie(0);
    string solve(){
        int n,k;cin>>n>>k;
        string s;cin>>s;
        unordered_map<int,int> hash;
        for(char c:s){
            hash[c-'0']++;
        }
        // 由于Alice是一个不连续的子序列
        // 当k比字符串中1的个数多时,Alice就可以直接替换获得胜利
        // 当可操作的字符串超过s的一半时
        // Alice可以凭借不连续的优势比字符串完全清零
        return hash[1]<=k||n<2*k?"Alice":"Bob";
    }
    int main(){
        IOS;
        int t;cin>>t;
        while(t--)
            cout<<solve()<<(t!=0?"\n":"");
        return 0;
    }
    ```
    
- E. MEX Count
    
    Define the $\mathrm{MEX}$ (minimum excluded value) of an array to be the smallest nonnegative integer not present in the array. For example,
    
    - $\mathrm{MEX}([2, 2, 1]) = 0$ because $0$ is not in the array.
    - $\mathrm{MEX}([3, 1, 0, 1]) = 2$ because $0$ and $1$ are in the array but $2$ is not.
    - $\mathrm{MEX}([0, 3, 1, 2]) = 4$ because $0$, $1$, $2$, and $3$ are in the array but $4$ is not.
    
    You are given an array $a$ of size $n$ of nonnegative integers.
    
    For all $k$ ($0\leq k \leq n$), count the number of possible values of $\mathrm{MEX}(a)$ after removing exactly $k$ values from $a$.
    
    给定一个大小为n的数组，MEX为数组中不存在的最小负整数，请问在删除0到n个元素后，MEX数量分别为多少呢？
    
    > -
    > 
    
    比赛版本（未提交）
    
    ```cpp
    #include<bits/stdc++.h>
    using namespace std;
    #define IOS ios::sync_with_stdio(false);cin.tie(0);
    vector<int> solve(){
        int n;cin>>n;
        set<int> a;
        unordered_map<int,int> hash;
        for(int i=0;i<n;i++){
            int t;cin>>t;
            a.insert(t);
            hash[t]++;
        }
        // 删除0到n个数后的MEX个数
        vector<int> dp(n+1,1);
        // 仅有1个数
        if(a.size()==1) return dp;
        // 删除0个和删除n个都是1
        for(int i=1;i<n;i++){
            int k=i;    //操作次数
            for(int& j:a){
                if(k>=hash[j]) dp[i]++;
            }
        }
        return dp;
    }
    int main(){
        IOS;
        int t;cin>>t;
        while(t--){
            vector<int> res = solve();
            int n=res.size();
            for(int i=0;i<n;i++)
                cout<<res[i]<<(i!=n-1?" ":"");
            cout<<(t!=0?"\n":"");
        }
        return 0;
    }
    ```
    
    重新思考版
    
    ```cpp
    #include<bits/stdc++.h>
    using namespace std;
    #define IOS ios::sync_with_stdio(false);cin.tie(0);
    vector<int> solve(){
        int n;cin>>n;
        set<int> fmex;
        // 加入所有的值,找到第一个MEX
        for(int i=0;i<=n;i++)
            fmex.insert(i);
        // 记录每个值出现的次数
        unordered_map<int,int> cnt;
        for(int i=0;i<n;i++){
        	int t;cin>>t;
            cnt[t]++;
            fmex.erase(t);
        }
        unordered_map<int,vector<int>> f;
        // 记录对应次数出现的值
        for(pair<int,int> p:cnt)
            f[p.second].push_back(p.first);
        vector<int> dp; // 结果
        set<int> val;   // 推测可能所有的MEX
        // 初始MEX
        int mex=*fmex.begin();
        val.insert(mex);
        /*
            假设t是数组中的MEX,那t必须满足:
            [1]数组中必须有{0,...,t-1}的完整序列,即数组长度n-k要大于或等于t,使得可以容纳完整序列
            [2]若数组中存在t,则必须cnt[t]<=k,使得t可以被删掉
        */
        for(int k=0;k<=n;k++){
            // 使其拥有完整的{0....t-1}
            val.erase(n-k+1);
            for(int& i:f[k]){
                // 优化:i<=min(n-k,mex)
                if(i<=(n-k)&&i<=mex)
                    val.insert(i);
            }
            dp.push_back(val.size());
        }
        return dp;
    }
    int main(){
        IOS;
        int t;cin>>t;
        while(t--){
            vector<int> res = solve();
            int n=res.size();
            for(int i=0;i<n;i++)
                cout<<res[i]<<(i!=n-1?" ":"");
            cout<<(t!=0?"\n":"");
        }
        return 0;
    }
    ```