#include<bits/stdc++.h>
using namespace std;
using ll = long long;
#define ios ios::sync_with_stdio(false);cin.tie(0);

string solve() {
    
}

int main() {
    ios;
    int t; cin >> t;
    while (t--)
        cout << solve() << (t ? "\n" : "");
    return 0;
}