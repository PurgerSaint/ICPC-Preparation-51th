> 过题数降序的第一题
# 489B.BerSU Ball
[Rating]1200
[Tag]DFS、动态规划、图匹配、贪心、排序、双指针
[Time]思考用时17:34
## 题目介绍
The Berland State University is hosting a ballroom dance in celebration of its 100500-th anniversary! n boys and m girls are already busy rehearsing waltz, minuet, polonaise and quadrille moves.
We know that several boy&girl pairs are going to be invited to the ball. However, the partners' dancing skill in each pair must differ by at most one.
For each boy, we know his dancing skills. Similarly, for each girl we know her dancing skills. Write a code that can determine the largest possible number of pairs that can be formed from n boys and m girls.
> $1<=n<=100, 1<=m<=100, 1<=a_{i}<=100, 1<=b_{i}<=100$
## 提交情况
- 提交时间:16:22 状态:WA 详细状态:Wrong answer on test 5  
> 男生技能和女生技能都进行排序，然后用一个数组记录对应女生是否被选择  
用二分查找对应合适的女生，找不到直接退出(代表后续都无法满足)  
如果找得到，优先往前找相差不过1的，否则就包括当前往后找
- 提交时间:16:35 状态:AC 详细状态:-  
> 我虽然做了往前搜索的操作，但是我先前对于二分结果就判死了  
理应不用判死，直接用搜索就行
- 提交时间:16:37 状态:AC 详细状态:-  
> 就像作者题解一样，当前数据非常小，可以直接暴力

> 过题数降序的第二题
# 433B.Kuriyama Mirai's Stones
[Rating]1200
[Tag]动态规划、模拟、排序
[Time]思考用时10:55
## 题目介绍
Kuriyama Mirai has killed many monsters and got many (namely $n$) stones. She numbers the stones from $1$ to $n$. The cost of the i-th stone is $v_{i}$. Kuriyama Mirai wants to know something about these stones so she will ask you two kinds of questions:
1. She will tell you two numbers, $l$ and $r$ ($1≤l≤r≤n$), and you should tell her $\sum{r}{i=l}v_{i}$.
2. Let $u_{i}$ be the cost of the i-th cheapest stone (the cost that will be on the i-th place if we arrange all the stone costs in non-decreasing order). This time she will tell you two numbers, $l$ and $r$ ($1≤l≤r≤n$), and you should tell her $\sum{r}{i=l}u_{i}$.
For every question you should give the correct answer, or Kuriyama Mirai will say "fuyukai desu" and then become unhappy.
> $1<=n<=10^5, 1<=a_{i}<=10^9, 1<=m<=10^5, 1<=l<=r<=n, 1<=type<=2$
# 提交情况
- 提交时间:16:52 状态:AC 详细状态:-  
> 另存一个数组用于排序，然后对原数组和排序后的数组都做前缀和，直接根据题目要求输出对应数组的前缀和相减即可
- 提交时间:17:03 状态:AC 详细状态:-  
> 用树状数组代替前缀和  

> 过题数降序的第三题
# 327A.Flipping Game
[Rating]1200
[Tag]暴力枚举、模拟、动态规划
[Time]思考用时09:19，理解与扩展用时28:09
## 题目介绍
Iahub got bored, so he invented a game to be played on paper.
He writes n integers $a_{1}, a_{2}, \dots, a_{n}$. Each of those integers can be either 0 or 1. He's allowed to do exactly one move: he chooses two indices $i$ and $j$ ($1≤i≤j≤n$) and flips all values $a_{k}$ for which their positions are in range $[i,j]$ (that is $i≤k≤j$). Flip the value of $x$ means to apply operation $x=1-x$.
The goal of the game is that after **exactly** one move to obtain the maximum number of ones. Write a program to solve the little game of Iahub.
> $1<=n<=100, 0<=a_{i}<=1$
## 提交情况
- 提交时间:17:31 状态:WA 详细状态:Wrong answer on test 3  
> 既然要得到最多的1，且数组内只含0和1  
那我直接将数组压缩成对应数字和出现次数，找到出现次数最多的0，只翻转一次，得到最终答案即可
- 提交时间:17:36 状态:WA 详细状态:Wrong answer on test 3  
> 题目说必须要翻转一段，第一次提交中如果没出现0，我就不翻转了  
那我就翻转出现次数最少的那个1即可  
- 提交时间:17:38 状态:WA 详细状态:Wrong answer on test 3  
> 不应该只翻转出现次数最少的那个1，应该只翻转其中一个1就行
- 提交时间:17:52 状态:AC 详细状态:-  
> 在看题解和Gemini指导下，这题应该使用动态规划的最小子段和来解决问题  
我设置 $cnt[i]$ (`cnt`用`pair<int,int>`存储，第一个字段代表`0`的个数，第二个字段代表`1`的个数)  
然后再设置 $dp[i][j]$ 代表从 $i$ 到 $j$ 区间内的翻转收益(`0`的个数减去`1`的个数)  
最后寻找最大翻转收益，加上原有和就行  
同时我的自创方法还犯了以下三个错误:  
> 1. 数组坐标越界  
> 2. 公式中坐标使用错误  
> 3. 漏算长度为`1`的区间
- 提交时间:18:00 状态:WA 详细状态:Wrong answer on test 5  
> 我将我第一次提交时的压缩版本做动态规划版本，对于区间为1的，其收益为负的次数，0则为正的次数  
同上一个提交一样计算最大值，输出总和加上最大值即可  
- 提交时间:18:03 状态:AC 详细状态:-  
> 我犯了另一个动态规划的错误，使用我这个修改后的动态规划，应该要保存状态  
我忘记保存每个区间应有的收益，获取到的只是每个连续子段区间的收益  
而不是真正连续含不同字符子段的收益

> 过题数降序的第四题
# 1352B.Same Parity Summands
[Rating]1200
[Tag]数学、构造
[Time]思考用时14:34(+1h,吃饭)
## 题目介绍
You are given two positive integers $n$ ($1 \le n \le 10^9$) and $k$ ($1 \le k \le 100$). Represent the number $n$ as the sum of $k$ positive integers of the same parity (have the same remainder when divided by $2$).
In other words, find $a_1, a_2, \ldots, a_k$ such that all $a_i \gt 0$, $n = a_1 + a_2 + \ldots + a_k$ and either all $a_i$ are even or all $a_i$ are odd at the same time.
If such a representation does not exist, then report it.
> $1<=t<=1000, 1<=n<=10^9, 1<=k<=100$
- 提交时间:19:27 状态:AC 详细状态:-  
> 本题存在以下无法通过的特例:
> 1. $k$ 比 $n$ 大，而`0`不是正整数
> 2. 奇数 $n$ 使用偶数 $k$ 个表示，因为无论是 $k$ 个奇数还是偶数，总和必定是个偶数  
> 本题可以直接用`1`和`2`进行直接拼接  
如果 $n$ 是奇数，只能用`1`进行拼接，且此时必定拼接成功
> - 因为 $n$ 和 $k$ 是奇数, $k-1$ 必定为偶数, 那么剩下的 $n-(k-1)$ 必定大于0且是奇数   
> 如果 $n$ 是偶数，
> - 当 $k$ 是偶数，用`1`拼接, $n-(k-1)$ 为奇数
> - 当 $k$ 是奇数且 $n-(2*(k-1))$ 为偶数且大于0，用`2`拼接
> 其他情况一律不行。其他的数可能也有答案，但是太过于麻烦

# ABC比赛情况：
> 比赛时间: 2026-07-11 20:00:00(UTC+8)  
> 比赛名称: ABC 466  
> 比赛结果: [Rank]8608 [Performance]297 [New-Rating]547 [Diff]-29  

# A.Compromise 
## 题目介绍
给与 $N$ 个幸福值，第 $i$ 个幸福值代表 $X_{i}$ 幸福值，你只能选择一个  
问是否不管怎么选，你的幸福感都会为负，则输出`Yes`，否则输出`No`
> $2<=N<=10, -100<=X_{i}<=100$
## 提交情况
- 提交时间:20:01:59 状态:WA 详细状态:`WA*16`
> 直接寻找数组中是否存在有大于或等于0的元素，有就输出`NO`,否则输出`YES`
- 提交时间:20:04:32 状态:WA 详细状态:`AC*11 WA*13`
> 居然还错了，我去除了数组保存，看看是不是因为BUG
- 提交时间:20:06:51 状态:AC 详细状态:-  
> 我以为是`bool`变量我用`1`和`0`进行修改的问题，结果是我的输出的问题  
由于之前写CF导致我以为不认大小写的输出，忘记题目要求的大小写配合了

# B.Representative Balls
## 题目介绍
有 $N$ 个球，第 $i$ 个球的颜色为 $C_{i}$ ,大小为 $S_{i}$  
输出每个颜色中最大球的尺寸，如果该颜色没有球，输出`-1`
> $1<=N,M<=100, 1<=C_{i}<=M, 1<=S_{i}<=M$
## 提交情况
- 提交时间:20:11:18 状态:AC 详细状态:-  
> 直接开二维数组，像图论存点一样存每个球的大小，最后对存入的球排序找到最大值就行

# C.Count Close Pairs
## 题目介绍
**交互式问题**
在数轴上，点 $1,2,\dots,N$ 从左到右按此顺序排列  
最初，你只得到整数 $N$  
然后，你最多可以向法官提出以下问题 $2N$ 次:
- 选择满足 $1<=i<j<=N$ 的整数 $i$ 和 $j$ ,并询问 $i$ 和 $j$ 之间的距离是否至多为1  
输出距离至多为1的点对的数量，即满足 $1<=i<j<N$ 的整数对 $(i,j)$ 的数量，使得点 $i$ 和 $j$ 之间的距离至多为1
> $2<=N<=10^3$
**介绍**
首先，从标准输入接收表示分数的整数 $N$ :`N`  
然后，你可以向法官重复问题陈述中描述的问题最多 $2N$ 次  
将问题以`? i j`的格式输出到标准输出，其中 $i$ 和 $j$ 必须是满足 $1<=i<j<=N$ 的整数  
作为对此的响应，标准输入将输出以下内容之一:`Yes`,`No`  
这里，`Yes`表示点 $i$ 和 $j$ 之间的距离至多为1，`No`表示点 $i$ 和 $j$ 之间的距离大于1  
找到问题的答案 $X$ 后，请按`! X`的格式输出答案，然后立即终止程序
**提示**
每次输出内容后，请在末尾插入换行符并清空标准输出。否则，判别器可能会报错（超时）。  
如果在交互过程中输出无效内容，或者程序提前终止，则判罚结果不确定。  
输出答案后立即终止程序。否则，判罚结果将无法确定。  
分数和答案的排列方式在你和裁判互动开始时就已经确定，不会因为你提出的问题而改变。
## 提交情况
- 提交时间:20:20:26 状态:WA 详细状态:`WA*32`
> 依次排序的 $N$ 个点，那答案不就是显而易见的 $n-1$ 吗  
照个样子随便问问，然后直接输出 $n-1$ 即可
- 提交时间:20:28:59 状态:WA 详细状态:`WA*32`
> 不对，他既然这么问了，必然每个相邻点的距离不一样  
那么就问 $n-1$ 次，问每个 $(i,i+1) (1<=i<=n-1)$ ，然后如果是`Yes`，就计数，最后输出计数的结果即可
- 提交时间:20:29:50 状态:WA 详细状态:`WA*32`
> 不对，漏空格输出了，补上即可  
依次排序的 $N$ 个点，如果相邻的距离大于1，那么这个点不可能与其他的点距离不超过1的
- 提交时间:23:35:07 状态:AC 详细状态:-  
> 经GPT指导下，这题应该使用双指针  
用`r`保持右边界，每次不断询问 $(i,r+1)$  
如果 $i$ 和后续4个点距离都不超过1，由于按序排列，后三个点之间的距离应该也不超过1  
那么每个点 $i$ 可以构成的点对数量就是 $r-i$

# D.Placing Rooks
## 题目介绍
存在一个 $N\tiems N$ 的网格,最初网格上没有任何东西  
从现在开始，高格会按顺序对网格执行 $M$ 次操作，第 $i(1<=i<=M)$ 次操作如下:
- 移除放置在从上往下第 $R_{i}$ 行单元格上的所有方块
- 移除放置在左侧第 $C_{i}$ 列单元格上的所有方块
- 最后，将一块方块放置在从上往下第 $R_{i}$ 行、从左往右第 $C_{i}$ 列单元格上  
输出经 $M$ 次操作后放置在网格上的方块数量
> $1<=N<=3\times 10^5, 1<=M<=3\times 10^5, 1<=R_{i}<=N, 1<=C_{i}<=M$
## 提交情况
- 提交时间:20:38:24 状态:WA 详细状态:`AC*11 WA*13`
> 倒序处理所有操作，并记录已经放置的行和列  
即后面的方块一定会在方格中，且前面的方块一定不能与后面的方格同行或同列
- 提交时间:20:48:20 状态:WA 详细状态:`AC*11 WA*13`
> 我将数组用倒序+删除的方式访问了
- 提交时间:20:52:23 状态:WA 详细状态:`AC*11 WA*13`
> 把经历过的行和列用数组标记，而非用之前的`set`进行标记查找了
- 提交时间:23:35:07 状态:AC 详细状态:-  
> 经gpt指导下，我的方法完全正确，问题在于跳过的方块我没标记他的行和列  
虽然当前方块不用放，但是他的行和列也会被清除，也会影响之前的方块，我忘记这一点了

# E.Range Flip
## 题目介绍
有 $N$ 张牌依次排成一排  
卡片 $i$ 的正面写着整数 $A_{i}$ ,背面写着整数 $B_{i}$。初始时,所有牌均正面朝上  
你最多可以执行以下操作 $K$ 次:
- 选择满足 $1<=l<=r<=N$ 的整数 $l$ 和 $r$ , 翻转 $l$ 和 $r$ 中的每一张卡片 $i(l<=i<r)$ ,使其牌面朝下的一面朝上  
求卡片朝上的最大可能和
> $1<=N<=2\times 10^5, 1<=K<=10, 1<=A_{i},B_{i}<=10^9$
## 提交情况
- 提交时间:21:04:19	 状态:WA 详细状态:`AC*3 WA*2 TLE*3 MLE*23`
> 这题做法同327A一样，先算出每一张的收益(背面数字减去正面数字)，然后用动态规划  
其`dp[i][j]`的定义同327A一样，然后选出所有不为0的收益，并放到另一个数组中对收益进行降序  
然后选取前K个即可  
- 尝试修改
提交后我发现问题，一个是选择会有重叠(不像327A一样是选一次)，另一个是样例中可以选择已经翻过来的(二次选择)  
而我提交这一次恰好是碰巧过了样例(其实正确选择的区间会有重叠)  
对于MLE，我选择不定义过大的DP数组  
然后我优化后的方法，即保存翻转后的代价和未翻转的代价，如果有翻转，用未翻转里最大的，否则用翻转后最大的  
但这个方法一直在运行，没有任何输出  
- 提交时间:23:00:56 状态:AC 详细状态:-  
> gpt5.6-luna说是`k个互不重叠的最大区间`问题，我照着他的代码抄了一遍，但是思路还没有完全理解