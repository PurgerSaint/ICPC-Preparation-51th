# 
[Rating]
[Tag]
[Time]
## 题目详情

> $$
## 提交详情
- 提交时间: 状态: 详细状态:``
> 

# 2256B.Domino Tiles
[Rating]-
[Tag]模拟、数学
[Time]补题23:16
## 题目详情
Nygglatho returns from the market with an old box of tiles whose painted marks have begun to fade. Before she can put it away, Chtholly and the young fairies have already spread the tiles across the dining table and turned them into a puzzle.
There is a row of $n$ tiles. Each tile should be marked with either $\mathtt 0$ or $\mathtt 1$. However, some of the marks have faded away.
The current row is represented by a string $s$ of length $n$. Each character of $s$ is $\mathtt{0}$, $\mathtt{1}$, or $\mathtt{?}$. Chtholly must replace every $\mathtt{?}$ with either $\mathtt{0}$ or $\mathtt{1}$.
After replacement, for every $1 \le i \lt n$, the two neighboring tiles $s_i$ and $s_{i+1}$ form a domino of weight $(s_i + s_{i+1})$. Note that two consecutive dominoes share exactly one tile. The completed row is valid if every two consecutive dominoes have different weights.
Determine the number of different$^{\text{∗}}$ ways to replace all $\mathtt{?}$ characters so that the completed row is valid. Output the answer modulo $998\,244\,353$.
$^{\text{∗}}$Two ways of replacement are considered different if the resulting strings are different.
**Input**
Each test contains multiple test cases. The first line contains the number of test cases $t$ ($1 \le t \le 10^4$). The description of the test cases follows.
The first line of each test case contains one integer $n$ ($2 \le n \le 2 \cdot 10^5$) — the number of tiles.
The second line contains the string $s$ of length $n$, where $s_i= \mathtt{0}$, $\mathtt{1}$, or $\mathtt{?}$.
It is guaranteed that the sum of $n$ over all test cases does not exceed $2 \cdot 10^5$.
**Output**
For each test case, output one integer — the number of valid ways to replace all $\mathtt{?}$ characters, modulo $998\,244\,353$.
## 提交详情
- 提交时间:(比赛时)02:08:22 状态:WA 详细状态:`Wrong answer on pretest 2`
> 预先定义四种状态的代号:
```cpp
if (s[i] == '0' && s[i + 1] == '0') j = 1;
if (s[i] == '0' && s[i + 1] == '1') j = 2;
if (s[i] == '1' && s[i + 1] == '0') j = 3;
if (s[i] == '1' && s[i + 1] == '1') j = 4;
```
然后用`x`记录已有不同状态的个数，并记录`?`和非`?`所有的下标  
再枚举从 $2$ 到 $5$ 长度下所有可能的序列:
```cpp
ans[5] = {"10011", "11001", "01100", "00110"};
ans[4] = {"1100", "0011", "1001", "0110", "1101", "0010", "1011", "0100"};
ans[3] = {"110", "011", "101", "001", "100"};
ans[2] = {"00", "01", "10", "11"};
```
对于长度为 $[2,5]$ 之内的，根据非`?`下标匹配对应字符串个数即可  
无法显式构造长度达到6及以上符合条件的序列，因为要保证恰好四种状态存在，序列至多5长度  
推论:对于长度超过 $5$ 的序列，不存在含3个及以上`?`或两个非连续`?`的构造方法  
对于单个`?`，若其在两侧，则需要至少一种状态不存在；若其在内部，则需要两种不同状态不存在  
对于两个`?`，可知其会需要2种不同状态(连续且在两侧)、3种不同状态(连续且在内部)  
而对于非连续的两个`?`，至少需要4种不同的状态，在当前状态下与题目条件不符  
三个及以上`?`需要构造的不同状态更多，故更不符合题目条件 
再对于两个连续在内部的`?`，若左右字符不同，且此时长度超过5，必然不会有低于3种不同状态的情况出现，故也不行  
枚举单个`?`:  
`_0`的情况:`10`(3)、`00`(1)  
`_1`的情况:`01`(2)、`11`(4)  
`0_`的情况:`01`(2)、`00`(1)  
`1_`的情况:`10`(3)、`11`(4)  
`0_0`的情况:`010`(2和3)  
`1_1`的情况:`101`(2和3)  
`0_1`的情况:`001`(1和2)、`011`(2和4)  
`1_0`的情况:`100`(1和3)、`110`(3和4)  
枚举两个连续`?`:
`__0`的情况:`110`(3和4)、`100`(1和3)、`010`(2和3) 
`__1`的情况:`001`(1和2)、`101`(2和3)、`011`(2和4)  
`0__`的情况:`010`(2和3)、`011`(2和4)、`001`(1和2) 
`1__`的情况:`101`(2和3)、`100`(1和3)、`110`(3和4)  
`1__1`的情况:`1001`(1和2、3)、`1101`(2和3、4)、`1011`(2和3、4) 
`0__0`的情况:`0110`(2和3、4)、`0010`(1和2、3)、`0100`(1和2、3) 
`1__0`的情况:`1100`(1和3、4)  
`0__1`的情况:`0011`(1和2、4)
- 提交时间:02:14:00 状态:AC 详细状态:`-`
> 我犯的错误，就是将*相邻两个牌组成的权重不相同*看成了*所有状态不相同*  
即对于`abc`，仅要求 $a+b\equle b+c$  
那么此时可构造得到`110`、`011`、`001`、`100`
设从`110`开始扩展，由后两个字符可以得到第四个字符`0`，那么接着就是第五个字符`1`  
故可以扩展直到`11001100`  
那么直接根据这四个开头，构造四个长度为 $n$ 的序列即可，然后比较这四个序列哪个跟题目符合就可以  
看*GPT*的提示后知道的