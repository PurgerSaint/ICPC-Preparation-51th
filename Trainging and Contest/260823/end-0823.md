1. 单人VP
> 比赛名称: The 2022 ICPC Asia Regionals Online Contest (II)   
> 历史比赛该场结果(以提交数降序):   
[E] 2353/5962  
[J] 1825/7610  
[F] 1096/7928  
[A] 628/1929  
[B] 579/4972  
[K] 395/1445
[G] 354/1041  
[L] 201/821  
[H] 33/245  
[D] 31/154  
[C] 7/170  
[I] 7/264
> 比赛开始时间: 15:10:00
> 比赛结束时间: 19:24:26
> VP结果:[Solved]1 [Time]14 [Rank]1772  
[E]00:14  
[F]-3
# E.An Interesting Sequence
## 题目详情
You should generate a sequence of positive integers of length $n$.
Of course,this sequence needs to meet some requirements.
*   $\forall \ i \in [1,n] \ \ a_i>1$
*   $\forall \ i \in [2,n] \ \ gcd(a_{i-1},a_i)=1$
*   $a_1=k(k>1)$
$gcd(x,y)$ means the greatest common divisor of $x$ and $y$.
You need to find the minimum value of $\sum_{i=1}^{n} a_i$ .
*输入格式*
The only line contains two integers  $n,k$
$2\le n\le 10^8,2 \le k\le 10^8$
*输出格式*
One integer — the answer to the problem. 
## 提交详情
- 提交时间:15:24:38 状态:AC 详细状态:`-`
> 构造一个序列，第一项为 $k$ ，后续每一项都要大于1，且每一项与前一项的最大公因数是1  
即相邻两个数要互质，输出这个序列总和的最小值  
测试想到的第一个方法，2和3交替排列，因为不能为1  
如果 $k$ 是偶数，第二个就放3，第三个就放2，依此类推    
如果 $k$ 是奇数，第二个就放2，第三个就放3，依此类推  
是否符合条件?每一项大于1，满足；第一项为 $k$ ，满足；任两项最大公因数为1， $gcd(2,3)=1$ ，奇数和偶数不一定互质  
这里有特例，如果 $k$ 是奇数，就不用管  
如果 $k$ 是偶数，就要考虑 $k$ 可能是3的倍数，如6、12，这时最好的方法，就给第二个设定为5即可  
对于样例 $n=2,k=5$ ，正好就是`5 2`  
对于手动构造样例 $n=3,k=6$，就得`6 5 2`  
手动样例`3 5`，预期目标序列`5 2 3`，总和为10，输出通过  
手动样例`4 6`，预期目标序列`6 5 2 3`，总和为16，输出通过  
手动样例`4 9`，预期目标序列`9 2 3 2`，总和为16，输出通过  
手动样例`4 12`，预期目标序列`12 5 2 3`，总和为22，输出通过  

# J.A Game about Increasing Sequences
## 题目详情
Alice and Bob like playing games.
The game is played on a sequence of length $n$. Alice and Bob take turns performing the operation, with Alice going first.
In each operation, the player can remove an element from the beginning or the end of the sequence.
If this operation is not the first operation of the game, the removed element must be strictly greater than all the previously removed elements.
The player who cannot perform the operation loses.
Please determine who will win the game if both Alice and Bob play the game optimally.
*输入格式*
The first line contains a single integers $n(1 \leq n \leq {10}^{5})$,representing the length of the sequence.
The second line contains $n$ integers $a_{1},a_{2},...,a_{n}(1 \leq a_{i} \leq {10}^{5})$,representing the sequence.
*输出格式*
For each test case, print "Alice" if Alice will win the game, otherwise print "Bob".
## 提交详情
- 提交时间: 状态: 详细状态:``
> 第一眼看上去有点博弈论的样子啊  
可以从队头或队尾取出一个元素  
自第二次操作开始，拿取的元素必定要大于之前拿取的所有元素  
赢的条件为对方没有操作，即不能选，或队列为空  
爱丽丝先手，鲍勃后手，两者都是精明玩家(最优策略)，问最后谁会赢  
按模拟的心思，维护拿掉的最大值和现有最大值  
设队列最大值位于队尾自后往前第 $x+1$ 个，若 $x$ 为奇数，爱丽丝会始终维护 $x$ 为奇数，即当 $x$ 为偶数时，他会在前面拿一个，鲍勃同理相反  
但这样太麻烦了  
对于题目样例`1 3 2`，如果爱丽丝先拿`2`，然后鲍勃拿`3`，这样鲍勃赢  
如果爱丽丝先拿`1`，爱丽丝要赢必须得让鲍勃拿`2`，可是鲍勃可以直接拿`3`赢得此局  
即题目关键还是在于队列最大值的位置  
构造样例`1 2 4 3`，爱丽丝和鲍勃要赢必须要拿到`4`  
爱丽丝要赢，必须先要拿`1`，然后鲍勃可以拿`2`或拿`3`，此时不管鲍勃拿啥，爱丽丝都可以在下一步拿到`4`  
相反，如果爱丽丝先拿`3`，这会使得鲍勃直接拿到`4`获得胜利  
再构造样例`1 4 2 3`，爱丽丝要赢也是只能拿`3`，然后不管鲍勃拿啥，爱丽丝都能下一步拿到`4`，相反，爱丽丝如果先拿`1`会使得鲍勃胜利  
不对不对，我漏东西了，对于`1 3 2`，如果爱丽丝直接拿`2`，他就赢了，因为鲍勃没有可以拿的了  
对于`1 2 4 3`和`1 4 2 3`也同理  
再构造样例`1 3 4 2`，这样爱丽丝拿`1`才会最终赢得胜利  
对于样例`1 3 5 2 4`，爱丽丝先拿`4`就会直接胜利(没有可拿的)  
对于样例`1 3 5 4 2`，爱丽丝唯一能赢的操作路径为`2 -> 4 -> 5`
除非爱丽丝抽风，选择了`1 -> 2 -> 3/4 -> 5`，这样鲍勃才能赢  
设队列最大值离尾部 $x$ ，离头部 $y$ 
对于样例`1 3 4 5 2`，按题目的操作路径为`1 -> 3 -> 4 -> 5`  
我感觉这题不用去模拟过程的，肯定可以通过规律直接解题的  
对于`1 3 5 4 6 2`，爱丽丝赢的路径为`1 -> 3 -> 5`，或`1 -> 2 -> 6`
对于`1 3 5 6 4 2`，爱丽丝赢的路径为`2 -> 1 -> 3 -> 4/5 -> 6`，亦或是`1 -> 3 -> 2 -> 4/5 -> 6`  
但是我不能直接根据最大值来选择，因为可以选择次大值来使得对方无元素可操作

# F.Infinity Tree
## 题目详情
You have a rooted tree with infinite number of nodes.
At the beginning, the tree has only one node (numbered 1 and is the root node), and every second, each node will add $k$ child nodes. Specifically, if at the $xth$ second this tree has $p$ nodes, and at $(x+1)th$ second, the number of nodes will become $(k+1)\times p$. For the node numbered $y(1\leq y\leq p)$, the generated $k$ child nodes are numbered in $[p+(y-1)\times k+1,p+y\times k]$.
Now you want to know the lowest common ancestors of two nodes $x,y$.
For two nodes $u$ and $v$ of a rooted tree $T$, the lowest common ancestors $LCA(T, u, v)$ represents a node $x$ such that $x$ is the ancestor of $u$ and $v$ and the depth of $x$ is as large as possible. Here, a node can also be its own ancestor.
*输入格式*
The input consists of multiple test cases.
The first line contains a single integer $T(1\leq T \leq 10^5)$, indicating there are $T$ test cases.
In each test case:
The only line contains three positive integers $k,x,y(2\leq k,x,y \leq 10^{18})$, representing the parameters and query nodes of the tree.
*输出格式*
For each case,the output contains a single number, indicating the the lowest common ancestors of the query nodes.
## 提交详情
- 提交时间:16:51:23 状态:TLE 详细状态:`-`
> 我嘞个豆，LCA，我就应该先学再VP的  
每个节点每秒都会增加 $k$ 个子节点，使得节点 $y(1\le y\le p)$ 会生成节点 $[p+(y-1)*k+1, p+y*k]$  
求任意 $k$ 下 $x,y$ 的最近最先公共祖先  
感觉不用LCA，这题貌似是(离散)数学题  
对于初始仅有的节点`1`，会生成 $[2, 1+k]$ ，使得第一秒有 $k+1$ 个节点  
第二秒时，会产生高达 $(k+1)*k+(k+1)=(k+1)^2$ 个节点  
第三秒时，会产生高达 $(k+1)^{2}*k+(k+1)^2=(k+1)^3$ 个节点  
对于第 $i$ 秒，产生的节点数为 $(k+1)^i$ ，生成的节点编号范围为 $[(k+1)^{i-1}+1, (k+1)^i]$ 
设上一秒有 $p$ 个节点，那么此时生成的第 $1$ 个到第 $k$ 个节点是第一个1节点产生的，可得第 $j$ 个节点再此时产生的节点范围为 $[(k)^{j-1}+1, k^j]$  
那么我先找到 $x,y$ 所有的根节点(先找在哪层，然后就能找到其父节点)，然后直到遇到第一个相同的即可  
手动构造样例`9999 1 1`，输出1，通过  
- 提交时间:17:06:52 状态:TLE 详细状态:`-`
> 我是先找出 $x$ 的所有根节点，再回溯 $y$ 的根节点，判断 $y$ 的根节点是否在 $x$ 的根节点之中  
一开始我就枚举所有 $k+1$ 的次方，直到超过 $x,y$ 的最大值  
我当前修改成先枚举 $x,y$ 中小的那个的根节点，再枚举大的，使得`set<ll>`的插入操作最少化  
但这样有可能跟原先一样，耗时的大头还是在回溯根节点  
> 优化方法未知，先搁置  
- 提交时间:17:45:22 状态:TLE 详细状态:`-`
> 要么就同时推进，先让 $x,y$ 处于同一层(同一秒产生的节点)中(高层的节点往前推进，使其根节点与小的那个同层)，然后再同时推进，目标就是两个节点相同  
> 这还是超时，那就得上数学规律秒杀了，赛时代码如下:  
```cpp
ll solve() {
    ll k, x, y; cin >> k >> x >> y;
    vector<ll> ak;
    ll c = 1;
    while (true) {
        ak.push_back(c);
        if (c > max(x, y)) break;
        c *= (k + 1);
    }
    if (x == y) return x;
    auto get_leave = [&](const ll& g) -> ll {
        auto it = lower_bound(range(ak), g);
        return it - ak.begin();
    };
    ll x_leave = get_leave(x), y_leave = get_leave(y);
    // cout << x_leave << " " << y_leave << "\n";
    auto up_leave = [&](ll& g, ll& leave, const ll& target) {
        while (true) {
            auto it = lower_bound(range(ak), g);
            leave = it - ak.begin();
            // cout << g << " " << leave << " " << target << "\n";
            if (leave <= target || leave == 0) return;
            auto pre = prev(it);
            g -= *pre;
            g = g / k + (g % k > 0);
        }
    };
    // cout << x << " " << x_leave << "\t" << y << " " << y_leave << "\n";
    while (x != y) {
        if (x_leave > y_leave) up_leave(x, x_leave, y_leave);
        else if (y_leave > x_leave) up_leave(y, y_leave, x_leave);
        else {
            up_leave(x, x_leave, x_leave - 1);
            up_leave(y, y_leave, y_leave - 1);
        }
    }
    return x;
}
```
- 提交时间:(08/24)00:33:36 状态:AC 详细状态:`-`
> 经*GPT*指导后，我的方法完全正确，错误在于`c *= (k + 1)`时如果`k+1`过大会导致溢出long long，检测溢出边界后即可通过

# A.Yet Another Remainder
## 题目详情
There is a positive integer $x$($1\le x<10^{1000000}$) in the computer, but now you don't know it. Since the integer is extremely large, it is represented as a string $a_1a_2a_3...a_n$, where the integer $n$ denotes the number of digits in $x$. For example, integer $123$ can be represented as $a_1=1,a_2=2,a_3=3$.
Now you don't want to know the exact value of integer $x$, but the remainder of $x$ when it is divided by another positive integer $p$. Formally, you should calculate $x\ mod \ p$, where $mod$ means the remainder of the division.
To get information, you can ask computer some questions: $Q(l,r,k)$, ask the sum of the digits from $l$ to $r$, step by $k$. Formally, you will get $a_l+a_{l+k}+a_{l+2k}+...+a_r$.
Now you have asked computer some questions in $\min(100,n)$ rounds. In the $i^{th}$ round, you thrown $i$ questions: in the $j^{th}$ question in this round, you ask $Q(j,i\times \lfloor\frac{n-j}{i}\rfloor+j,i)$, where $\lfloor x\rfloor$ means the greatest integer less than or equal to $x$. In other word, let $l$ be $j$, let $r$ be $i\times \lfloor\frac{n-j}{i}\rfloor+j$, you will get $a_l+a_{l+i}+a_{l+2i}+\ldots+a_r$.
Hint: here $r$ means the greatest integer satisfying $r=l+k\cdot i,k\in R$ and $r\le n$.
Finally, you should answer $q$ queries. In each query, you are given a prime number $p$(with special limit:$p=3$ or $7\le p\le 97$), and you should tell computer $x\ mod\ p$ as the answer.
*输入格式*
There are multiple test cases.
The first line contains an integer $t$($1\le t\le 100$), the number of test cases.
The first line of each test case is an integer $n$($1\le n\le10^6$), the number of digits of the hidden number.
The following $\min(100,n)$ lines are about the questions.
The $i^{th}$ line contains $i$ integers $b_{i,1},b_{i,2},\ldots,b_{i,i}$($0\le b_{i,j}\le10^7$), $b_{i,j}$ represents the answer of the $j^{th}$ question of the $i^{th}$ round.
It is guaranteed that there exists a number fits all the answers of the questions.
The next line contains an integer $q$($1\le q\le 10)$, the number of queries.
Then $q$ line follows, each line contains a prime number $p$($p=3$ or $7\le p\le 97$), the modulus of the query.
*输出格式*
For each query, print your answer in a single line.
## 提交详情
- 提交时间: 状态: 详细状态:``
> 在知道每位起始，往后若干位的数位之和下，拼凑出这个数，并求得这个数模另一个数的答案  
问题在于，这个数可能很大，纵使拼出来了，也不能完整模出对应答案  
除非只拼凑出最高模的位数或高一位(比模小)  
不行，虽然题目明牌给你这个数的所有位，但是不能完全根据位来求得模的答案  
不对，好像位和质数有点关系  
设质数为97，还是不对，$10^{98}\bmod 97$ 不为0啊  
继手动模拟除法后，遇到手动模拟取余了  
手动构建，直到遇到近几位拼出来的数为对应质数的倍数？题目没说，这不一定存在  
要模拟取余，就要找到这个质数和 $x$ 的最小公倍数  
我模拟了 $3$ 和 $7$ 的周期，发现他们都是在 $[p,10*p]$ 内保持一个周期  

# B.Non-decreasing Array
## 题目详情
You are given a non-decreasing array of integers $a_1,a_2,…,a_n$. In one operation, when the current length of the array is $m$:
*   Firstly you can choose an index $i(1<i<m)$ and delete $a_i$ $(m$ decrease $1)$  or you can do nothing,
*   Secondly you can choose an index $i(1<i<m)$ and change $a_i$ to any integer.
You should ensure that the array is non-decreasing after every delete or change.
Now you want to know that after operating $k(1\le k \le n)$ times, when the current length of the array is $m$, what is the maximum value of $\sum_{i=2}^{m} {(a_i-a_{i-1})^2}$.
You need to answer for each $k(1\leq k\leq n)$, different queries are independent of each other.
*输入格式*
The first line contains one integer  $n(3\le n  \le 100)$.
The second line contains $n$ integers $a_1,a_2,...,a_n(-10^9 \le a_i \le 10^9)$.
*输出格式*
Output $n$ lines, each of which contains a single integer—the $i$-th number is for the answer of $k=i$. 
## 提交详情
- 提交时间: 状态: 详细状态:``
> 每次操作我可以删除其中一个数，或者将某个数修改为任意一个整数  
bur，任意整数，那我要是改成LLONG_MAX不是也可以吗  
题目关键*每次删除或修改后数组仍然是非递减的*，那就不行啦  
即我要修改 $a_{i}$ ，只能将其改为 $[a_{i-1}, a_{i+1}]$ 中任意一个数  
有个贪心的方法，是否最后都是 $(a_{max}-a_{min})^2$  
我感觉不用论证， $(a_{n}-a_{1})^2$ 一定大于 $(a_{2}-a_{1})^2+\dots+(a_{n}-a_{n-1})^2$  
不对啊，我想个蛋啊，公式题  
对于 $\displaystyle \sum_{i=2}^{m} (a_{i}-a_{i-1})^2$ ，其扩展如下:  
$=a_{2}^2-2*a_{2}*a_{1}+a_{1}^2+a_{3}^2-2*a_{3}*a_{2}+a_{2}^2+\dots+a_{n}^2-2*a_{n}*a_{n-1}+a_{n-1}^2$  
$\displaystyle =\sum_{i=1}^{m}(a_{i})+\sum_{i=2}^{m-1}(a_{i})-2*(a_{1}*a_{2}+\dots+a_{n}*a_{n-1})$  
还是要想，公式找不出规律，而且我感觉，要是无限拉高 $a_{n}$ 也不是不行  
目前最大的问题，就是我没看懂样例的`1 2 3 4 5`为啥能到输出`10 16 16 16 16`  
`i=5`输出`16`我可以通过删除`2 3 4`，剩下`1 5`得到   
但 $i\in[2,3]$ 下得到`16`, $i=1$ 得到 $10$ 我也没想通  
纠正之前的操作，所有操作只能操作中间的数($1<i<n$)  

2. LCA学习
# P3379 【模板】最近公共祖先（LCA）
[Rating]
[Tag]
[Time]-
## 题目详情
如题，给定一棵有根多叉树，请求出指定两个点之间最近的公共祖先。
*输入格式*
第一行包含三个正整数 $N,M,S$，分别表示树的结点个数、询问的个数和树根结点的序号。
接下来 $N-1$ 行每行包含两个正整数 $x, y$，表示 $x$ 结点和 $y$ 结点之间有一条直接连接的边（数据保证可以构成树）。
接下来 $M$ 行每行包含两个正整数 $a, b$，表示询问 $a$ 结点和 $b$ 结点的最近公共祖先。
*输出格式*
输出包含 $M$ 行，每行包含一个正整数，依次为每一个询问的结果。
## 提交详情
- 提交时间:00:12:10 状态:AC 详细状态:`-`
> 我照着*Gemma*给我的LCA代码读了一遍，理解了一遍  
LCA的方法跟我J题想的方法差不多，都是先对齐，然后齐头并进(同时推进)，直到两结点相等(同一节点，即公共祖先)  
但不过LCA存储其父节点的方法有点奇妙，他是直接存自当前节点往前跳跃多少层后的结果  
初始化要用DFS来存储每个点的深度，和其向前跳跃多少层的最终结果  
```cpp
for (int i = 1; i < LOGN; i++)
    f[u][i] = f[f[u][i - 1]][i - 1];
```  
直接通过间接跳跃来实现找到目标，这里的 $i$ 代表向前跳 $2^i$ 步  
即这里我先往前走，跳2步到达 $i-1$ ，访问`f[u][i-1]`再回来，实现跳了 $2^{i-1}*2=2^i$ 步到达目标节点  
然后LCA存的是每 $2^i$ 步，因为任何整数可以通过二的幂，即二进制来表示  
> 复盘失败，错在我还没有真正理解LCA的推进方法